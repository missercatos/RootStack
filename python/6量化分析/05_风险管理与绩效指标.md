# 风险管理与绩效指标 (Risk & Performance)
---

## 📖 章节概述

收益率只是故事的一半，风险才是另一半。本章系统讲解量化策略的绩效评估体系：Sharpe 比率、Sortino 比率、最大回撤、Calmar 比率、Alpha/Beta、VaR（在险价值）。每个指标都给出数学公式和 NumPy 实现，并讨论 CAPM 模型和风险平价的基本概念。这是从"看看赚了多少"到"科学评估策略质量"的关键一步。

> **核心理念**：金融中的"风险"不是模糊的直觉，而是可以用数学定义的量。在 C 程序员眼中，这些指标不过是对收益率序列做几个统计运算：均值、标准差、最小累计值、分位数——都是 NumPy 一行代码的事。关键是要理解每个指标背后的假设和局限性。

---

### 📚 第一节：收益率的基本处理

#### 1.1 简单收益率与对数收益率

```bash
python -c "
import numpy as np
import pandas as pd

# 模拟每日价格
np.random.seed(42)
prices = 100 + np.cumsum(np.random.randn(1000) * 2)
prices = np.maximum(prices, 10)

# 简单收益率
simple_ret = (prices[1:] - prices[:-1]) / prices[:-1]

# 对数收益率
log_ret = np.log(prices[1:] / prices[:-1])

# 两者在很小的时候几乎相等
print('Simple return (first 5):', simple_ret[:5])
print('Log return (first 5):  ', log_ret[:5])
print()
print('Differences are tiny for daily returns')
print(f'Max diff: {np.max(np.abs(log_ret - simple_ret)):.10f}')
"
```

对数收益率的优势：时间可加性。N 天对数收益 = 各天对数收益之和。简单收益率则需连乘。

#### 1.2 年化计算

```bash
python -c "
import numpy as np

# 假设交易日在 252 天/年
trading_days = 252

np.random.seed(1)
daily_returns = np.random.randn(1000) * 0.01  # 日收益率序列

# 年化收益率
annual_return = daily_returns.mean() * trading_days

# 年化波动率
annual_vol = daily_returns.std() * np.sqrt(trading_days)

# 累计收益率
cumulative_return = np.prod(1 + daily_returns) - 1

print(f'Mean daily return:   {daily_returns.mean():.6f}')
print(f'Annualized return:   {annual_return:.4f} ({annual_return*100:.2f}%)')
print(f'Annualized volatility:{annual_vol:.4f} ({annual_vol*100:.2f}%)')
print(f'Cumulative return:   {cumulative_return:.4f} ({cumulative_return*100:.2f}%)')
"
```

年化公式总结：

| 指标 | 日频 → 年化 |
|------|-----------|
| 收益率 | `μ_daily * 252` |
| 波动率 | `σ_daily * sqrt(252)` |
| 夏普比率 | `√252 * μ_daily / σ_daily` |

---

### 📚 第二节：核心绩效指标

#### 2.1 夏普比率 (Sharpe Ratio)

$$\text{Sharpe} = \frac{R_p - R_f}{\sigma_p}$$

衡量每单位风险（波动率）的超额收益。年化夏普比率 > 1 通常被认为不错。

```bash
python -c "
import numpy as np

np.random.seed(0)
returns = np.random.randn(1000) * 0.015 + 0.0005  # 模拟策略日收益
risk_free_rate = 0.03 / 252  # 日无风险利率（年化 3%）

# 超额收益
excess_returns = returns - risk_free_rate

# 夏普比率（年化）
sharpe_daily = excess_returns.mean() / excess_returns.std()
sharpe_annual = sharpe_daily * np.sqrt(252)

print(f'Daily Sharpe:   {sharpe_daily:.4f}')
print(f'Annual Sharpe:  {sharpe_annual:.4f}')
print()
print('Interpretation:')
print('  > 2.0 : Excellent')
print('  1.0 - 2.0 : Good')
print('  0.5 - 1.0 : Acceptable')
print('  < 0.5 : Poor')
"
```

#### 2.2 Sortino 比率

夏普比率惩罚上行波动（正收益的波动），而投资者只关心下行风险。Sortino 比率用**下行标准差**替代总标准差：

$$\text{Sortino} = \frac{R_p - R_f}{\sigma_{\text{down}}}$$

其中 $\sigma_{\text{down}} = \sqrt{\frac{1}{n} \sum \min(R_i, 0)^2}$

```bash
python -c "
import numpy as np

np.random.seed(1)
returns = np.random.randn(1000) * 0.015 + 0.0003

risk_free_daily = 0.03 / 252
excess = returns - risk_free_daily

# 下行标准差
downside_returns = np.minimum(returns, 0)
downside_std = np.sqrt(np.mean(downside_returns ** 2))

sortino_daily = excess.mean() / downside_std
sortino_annual = sortino_daily * np.sqrt(252)

print(f'Downside std:    {downside_std:.6f}')
print(f'Total std:       {returns.std():.6f}')
print(f'Sortino (annual):{sortino_annual:.4f}')
print(f'Sharpe (annual): {excess.mean() / returns.std() * np.sqrt(252):.4f}')
print()
print('Sortino > Sharpe → most volatility is upside')
"
```

#### 2.3 最大回撤 (Max Drawdown)

回撤是从历史最高点到当前点的跌幅。最大回撤是期间最严重的损失。

```bash
python -c "
import numpy as np

np.random.seed(0)
returns = np.random.randn(500) * 0.015 + 0.0003
equity_curve = 100 * np.cumprod(1 + returns)  # 净值曲线

# 计算最大回撤
rolling_max = np.maximum.accumulate(equity_curve)
drawdown = (equity_curve - rolling_max) / rolling_max
max_drawdown = np.min(drawdown)
max_dd_idx = np.argmin(drawdown)

print(f'Max drawdown: {max_drawdown:.2%}')
print(f'Occurred at index: {max_dd_idx}')
print(f'Drawdown end: {drawdown[-1]:.4%}')

# 回撤持续期（drawdown duration）
is_in_drawdown = drawdown < 0
# 寻找最长回撤持续期
def max_drawdown_duration(drawdown):
    """计算最长水下持续期（bar 数）"""
    dd_series = drawdown < 0
    duration = 0
    current = 0
    for d in dd_series:
        if d:
            current += 1
            duration = max(duration, current)
        else:
            current = 0
    return duration

print(f'Max drawdown duration: {max_drawdown_duration(drawdown)} days')
"
```

图解回撤：

```
净值曲线
    /\
   /  \    /\
  /    \  /  \    最高点（watermark）
 /      \/    \/\
/                 ← 当前点
|-------|        回撤（跌幅）
```

#### 2.4 Calmar 比率

$$\text{Calmar} = \frac{\text{年化收益率}}{|\text{最大回撤}|}$$

```bash
python -c "
import numpy as np

np.random.seed(0)
returns = np.random.randn(500) * 0.015 + 0.0005
equity = 100 * np.cumprod(1 + returns)

# 年化收益
annual_ret = returns.mean() * 252

# 最大回撤
roll_max = np.maximum.accumulate(equity)
max_dd = np.min((equity - roll_max) / roll_max)

calmar = annual_ret / abs(max_dd)

print(f'Annual return: {annual_ret:.2%}')
print(f'Max drawdown:  {max_dd:.2%}')
print(f'Calmar ratio:  {calmar:.4f}')
print('Interpretation: Calmar > 1 means annual return exceeds max drawdown')
"
```

---

### 📚 第三节：Alpha 与 Beta —— CAPM 模型

#### 3.1 CAPM 基础

资本资产定价模型 (CAPM) 将股票/策略的收益分解为：

$$R_{\text{strategy}} = \alpha + \beta \cdot R_{\text{market}} + \epsilon$$

- **Alpha ($\alpha$)**：独立于市场波动的超额收益（策略真正的"能力"）
- **Beta ($\beta$)**：策略收益对市场收益的敏感度
- **$\epsilon$**：随机误差

#### 3.2 NumPy 实现

```bash
python -c "
import numpy as np

np.random.seed(42)
n = 500

# 模拟市场收益和策略收益
market_returns = np.random.randn(n) * 0.01 + 0.0003
# 策略收益 = alpha + beta * 市场收益 + 噪声
true_alpha = 0.0002
true_beta = 1.2
noise = np.random.randn(n) * 0.008
strategy_returns = true_alpha + true_beta * market_returns + noise

# 用线性回归估计 alpha 和 beta
# y = alpha + beta * x → 用 np.polyfit 或公式
beta = np.cov(strategy_returns, market_returns)[0, 1] / np.var(market_returns)
alpha = strategy_returns.mean() - beta * market_returns.mean()

print(f'True Alpha: {true_alpha * 252:.4f}  Estimated: {alpha * 252:.4f}')
print(f'True Beta:  {true_beta:.4f}       Estimated: {beta:.4f}')
print(f'Correlation: {np.corrcoef(strategy_returns, market_returns)[0, 1]:.4f}')

# 年化 Alpha（Jensen Alpha）
annual_alpha = alpha * 252
print(f'Annual Alpha: {annual_alpha:.2%}')
"
```

#### 3.3 信息比率 (Information Ratio)

Information Ratio 衡量每单位跟踪误差（相对于基准的偏离）的超额收益：

$$\text{IR} = \frac{E[R_{\text{strategy}} - R_{\text{benchmark}}]}{\sigma(R_{\text{strategy}} - R_{\text{benchmark}})}$$

```bash
python -c "
import numpy as np

np.random.seed(1)
n = 500
benchmark = np.random.randn(n) * 0.01 + 0.0003
tracking_error = np.random.randn(n) * 0.008
strategy = benchmark + tracking_error + 0.0002

excess = strategy - benchmark
ir = excess.mean() / excess.std() * np.sqrt(252)

print(f'Tracking error (annual): {excess.std() * np.sqrt(252):.4f}')
print(f'Information Ratio:       {ir:.4f}')
print('IR > 0.5 : Good active manager')
print('IR > 1.0 : Excellent')
"
```

---

### 📚 第四节：Value at Risk (VaR) 与 Conditional VaR

#### 4.1 VaR 的定义

VaR（在险价值）：在给定置信水平下，一定时期内的最大预期损失。

- **VaR 95%**：有 95% 的概率损失不超过 VaR（即 5% 概率损失超过 VaR）

```bash
python -c "
import numpy as np

np.random.seed(0)
returns = np.random.randn(1000) * 0.015 + 0.0003

# 历史 VaR（非参数法）
confidence = 0.95
var_95 = -np.percentile(returns, 100 * (1 - confidence))
var_99 = -np.percentile(returns, 100 * (1 - 0.99))

print(f'VaR 95% (daily): {var_95:.4f} ({var_95*100:.2f}%)')
print(f'VaR 99% (daily): {var_99:.4f} ({var_99*100:.2f}%)')
print(f'Meaning: 5% chance of losing > {var_95*100:.2f}% in a single day')
print()

# 参数法（假设正态分布）
from scipy.stats import norm
mu, sigma = returns.mean(), returns.std()
var_95_param = -(mu + sigma * norm.ppf(1 - 0.95))
print(f'VaR 95% (parametric): {var_95_param:.4f} ({var_95_param*100:.2f}%)')
"
```

#### 4.2 Conditional VaR (CVaR / Expected Shortfall)

VaR 只告诉你"阈值"，不告诉你阈值之外的损失有多大。CVaR 是超过 VaR 的所有损失的均值。

```bash
python -c "
import numpy as np

np.random.seed(1)
returns = np.random.randn(500) * 0.015 + 0.0002

# 历史 CVaR
confidence = 0.95
var_threshold = np.percentile(returns, 100 * (1 - confidence))
cvar = -np.mean(returns[returns <= var_threshold])

print(f'VaR 95% threshold: {var_threshold:.4f}')
print(f'CVaR 95% (expected shortfall): {cvar:.4f} ({cvar*100:.2f}%)')
print(f'Meaning: When things go wrong (worst 5%), avg loss is {cvar*100:.2f}%')
"
```

所有指标汇总：

| 指标 | 公式 | 用途 |
|------|------|------|
| Sharpe | $R/\sigma$ | 风险调整收益（对称风险） |
| Sortino | $R/\sigma_{down}$ | 风险调整收益（仅下行风险） |
| Max DD | $\min(1 - E_t / \max_{s \le t} E_s)$ | 最差情况损失 |
| Calmar | $R_{\text{annual}} / |MDD|$ | 收益/最大回撤比 |
| Alpha | 回归截距 | 独立于市场的收益 |
| Beta | 回归斜率 | 市场敏感度 |
| VaR | 分位数 | 阈值损失 |
| CVaR | 阈值外均值 | 尾部期望损失 |

---

### 📝 小节练习

> [!question] 选择题 1
> 夏普比率和 Sortino 比率的核心区别是什么？
> - [ ] A. 夏普比率考虑上行风险，Sortino 不考虑
> - [ ] B. Sortino 比率只考虑下行波动，夏普考虑全部波动
> - [ ] C. 两者完全一样
> - [ ] D. 夏普比率只能年化使用
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: 夏普比率使用总标准差（上行 + 下行波动），Sortino 使用下行标准差。对于收益不对称分布的策略（如期权策略），Sortino 更好地反映了投资者真正关心的风险。

> [!question] 判断题 1
> 最大回撤可以大于 100%。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > **解析**: 理论上，如果使用了杠杆，最大回撤可以超过 100%（净值降到负数）。对于不借钱的纯多头策略，最大回撤理论上是 100%（归零），实际在 0-100% 之间。

---

## 📋 章节测试

### 一、判断题

> [!question] 判断题 1
> 年化夏普比率 = 日夏普比率 × 252。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: 年化夏普比率 = 日夏普比率 × √252（注意是根号）。因为收益率随时期线性缩放（×252），但标准差随 √t 缩放（×√252）。

> [!question] 判断题 2
> Beta = 1.5 表示策略收益对市场收益的反应比市场本身大 50%。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > **解析**: Beta > 1 表示高贝塔（进攻型），市场涨 1% 时策略平均涨 1.5%，市场跌 1% 时策略平均跌 1.5%。Beta < 1 是防御型，Beta < 0 是逆周期。

> [!question] 判断题 3
> VaR 值的计算依赖于收益率分布的假设。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > **解析**: 参数法 VaR 假设正态分布，历史法不假设分布但假设历史重演，Monte Carlo 法需要指定分布模型。不同假设得出的 VaR 可能差异很大。

> [!question] 判断题 4
> 策略的 Alpha 为正表示该策略的收益完全来自于市场风险暴露。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: Alpha 为正表示市场无法解释的那部分超额收益——这才是策略"真正的能力"。正的 Alpha 说明即使在扣除了市场风险补偿（Beta × R_market）后，策略仍有利可图。

> [!question] 判断题 5
> 使用 `np.maximum.accumulate()` 可以高效计算回撤。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > **解析**: `np.maximum.accumulate(equity)` 给出每个时间点的历史最高净值（watermark），然后 `(equity - watermark) / watermark` 就是回撤。这是 O(n) 的向量化方法，比 Python for 循环快百倍。

---

### 二、选择题

> [!question] 选择题 1
> 下列哪个指标同时考虑了收益和回撤的大小？
> - [ ] A. Sharpe Ratio
> - [ ] B. Sortino Ratio
> - [ ] C. Calmar Ratio
> - [ ] D. Information Ratio
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > **解析**: Calmar Ratio = 年化收益 / |最大回撤|，直接衡量"赚多少 vs 可能亏多少"。Sharpe/Sortino 用标准差衡量风险，不是直接的回撤。

> [!question] 选择题 2
> CAPM 模型中，Alpha 的经济学含义是？
> - [ ] A. 市场整体收益
> - [ ] B. 策略对市场的敏感度
> - [ ] C. 策略独立于市场的超额收益能力
> - [ ] D. 无风险收益率
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > **解析**: Alpha 是回归截距，代表收益中不能被市场（Beta）解释的部分。正的 Alpha = "跑赢大盘的能力"，负的 Alpha = "跑输大盘"。

> [!question] 选择题 3
> 在 "风险平价" 策略中，各资产的权重如何确定？
> - [ ] A. 按市值加权
> - [ ] B. 等权重
> - [ ] C. 按风险贡献等分配（每类资产贡献相等的风险）
> - [ ] D. 按历史收益排序
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > **解析**: 风险平价（Risk Parity）的核心思想是让组合中每类资产贡献相等额的总风险（波动率）。这通常意味着低波动资产（如债券）获得更高杠杆，高波动资产（如股票）获得更低权重。

> [!question] 选择题 4
> 以下哪个能最好地描述信息比率？
> - [ ] A. 策略收益除以无风险利率
> - [ ] B. 相对于基准的超额收益除以跟踪误差
> - [ ] C. 策略收率及除以策略波动率
> - [ ] D. 策略阿尔法除以贝塔
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: Information Ratio = (R_strategy - R_benchmark) / tracking_error。衡量主动管理的"性价比"——每承担一单位偏离基准的风险，能获得多少超额收益。

> [!question] 选择题 5
> `np.percentile(returns, 5)` 返回什么？
> - [ ] A. 前 5% 的收益
> - [ ] B. 只有 5% 的收益率低于这个值
> - [ ] C. 只有 5% 的收益率高于这个值
> - [ ] D. 收益率的中位数
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: `np.percentile(returns, 5)` 返回第 5 百分位数——恰好有 5% 的数据小于等于这个值。这是 95% 置信水平 VaR 计算的基础。

> [!question] 选择题 6
> 年化收益率使用 252 来缩放日收益率，这个数字代表什么？
> - [ ] A. 一年有 252 个日历日
> - [ ] B. 一年大约有 252 个交易日
> - [ ] C. 金融行业约定俗成的任意数
> - [ ] D. 市场数据的统计特征
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: 美国和中国股市每年大约有 250-252 个交易日（排除周末和节假日）。对于加密货币（365 天交易），使用 365；对于月度数据，使用 12。

---

### 🛠️ 动手练习题

> [!example] 练习题 1：完整绩效报告
> **难度**: ⭐⭐
>
> 编写一个函数 `performance_report(returns, benchmark=None)`，输入策略日收益率序列和可选的基准收益率，输出包含以下内容的报告：
> - 年化收益率、年化波动率、夏普比率、Sortino 比率
> - 最大回撤、Calmar 比率
> - 胜率（正收益天数/总天数）、盈亏比（平均盈利/平均亏损）
> - 如果提供了基准：Alpha、Beta、信息比率、相关系数

> [!example] 练习题 2：回撤分析可视化
> **难度**: ⭐⭐
>
> 生成一个模拟策略的净值曲线，计算最大回撤及其持续期。用 Matplotlib 绘制净值曲线并在图上标注回撤区间：净值曲线画线、回撤区域用红色阴影标注、最大回撤位置用箭头标注。（提示：参考 [[../8数据可视化/|数据可视化]]）

> [!example] 练习题 3：滚动绩效指标
> **难度**: ⭐⭐⭐
>
> 用滚动窗口（252 个交易日）分别计算策略的：
> 1. 滚动年化收益率
> 2. 滚动年化波动率
> 3. 滚动夏普比率
> 4. 滚动最大回撤
>
> 绘制这些滚动指标的时序图，观察它们如何随时间变化。讨论：如果只看全期指标（一个数字），你会错过什么？
