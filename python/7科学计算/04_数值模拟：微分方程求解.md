# 数值模拟：微分方程求解 (Numerical Simulation)
---

## 📖 章节概述

科学计算的核心任务之一是求解微分方程——从牛顿力学到量子力学，从电路分析到流行病模型，微分方程无处不在。本章从常微分方程（ODE）的 scipy.integrate.solve_ivp 入手，覆盖 Runge-Kutta 方法家族；随后使用 NumPy 有限差分法求解一维热方程（PDE）实例；最后介绍蒙特卡洛模拟（π 估计、期权定价）作为随机模拟的入门。我们将对比 C 语言中手写 RK4 的代码量与 SciPy 一行调用的差距，并简要提及可视化输出的去向。

> **核心理念**：数值模拟是"理论"与"实验"之外的第三条科研道路。Python 科学栈让你在几分钟内搭建起在 C 中需要几天才能写好的仿真原型——先理解你的模型行为，再决定是否需要 C 级别的性能优化。

---

### 📚 第一节：常微分方程（ODE）数值解

#### 1.1 solve_ivp 基础

SciPy 的 `solve_ivp` 求解初值问题 `dy/dt = f(t, y)`，提供多种 Runge-Kutta 方法。

```python
import numpy as np
from scipy.integrate import solve_ivp
import matplotlib.pyplot as plt

# 经典 ODE：指数衰减  dy/dt = -k*y, y(0) = y0
def decay(t, y, k):
    return -k * y

k, y0 = 0.5, 10.0
sol = solve_ivp(decay, [0, 10], [y0], args=(k,),
                method='RK45', dense_output=True)

print(f"求解状态: {sol.message}")
print(f"时间点数: {len(sol.t)}")
print(f"y(5) ≈ {sol.sol(5)[0]:.4f}")   # 解析解: 10*exp(-0.5*5) ≈ 0.8208

# 设置固定的评估点
t_eval = np.linspace(0, 10, 100)
sol = solve_ivp(decay, [0, 10], [y0], args=(k,),
                t_eval=t_eval, method='RK45')
```

#### 1.2 Runge-Kutta 方法家族

| 方法 | 类型 | 精度 | 适用场景 |
|------|------|------|---------|
| `RK45`（默认） | 显式，自适应步长 | 5(4) 阶 | 非刚性 ODE |
| `RK23` | 显式，自适应步长 | 3(2) 阶 | 低精度需求 |
| `DOP853` | 显式，8 阶 | 高精度 | 高精度需求 |
| `Radau` | 隐式 | 5 阶 | 刚性 ODE |
| `BDF` | 隐式，多步 | 可变阶 | 严重刚性 ODE |
| `LSODA` | 自动切换 | 可变 | 未知刚性时 |

> ⚠️ **刚性（stiffness）** 是数值 ODE 求解的核心挑战：当系统中存在差异巨大的时间尺度时，显式方法必须使用极小的步长才能稳定。隐式方法（Radau, BDF）通过求解非线性方程组来保持稳定性，虽然每步更贵但总体更高效。

#### 1.3 刚性 ODE 示例

```python
import numpy as np
from scipy.integrate import solve_ivp
import time

# Van der Pol 振荡器（在 μ 很大时刚性）
def vanderpol(t, y, mu):
    return [y[1], mu * (1 - y[0]**2) * y[1] - y[0]]

mu = 1000  # 大 μ → 刚性
y0 = [2, 0]

# 显式 RK45 — 极慢（步长被限制）
t0 = time.perf_counter()
sol_rk45 = solve_ivp(vanderpol, [0, 3000], y0, args=(mu,),
                     method='RK45')
t1 = time.perf_counter()
print(f"RK45: {t1-t0:.2f}s, 步数: {len(sol_rk45.t)}")

# 隐式 Radau — 快很多
t0 = time.perf_counter()
sol_radau = solve_ivp(vanderpol, [0, 3000], y0, args=(mu,),
                      method='Radau')
t1 = time.perf_counter()
print(f"Radau: {t1-t0:.2f}s, 步数: {len(sol_radau.t)}")
# Radau 步数远少于 RK45，整体时间大幅缩短
```

#### 1.4 事件（Events）——提前终止

```python
import numpy as np
from scipy.integrate import solve_ivp

# 抛体运动：y'' = -g，求解何时落地
def projectile(t, state):
    x, y, vx, vy = state
    return [vx, vy, 0, -9.81]

# 定义事件：y = 0（触地），检测下降过程中穿过零点
def hit_ground(t, state):
    return state[1]  # y 坐标

hit_ground.terminal = True   # 触地时停止积分
hit_ground.direction = -1    # 仅在下降时检测

sol = solve_ivp(projectile, [0, 10], [0, 0, 10, 20],
                events=hit_ground, max_step=0.01)

print(f"飞行时间: {sol.t[-1]:.3f} s")
print(f"落地点 x: {sol.y[0, -1]:.3f} m")
```

### 📝 小节练习

> [!question] 选择题 1
> solve_ivp 中 `t_eval` 参数的作用是？
> - [ ] A. 指定积分的时间单位
> - [ ] B. 定频率输出值（在这些固定时间点评估解）
> - [ ] C. 评估解的质量
> - [ ] D. 指定初始条件
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: `t_eval` 指定输出时间点的数组，求解器在这些时间点评估解（通过插值），返回值在这些点上而非自适应步长得到的点上。

> [!question] 判断题 1
> `RK45` 方法适用于任何类型的 ODE 问题。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: RK45 是显式方法，对于**刚性系统**（如 Van der Pol μ=1000）效率极低甚至失败。刚性 ODE 应使用隐式方法如 Radau 或 BDF。

---

### 📚 第二节：偏微分方程（PDE）— 热方程

#### 2.1 一维热方程的有限差分解

热方程 `∂u/∂t = α ∂²u/∂x²` 使用显式 Euler 时间推进 + 中心差分空间离散：

```python
import numpy as np

# 参数
alpha = 0.01            # 热扩散系数
L = 1.0                 # 杆的长度
nx = 50                 # 空间网格点
dx = L / (nx - 1)
dt = 0.001              # 时间步长
nt = 5000               # 时间步数

# 稳定性条件（显式格式的 CFO 条件）
cfl = alpha * dt / dx**2
print(f"CFL = {cfl:.4f} （必须 < 0.5 以保证稳定）")

# 初始条件：高斯热脉冲
x = np.linspace(0, L, nx)
u = np.exp(-((x - 0.5)**2) / 0.01)

# 时间推进 — 向量化（无 Python 循环）
for n in range(nt):
    # 二阶中心差分：∂²u/∂x² ≈ (u[i+1] - 2*u[i] + u[i-1]) / dx²
    u[1:-1] += alpha * dt / dx**2 * (u[2:] - 2*u[1:-1] + u[:-2])

    # Dirichlet 边界条件：两端固定为 0
    u[0] = u[-1] = 0.0

    if n % 1000 == 0:
        print(f"步骤 {n:5d}, 总热量: {u.sum() * dx:.6f}")

print(f"最终总热量: {u.sum() * dx:.6f}")
# 热量应几乎完全耗散（因两端边界为 0）
```

> ⚠️ **CFL 稳定性条件**：显式格式要求 `α·dt/dx² < 0.5`，否则数值解振动发散。隐式格式（如 Crank-Nicolson）无条件稳定，但每步需解线性方程组。

#### 2.2 与 C 实现的对比

对比同样用 C 语言实现显式热方程求解器（伪代码）：

```c
// C 语言手写热方程（约 40 行，需管理内存、边界、CFL 检查）
double *u = malloc(nx * sizeof(double));
double *u_new = malloc(nx * sizeof(double));

// 初始化...
for (int n = 0; n < nt; n++) {
    for (int i = 1; i < nx-1; i++) {
        u_new[i] = u[i] + alpha * dt / (dx*dx)
                   * (u[i+1] - 2*u[i] + u[i-1]);
    }
    // 交换指针、处理边界...
    double *tmp = u; u = u_new; u_new = tmp;
}
// free / 错误检查 ...
```

NumPy 版本的代码行数约为 C 版本的 1/5，且无需管理内存、边界检查。但 C 版本在编译器 `-O3` 优化下可能比纯 NumPy 向量化还快（因为无需 Python 对象）。权衡：**开发速度 vs 运行时性能**。

> 🔗 关于性能对比的深入讨论，参见 [[05_性能对比：纯Python_vs_Cython_vs_C|性能对比章节]]。

### 📝 小节练习

> [!question] 选择题 1
> 热方程显式格式的 CFL 稳定性条件是？
> - [ ] A. 无条件稳定
> - [ ] B. `αΔt/Δx² < 0.5`
> - [ ] C. `Δt < Δx`
> - [ ] D. 取决于初始条件
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: 一维热方程显式 Euler 格式的稳定性条件为 `α·Δt/Δx² ≤ 0.5`。违反此条件将导致数值振荡快速增长。

> [!question] 判断题 1
> NumPy 的向量化热方程求解不需要 CFL 条件检查。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: CFL 条件源自**离散格式的数学稳定性**，与实现语言无关。NumPy 向量化只是用 C 层面的循环替代 Python 循环，数学稳定性要求不变。

---

### 📚 第三节：蒙特卡洛模拟

#### 3.1 π 估计

蒙特卡洛方法的核心思想是用随机采样逼近确定性量：

```python
import numpy as np

N = 1_000_000

# 在 [-1,1] 正方形中均匀采样
x = np.random.uniform(-1, 1, N)
y = np.random.uniform(-1, 1, N)

# 点在单位圆内的比例 → π/4
inside = (x**2 + y**2) <= 1
pi_estimate = 4 * inside.sum() / N

print(f"π 估计值: {pi_estimate:.6f}")
print(f"真实值:   {np.pi:.6f}")
print(f"误差:     {abs(pi_estimate - np.pi):.6f}")
# 蒙特卡洛误差 ∝ 1/√N，约 0.001 量级
```

#### 3.2 欧式期权定价

Black-Scholes 模型中，股票价格遵循几何布朗运动：

```
ST = S0 * exp((r - σ²/2)*T + σ*√T*ε)    其中 ε ~ N(0,1)
```

```python
import numpy as np

S0, K, r, sigma, T = 100, 105, 0.05, 0.2, 1.0
N = 500_000

np.random.seed(42)
Z = np.random.randn(N)
ST = S0 * np.exp((r - 0.5 * sigma**2) * T + sigma * np.sqrt(T) * Z)

# 看涨期权 payoff = max(ST - K, 0)
payoff = np.maximum(ST - K, 0)

# 现值贴现
option_price = np.exp(-r * T) * payoff.mean()
std_error = np.exp(-r * T) * payoff.std() / np.sqrt(N)

print(f"期权价格: {option_price:.4f}")
print(f"95% 置信区间: [{option_price - 1.96*std_error:.4f}, {option_price + 1.96*std_error:.4f}]")

# Black-Scholes 解析解（对比用）
from scipy.stats import norm
d1 = (np.log(S0/K) + (r + 0.5*sigma**2)*T) / (sigma*np.sqrt(T))
d2 = d1 - sigma*np.sqrt(T)
bs_price = S0*norm.cdf(d1) - K*np.exp(-r*T)*norm.cdf(d2)
print(f"BS 解析价格: {bs_price:.4f}")
```

> 💡 蒙特卡洛的收敛速度是 O(1/√N)——要想提高一位小数精度，需要 100 倍的计算量。对于低维问题，确定性积分方法（如 Simpson 规则）更高效；蒙卡的优势在于**高维积分**（> 4 维），其中确定性方法遭遇"维数诅咒"。

#### 3.3 方差缩减技巧

```python
import numpy as np

# 对偶变量法：用一对相反的随机数，降低方差
N_half = N // 2
Z = np.random.randn(N_half)
ST1 = S0 * np.exp((r - 0.5*sigma**2)*T + sigma*np.sqrt(T)*Z)
ST2 = S0 * np.exp((r - 0.5*sigma**2)*T + sigma*np.sqrt(T)*(-Z))

payoff_avg = 0.5 * (np.maximum(ST1 - K, 0) + np.maximum(ST2 - K, 0))
price_antithetic = np.exp(-r*T) * payoff_avg.mean()
print(f"对偶变量法价格: {price_antithetic:.4f}")
```

### 📝 小节练习

> [!question] 选择题 1
> 蒙特卡洛估计的收敛速度是？
> - [ ] A. O(1/N)
> - [ ] B. O(1/√N)
> - [ ] C. O(1/N²)
> - [ ] D. O(log N / N)
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: 蒙特卡洛估计的标准误差与 1/√N 成正比。这是中心极限定理的直接结果。

> [!question] 判断题 1
> 对于 2 维积分的数值计算，蒙特卡洛方法比 Simpson 规则更高效。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: 低维（≤4）积分中，Simpson 或 Gauss 积分等确定性方法的收敛速度远优于蒙特卡洛。蒙卡的优势在 5+ 维高维积分中显现，因为确定性方法遭遇"维数诅咒"——网格点数随维数指数增长。

---

### 📚 第四节：C 实现对比

#### 4.1 手写 RK4 对比

以下是在 C 中手写一个单步的经典四阶 Runge-Kutta（RK4）所需的核心函数：

```c
// C: 手写 RK4 的 dydt（导数函数）
void dydt(double t, double *y, double *dydt_out, void *params) {
    double k = *(double*)params;
    dydt_out[0] = -k * y[0];   // dy/dt = -k*y
}

// C: 通用 RK4 一步
void rk4_step(void (*f)(double, double*, double*, void*),
              double t, double *y, double h, int n_eq,
              double *y_new, void *params) {
    double *k1 = malloc(n_eq * sizeof(double));
    double *k2 = malloc(n_eq * sizeof(double));
    double *k3 = malloc(n_eq * sizeof(double));
    double *k4 = malloc(n_eq * sizeof(double));
    double *ytmp = malloc(n_eq * sizeof(double));

    f(t, y, k1, params);

    for (int i = 0; i < n_eq; i++)
        ytmp[i] = y[i] + 0.5 * h * k1[i];
    f(t + 0.5*h, ytmp, k2, params);

    for (int i = 0; i < n_eq; i++)
        ytmp[i] = y[i] + 0.5 * h * k2[i];
    f(t + 0.5*h, ytmp, k3, params);

    for (int i = 0; i < n_eq; i++)
        ytmp[i] = y[i] + h * k3[i];
    f(t + h, ytmp, k4, params);

    for (int i = 0; i < n_eq; i++)
        y_new[i] = y[i] + (h/6.0) * (k1[i] + 2*k2[i] + 2*k3[i] + k4[i]);

    free(k1); free(k2); free(k3); free(k4); free(ytmp);
}
```

对比 Python 等价代码：

```python
from scipy.integrate import solve_ivp

def dydt(t, y, k):
    return -k * y

sol = solve_ivp(dydt, [0, 10], [10.0], args=(0.5,), method='RK45')
```

Python 版本不仅更短，而且具有：
- 自适应步长（C 版本需要额外数百行代码实现）
- 事件检测
- 刚性自动检测（LSODA）
- 稠密插值输出

#### 4.2 何时该用 C

| 场景 | 推荐 |
|------|------|
| 原型开发、实验 | Python（SciPy） |
| 单次运行、低维 | Python |
| 大规模 Monte Carlo | Python + Numba 或 C |
| 实时嵌入式系统 | C（无 Python 解释器可用） |
| 大规模 PDE（如 CFD） | C/Fortran/C++（或 Python 调用 C 后端） |
| 参数扫描（同一模型多次运行） | C（或 Python + 多进程/MPI） |

> 🔗 关于 Python → C 性能桥接的完整讨论，见 [[05_性能对比：纯Python_vs_Cython_vs_C|性能对比]]和 [[../2精通/07_pybind11与Cython：给C_C++库披上Python外衣|pybind11 与 Cython]]。

### 📝 小节练习

> [!question] 选择题 1
> C 中手写 RK4 相比 Python 中调用 `solve_ivp`，以下哪项是 C 的优势？
> - [ ] A. 代码更短
> - [ ] B. 无需编译即可运行
> - [ ] C. 在一些场景下运行时速度更快（尤其是紧凑循环）
> - [ ] D. 自适应步长
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > **解析**: 纯 C 实现在编译优化的帮助下可以提供更快的裸计算速度（无 Python 解释器开销），特别适合需要极致性能的循环。但 SciPy 封装了 LAPACK/BLAS 级别的优化，在很多场景下性能差距不大。

> [!question] 判断题 1
> 蒙特卡洛期权定价在 C 中实现一定比 Python+NumPy 更快。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: 如果 Python 版本使用 NumPy 向量化（所有随机数生成和 payoff 计算都是 C 代码），性能与纯 C 非常接近。差异通常来自于内存分配模式而非语言本身。

---

## 📋 章节测试

### 一、判断题（正确选✅，错误选❌）

> [!question] 判断题 1
> `solve_ivp` 默认使用自适应步长，时间步长在求解过程中可以变化。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > **解析**: 默认方法 `RK45` 使用嵌入式误差估计自动调整步长——在解变化剧烈的区域使用小步长，平坦区域使用大步长。

> [!question] 判断题 2
> 显式热方程格式的 CFL 条件仅适用于 Python，在 C 语言中不需要遵守。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: CFL 条件源自差分格式的数学稳定性分析，与实现语言无关。C 语言也不会"豁免"于数值不稳定性。

> [!question] 判断题 3
> 蒙特卡洛方法在高维积分中比 Simpson 规则更有优势。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > **解析**: 蒙卡的收敛速度 O(1/√N) 与维度无关，而确定性方法的网格点数随维度指数增长。5 维以上蒙卡通常更优。

> [!question] 判断题 4
> `solve_ivp` 的 `args` 参数用于传递求解器的配置选项。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: `args` 用于向导数函数 `f(t, y)` 传递额外参数（如物理常数）。求解器配置通过 `method` 和 `options`（如 `rtol`, `atol`）控制。

> [!question] 判断题 5
> 使用 NumPy 向量化编写的热方程程序，内部的差分循环在 C 层面执行，但每次迭代仍需返回 Python 层面。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > **解析**: 数组运算 `u[1:-1] + ...` 在 NumPy 内部 C 代码中完成，但外层的 `for n in range(nt)` 循环仍在 Python 层面。极端优化场景下可以考虑 Numba 或 Cython 消除这个外层循环。

> [!question] 判断题 6
> `hit_ground.terminal = True` 使得求解器在事件触发后立即停止。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > **解析**: `terminal = True` 标记事件为终止事件，求解器在事件触发后停止积分。

---

### 二、选择题（单项选择题）

> [!question] 选择题 1
> 以下哪种方法最适合求解刚性 ODE？
> - [ ] A. RK45
> - [ ] B. RK23
> - [ ] C. Radau
> - [ ] D. DOP853
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > **解析**: Radau 是隐式 Runge-Kutta 方法，具有优秀的稳定性特性，适用于刚性方程。DOP853 和 RK45 都是显式方法，在刚性系统中步长严重受限。

> [!question] 选择题 2
> CFL = `α * dt / dx²` 对于显式热方程格式，稳定性要求是？
> - [ ] A. CFL < 1.0
> - [ ] B. CFL < 0.5
> - [ ] C. CFL 无限制
> - [ ] D. CFL > 0.5
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: 一维显式热方程的稳定性条件是 CFL ≤ 0.5。二维时变为 CFL ≤ 0.25。

> [!question] 选择题 3
> 蒙特卡洛估计 π 时，样本量增加到 100 倍，精度大约提高多少？
> - [ ] A. 100 倍
> - [ ] B. 10 倍
> - [ ] C. 约 3 倍
> - [ ] D. 不变
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: 蒙卡误差 ∝ 1/√N。增加 100 倍样本量，误差减小到原来的 1/√100 = 1/10。

> [!question] 选择题 4
> 对偶变量法（antithetic variates）的作用是？
> - [ ] A. 增加模拟的精度
> - [ ] B. 减少蒙特卡洛估计的方差
> - [ ] C. 改变随机数的种子
> - [ ] D. 加速随机数生成
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: 对偶变量法使用成对负相关的随机数（Z 和 -Z），通过引入负相关性减少 payoff 估计的方差，从而在同样本量下获得更精确的估计。

> [!question] 选择题 5
> 在 Python 中求解单个 ODE 的最常用 SciPy 函数是？
> - [ ] A. `scipy.ode`
> - [ ] B. `scipy.integrate.odeint`
> - [ ] C. `scipy.integrate.solve_ivp`
> - [ ] D. `scipy.integrate.quad`
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > **解析**: `solve_ivp` 是 SciPy 推荐的现代 ODE 求解接口（自 SciPy 1.0+），`odeint` 是旧版 API（基于 LSODA）。`quad` 用于定积分。

---

### 🛠️ 动手练习题

> [!example] 练习题 1：三体问题仿真
> **难度**: ⭐⭐⭐
>
> 使用 `solve_ivp` 模拟引力三体问题（3 个质量体之间的牛顿引力）。ODE 包含 18 个变量（3 体 × 3 个位置坐标 + 3 个速度分量）。初值使用"8 字形"轨道（figure-eight orbit）的已知初始条件。使用 `events` 检测碰撞（任意两体距离 < 阈值）。观察系统是否长时间保持稳定。

> [!example] 练习题 2：SIR 流行病模型
> **难度**: ⭐⭐
>
> 模拟 SIR 流行病模型：
> ```
> dS/dt = -β·S·I/N
> dI/dt = β·S·I/N - γ·I
> dR/dt = γ·I
> ```
> 使用 `solve_ivp` 求解，参数 β=0.3, γ=0.1, N=10000。绘制 S, I, R 随时间变化的曲线。找出感染峰值时间（使用 `events` 检测 dI/dt=0）。尝试不同的 β/γ 比值（基本传染数 R₀ = β/γ），观察疫情是爆发还是消退。

> [!example] 练习题 3：二维热方程
> **难度**: ⭐⭐⭐
>
> 使用 NumPy 实现二维热方程 `∂u/∂t = α(∂²u/∂x² + ∂²u/∂y²)` 的显式有限差分格式。初始条件为板块中心的 Hot Spot（高斯热源）。注意二维 CFL 条件 `αΔt(1/Δx² + 1/Δy²) ≤ 0.5`。每 100 步记录当前热量总和。观察热量的扩散和守恒（当所有边界绝热时）。

> [!example] 练习题 4：期权价格收敛性分析
> **难度**: ⭐⭐
>
> 对于欧式看涨期权（S0=100, K=100, r=0.02, σ=0.25, T=1.0），使用蒙特卡洛模拟计算期权价格。对不同的样本数 N ∈ {10², 10³, 10⁴, 10⁵, 10⁶}，每个 N 重复 50 次实验（使用不同随机种子），计算价格估计的均值和标准差。绘制标准差 vs N 的双对数图（log-log plot），验证标准差 ∝ 1/√N。

> [!example] 练习题 5：Runge-Kutta 方法数值精度对比
> **难度**: ⭐⭐⭐
>
> 使用不同求解器（RK45, RK23, DOP853, Radau）求解简单的谐振子 `y'' + y = 0, y(0)=1, y'(0)=0`。已知解析解为 `y(t) = cos(t)`。对比 t=[0, 100] 的长期行为：
> - 测量能量 `E = (y² + (y')²)/2` 的守恒程度（在 t=100 时的值 vs 初始值 0.5）
> - 测量相位误差（数值解与解析解 cos(t) 的相位差）
> - 统计每个求解器的步数和运行时间

> 🔗 完成数值模拟后，将结果可视化的技巧见 [[../8数据可视化/|数据可视化章节]]。
