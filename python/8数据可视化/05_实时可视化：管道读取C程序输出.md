# 实时可视化：管道读取 C 程序输出 (Real-time Visualization)
---

## 📖 章节概述

静态图表展示已经发生的——而实时可视化让你看到**正在发生**的。本章聚焦 C 程序员最核心的数据可视化场景：C 程序持续输出数据流，Python 脚本即时读取并更新图表。我们将从简单的 `subprocess.Popen` 管道模型开始，深入 `matplotlib.animation` 的实时更新机制，最终构建一个完整的"传感器数据仪表盘"——C 程序模拟传感器，Python 实时绑图。

> **核心理念**：实时可视化的本质是"生产者-消费者"模式——C 程序是生产者（写入 stdout），Python 是消费者（读取 stdin 并更新视图）。这与 Linux 管道哲学一脉相承 `./producer | ./consumer`，只不过 consumer 多了绑图能力。这个模式让你能用 C 的高性能采集数据，用 Python 的生态呈现数据，各司其职，是两种语言协同的最佳体现。

---

### 📚 第一节：subprocess.Popen —— 在 Python 中启动 C 程序
---

1.1 Popen 管道通信模型
---------------------

Python 通过 `subprocess.Popen` 启动 C 程序，捕获其 stdout 输出：

```python
import subprocess
import sys

proc = subprocess.Popen(
    ['./my_c_program'],
    stdout=subprocess.PIPE,     # 捕获标准输出
    stderr=subprocess.STDOUT,   # 将 stderr 合并到 stdout
    text=True,                  # 以文本模式读取（而非 bytes）
    bufsize=1,                  # 行缓冲：每写入一行就刷新
)

# 逐行读取 C 程序的输出
for line in proc.stdout:
    line = line.strip()
    if line:
        print(f"Received: {line}")
```

> `subprocess.Popen` 等价于 C 中的 `fork() + exec()` 组合。`stdout=subprocess.PIPE` 创建一个内核级管道（pipe），C 程序的 `printf` 写入管道，Python 的 `proc.stdout` 从管道读取——这一切都是操作系统管理的缓冲区，Python 层面不需要显式调用 `pipe()` 或 `dup2()`。

1.2 C 程序的"持续输出"模式
------------------------

实时可视化要求 C 程序不断输出数据，而不是一次性计算完就退出：

```c
// sensor_sim.c  —  模拟温度传感器
#include <stdio.h>
#include <unistd.h>
#include <math.h>
#include <time.h>
#include <stdlib.h>

int main() {
    double t = 0.0;
    srand(time(NULL));

    while (1) {
        double temperature = 25.0 + 10.0 * sin(t / 10.0) +
                            (double)rand() / RAND_MAX * 2.0 - 1.0;
        double humidity = 60.0 + 15.0 * cos(t / 15.0) +
                         (double)rand() / RAND_MAX * 5.0 - 2.5;

        printf("%.2f %.3f %.3f\n", t, temperature, humidity);
        fflush(stdout);  // 关键！确保数据立即刷新

        usleep(100000);  // 100ms 采样间隔
        t += 0.1;
    }
    return 0;
}
```

> `fflush(stdout)` 是实时管道通信的**关键**。默认情况下，当 stdout 连接到管道（不是终端）时，glibc 使用全缓冲（block buffering）——数据在缓冲区积攒到 4KB 才发送。`fflush()` 强制立即发送，确保 Python 能及时收到每一行。

1.3 超时处理与进程管理
--------------------

C 程序可能挂死或运行时间过长，需要健壮的超时管理：

```python
import subprocess
import threading

def read_output(proc):
    for line in proc.stdout:
        print(f"Data: {line.strip()}")

proc = subprocess.Popen(
    ['./sensor_sim'],
    stdout=subprocess.PIPE,
    stderr=subprocess.STDOUT,
    text=True,
    bufsize=1,
)

reader = threading.Thread(target=read_output, args=(proc,), daemon=True)
reader.start()

try:
    reader.join(timeout=10)  # 最多运行 10 秒
except Exception:
    pass
finally:
    proc.terminate()
    proc.wait()
```

```bash
gcc -o sensor_sim sensor_sim.c -lm
```

### 📝 小节练习

> [!question] 选择题 1
> `subprocess.Popen` 的 `bufsize=1` 参数的作用是？
> - [ ] A. 设置管道缓冲区为 1 字节
> - [ ] B. 启用行缓冲（line buffering）
> - [ ] C. 每次只读取 1 行
> - [ ] D. 禁用所有缓冲
>
> > [!success]- 点击查看答案
> > > 正确答案: B
> > > **解析**: `bufsize=1` 在文本模式下表示**行缓冲**——每当 C 程序写入一个换行符 `\n` 并调用 `fflush()` 时，Python 就能立刻读取到这一行。`bufsize=0` 禁用缓冲（仅二进制模式），`bufsize=-1` 使用系统默认缓冲。

> [!question] 选择题 2
> 在 C 程序的管道输出中，`fflush(stdout)` 为什么至关重要？
> - [ ] A. 提高写入性能
> - [ ] B. 因为管道模式下 `stdout` 默认是全缓冲，需要手动刷新才能即时传递
> - [ ] C. 防止内存泄漏
> - [ ] D. 格式化输出
>
> > [!success]- 点击查看答案
> > > 正确答案: B
> > > **解析**: 当 `stdout` 连接到管道（而非终端）时，glibc 默认使用**全缓冲**（block buffering，缓冲区大小通常是 4096 或 8192 字节）。不调用 `fflush()`，数据会积攒在 C 进程的缓冲区中，直到缓冲区满了才发送——Python 端会出现明显的延迟。

---

### 📚 第二节：matplotlib.animation —— 实时更新图表
---

2.1 FuncAnimation 基础
----------------------

`matplotlib.animation.FuncAnimation` 是实现实时图表的核心工具：

```python
import matplotlib.pyplot as plt
import matplotlib.animation as animation
import numpy as np

fig, ax = plt.subplots(figsize=(8, 5))
line, = ax.plot([], [], 'b-', linewidth=2)
ax.set_xlim(0, 10)
ax.set_ylim(-2, 2)
ax.set_xlabel('Time (s)')
ax.set_ylabel('Value')
ax.grid(True, alpha=0.3)

x_data, y_data = [], []

def init():
    line.set_data([], [])
    return line,

def animate(frame):
    t = frame * 0.1
    x_data.append(t)
    y_data.append(np.sin(t))
    if len(x_data) > 200:
        x_data.pop(0)
        y_data.pop(0)
    line.set_data(x_data, y_data)
    ax.set_xlim(max(0, t - 5), max(5, t + 1))
    return line,

ani = animation.FuncAnimation(
    fig, animate,
    init_func=init,
    frames=500,
    interval=50,       # 每 50ms 更新一次
    blit=True,         # 只重绘变化的部分（性能优化）
)

# 保存为视频文件
ani.save('animation.mp4', writer='ffmpeg', fps=20)
# 或保存为 GIF
# ani.save('animation.gif', writer='pillow', fps=10)

plt.show()  # 交互式显示
```

> `FuncAnimation` 每隔 `interval` 毫秒调用一次 `animate(frame)` 函数，其中 `frame` 是递增的帧号。`animate` 函数更新图表数据并返回更新的 artist 对象（用于 `blit=True` 的重绘优化）。

2.2 从 C 程序管道读取并实时绑图
------------------------------

这是本节的核心模式——C 程序持续输出，Python 实时更新图表：

```python
import subprocess
import threading
import time
import matplotlib.pyplot as plt
import matplotlib.animation as animation
from collections import deque

# 启动 C 程序
proc = subprocess.Popen(
    ['./sensor_sim'],
    stdout=subprocess.PIPE,
    stderr=subprocess.STDOUT,
    text=True,
    bufsize=1,
)

# 数据缓冲区（线程安全结构）
MAX_POINTS = 300
t_data = deque(maxlen=MAX_POINTS)
temp_data = deque(maxlen=MAX_POINTS)
hum_data = deque(maxlen=MAX_POINTS)
new_data_lock = threading.Lock()

def reader_thread():
    """后台线程：持续读取 C 程序输出"""
    for line in proc.stdout:
        line = line.strip()
        if not line:
            continue
        try:
            parts = line.split()
            t_val = float(parts[0])
            temp_val = float(parts[1])
            hum_val = float(parts[2])
            with new_data_lock:
                t_data.append(t_val)
                temp_data.append(temp_val)
                hum_data.append(hum_val)
        except (ValueError, IndexError):
            pass

# 启动后台读取线程
reader = threading.Thread(target=reader_thread, daemon=True)
reader.start()

# 创建图形
fig, (ax1, ax2) = plt.subplots(2, 1, figsize=(10, 8), sharex=True)

line_temp, = ax1.plot([], [], 'r-', linewidth=1.5, label='Temperature')
line_hum, = ax2.plot([], [], 'b-', linewidth=1.5, label='Humidity')

ax1.set_ylabel('Temperature (°C)', color='r')
ax1.set_ylim(10, 40)
ax1.legend(loc='upper right')
ax1.grid(True, alpha=0.3)

ax2.set_xlabel('Time (s)')
ax2.set_ylabel('Humidity (%)', color='b')
ax2.set_ylim(40, 80)
ax2.legend(loc='upper right')
ax2.grid(True, alpha=0.3)

fig.suptitle('Real-time Sensor Data from C Program', fontsize=14)

def init():
    line_temp.set_data([], [])
    line_hum.set_data([], [])
    return line_temp, line_hum

def animate(frame):
    with new_data_lock:
        if len(t_data) > 0:
            t_list = list(t_data)
            line_temp.set_data(t_list, list(temp_data))
            line_hum.set_data(t_list, list(hum_data))
        if len(t_data) > 1:
            window = max(5, t_data[-1] - t_data[0])
            ax1.set_xlim(t_data[-1] - window, t_data[-1] + 1)
    return line_temp, line_hum

ani = animation.FuncAnimation(
    fig, animate, init_func=init,
    interval=100, blit=True
)

plt.tight_layout()
plt.show()

proc.terminate()
```

> `deque(maxlen=MAX_POINTS)` 是一个固定长度的双端队列——当超过最大容量时，自动从左侧丢弃最旧的数据。这相当于 C 语言中的环形缓冲区（circular buffer），避免了无限增长的内存占用。

2.3 保存为视频文件
------------------

```python
# 使用 ffmpeg 保存动画
ani.save('sensor_live.mp4', writer='ffmpeg', fps=20, dpi=100)

# 先检查可用的 writer
print(animation.writers.list())
# ['ffmpeg', 'ffmpeg_file', 'pillow', 'html']
```

```bash
# 需要安装 ffmpeg
sudo apt install ffmpeg
```

### 📝 小节练习

> [!question] 选择题 1
> `matplotlib.animation.FuncAnimation` 的 `blit=True` 参数的作用是？
> - [ ] A. 使用 GPU 加速渲染
> - [ ] B. 只重绘变化的部分（性能优化）
> - [ ] C. 将图表导出为位图格式
> - [ ] D. 自动调整坐标轴范围
>
> > [!success]- 点击查看答案
> > > 正确答案: B
> > > **解析**: `blit=True` 启用"脏矩形"重绘——只更新图表中实际发生变化的部分，而不是每帧都重新渲染整个画布。对于实时图表，这可以显著提升性能并减少 CPU 占用。需要使用 `return` 语句明确返回被更新的 artist 对象。

> [!question] 判断题 1
> `deque(maxlen=300)` 在数据超过 300 个后会自动删除最新添加的元素。（ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > > 答案: 错误
> > > **解析**: `deque(maxlen=N)` 在数据超过 N 后，自动从**左侧**（最旧的一端）丢弃元素，保留最新的 N 个。这等价于先进先出的环形缓冲区。新的数据从右侧追加（`append()`）。

---

### 📚 第三节：实时图表的高级模式
---

3.1 滚动窗口 vs 累积视图
-----------------------

实时图表有两种常见模式：

```python
# 模式一：滚动窗口（适合长时间运行，只显示最近 N 秒）
from collections import deque

window_size = 200
x_win = deque(maxlen=window_size)
y_win = deque(maxlen=window_size)

def animate_scroll(frame):
    # ... 添加新数据 ...
    line.set_data(list(x_win), list(y_win))
    if len(x_win) > 0:
        ax.set_xlim(x_win[0], x_win[-1] + 1)
    return line,

# 模式二：累积视图（适合短期实验，显示全部数据）
x_all, y_all = [], []

def animate_accumulate(frame):
    # ... 添加新数据 ...
    line.set_data(x_all, y_all)
    ax.relim()           # 重新计算数据范围
    ax.autoscale_view()  # 自动缩放
    return line,
```

3.2 多个 C 程序并行读取
---------------------

当需要同时监控多个 C 程序时，每个程序一个读取线程：

```python
import subprocess
import threading
from collections import deque

def launch_monitor(command, data_buffer, label):
    proc = subprocess.Popen(
        command, stdout=subprocess.PIPE,
        text=True, bufsize=1
    )
    for line in proc.stdout:
        line = line.strip()
        if line:
            try:
                val = float(line)
                data_buffer.append(val)
            except ValueError:
                pass

# 三个 C 程序实例
buffer_a = deque(maxlen=300)
buffer_b = deque(maxlen=300)
buffer_c = deque(maxlen=300)

threads = [
    threading.Thread(target=launch_monitor,
                     args=('./sensor_A', buffer_a, 'A'), daemon=True),
    threading.Thread(target=launch_monitor,
                     args=('./sensor_B', buffer_b, 'B'), daemon=True),
    threading.Thread(target=launch_monitor,
                     args=('./sensor_C', buffer_c, 'C'), daemon=True),
]

for t in threads:
    t.start()

# animate 函数从三个缓冲区读取并更新三条线
```

> 这相当于 C 语言中用 `fork()` 创建多个子进程，每个写入不同的 pipe。Python 的 `threading` 替代了 `fork()`（因为 Python 的 GIL 让多线程在 I/O 密集型任务上表现良好），但底层管道通信原理完全相同。

3.3 带暂停/恢复控制的实时图表
---------------------------

```python
import matplotlib.pyplot as plt
import matplotlib.animation as animation

class RealtimePlot:
    def __init__(self):
        self.paused = False
        self.fig, self.ax = plt.subplots()
        self.fig.canvas.mpl_connect('key_press_event', self.on_key)
        self.line, = self.ax.plot([], [], 'b-')
        print("Press SPACE to pause/resume, Q to quit")

    def on_key(self, event):
        if event.key == ' ':
            self.paused = not self.paused
            status = 'PAUSED' if self.paused else 'RUNNING'
            print(f"Status: {status}")
        elif event.key == 'q':
            plt.close()

    def animate(self, frame):
        if not self.paused:
            # ... 更新数据 ...
            pass
        return self.line,

rp = RealtimePlot()
ani = animation.FuncAnimation(rp.fig, rp.animate, interval=50)
plt.show()
```

### 📝 小节练习

> [!question] 选择题 1
> 滚动窗口实时图表的 `deque(maxlen=N)` 缓冲区，在功能上等价于 C 语言中的什么数据结构？
> - [ ] A. 单向链表（linked list）
> - [ ] B. 环形缓冲区（circular buffer / ring buffer）
> - [ ] C. 二叉搜索树（BST）
> - [ ] D. 哈希表（hash table）
>
> > [!success]- 点击查看答案
> > > 正确答案: B
> > > **解析**: `deque(maxlen=N)` 的行为等价于环形缓冲区——固定容量，先进先出（FIFO）。C 语言中通常用 `int buf[N]` + 两个指针 `head`/`tail` 实现环形缓冲。

> [!question] 判断题 1
> 使用 `threading.Thread` 读取 C 程序输出时，`daemon=True` 确保主程序退出时该线程也自动终止。（ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > > 答案: 正确
> > > **解析**: `daemon=True` 将线程标记为守护线程。Python 进程退出时，所有守护线程会被自动终止（不做清理）。非守护线程（默认 `daemon=False`）会阻止进程退出，直到线程自己完成。

---

### 📚 第四节：python -c 管道一行流实时可视化
---

4.1 基础管道模式
----------------

最简单的实时可视化——不需要 `.py` 文件：

```bash
# C 程序持续输出，Python 一行流读取
./sensor_sim | python3 -c "
import sys
for line in sys.stdin:
    line = line.strip()
    if line:
        parts = line.split()
        print(f'Time: {parts[0]}, Temp: {parts[1]}, Hum: {parts[2]}')
"
```

4.2 使用 pipe 快速验证 C 程序输出
-------------------------------

```bash
# 运行 C 程序 3 秒，查看前 10 行输出
timeout 3 ./sensor_sim | head -n 10

# 运行 C 程序并过滤异常值
./sensor_sim | python3 -c "
import sys
for line in sys.stdin:
    parts = line.split()
    if len(parts) == 3:
        temp = float(parts[1])
        if temp < 20 or temp > 40:
            print(f'WARNING: {line.strip()}')
" &

# 同时保存到日志文件
./sensor_sim | tee sensor_log.txt | python3 plot_script.py
```

4.3 将 C 程序输出直接保存为 CSV 供后续分析
----------------------------------------

```bash
#!/bin/bash
# 采集 60 秒的数据
timeout 60 ./sensor_sim | python3 -c "
import sys, csv
writer = csv.writer(open('sensor_data.csv', 'w'))
writer.writerow(['time', 'temperature', 'humidity'])
for line in sys.stdin:
    if line.strip():
        writer.writerow(line.split())
"
echo "Data saved to sensor_data.csv"
```

然后离线分析：
```python
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt

df = pd.read_csv('sensor_data.csv')
sns.lineplot(data=df, x='time', y='temperature')
plt.savefig('offline_analysis.png')
```

> `timeout 60 ./sensor_sim | python3 ...` 模式是先采集数据、再离线分析。与之相对的是 `./sensor_sim | python3 live_plot.py` 的实时模式。两种模式适用于不同场景：前者适合精确的统计分析，后者适合实时监控。

### 📝 小节练习

> [!question] 选择题 1
> `./c_program | python3 -c "$(cat script.py)"` 模式中，`$(cat script.py)` 的作用是？
> - [ ] A. 将 script.py 编译为字节码
> - [ ] B. 将 script.py 的内容作为字符串传给 `python -c`
> - [ ] C. 检查 script.py 的语法
> - [ ] D. 创建一个 script.py 的副本
>
> > [!success]- 点击查看答案
> > > 正确答案: B
> > > **解析**: `$(cat script.py)` 是 bash 的命令替换——将 `cat script.py` 的输出（即文件内容）嵌入到命令行中。效果等同于 `python -c "script.py 的完整内容"`。注意：script.py 中的双引号会导致语法错误，实际使用推荐创建 .py 文件用管道连接。

> [!question] 判断题 1
> `timeout 5 ./c_program | python3 analyze.py` 中的 `timeout` 命令是 shell 内置的。（ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > > 答案: 错误
> > > **解析**: `timeout` 是 GNU Coreutils 提供的**外部命令**（`/usr/bin/timeout`），不是 bash 内置命令。它发送 SIGTERM 信号在指定时间后终止进程，如果进程不响应则发送 SIGKILL。

---

### 📚 第五节：完整实战 —— 实时传感器数据仪表盘
---

5.1 C 程序：模拟多元传感器
--------------------------

```c
// multi_sensor.c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <math.h>
#include <time.h>

int main() {
    double t = 0.0;
    double voltage = 220.0;  // 电压 (V)
    double current = 10.0;   // 电流 (A)

    srand(time(NULL));
    setvbuf(stdout, NULL, _IONBF, 0);  // 禁用缓冲

    while (1) {
        // 模拟电压波动（220V ± 5V）
        voltage = 220.0 + 3.0 * sin(t * 0.5) + (rand() % 100 - 50) * 0.02;

        // 模拟电流（随负载变化）
        current = 10.0 + 5.0 * fabs(sin(t * 0.3)) + (rand() % 100 - 50) * 0.05;

        // 计算功率 (W)
        double power = voltage * current;

        // 计算视在功率 (VA) 和功率因数
        double pf = 0.85 + 0.1 * sin(t * 0.2);

        printf("%.3f,%.3f,%.3f,%.3f,%.3f\n",
               t, voltage, current, power, pf);
        fflush(stdout);

        usleep(50000);  // 50ms → 20 samples/sec
        t += 0.05;
    }
    return 0;
}
```

5.2 Python 脚本：四面板实时仪表盘
-------------------------------

```python
import subprocess
import threading
from collections import deque
import matplotlib.pyplot as plt
import matplotlib.animation as animation
import matplotlib.gridspec as gridspec

# ---- 启动 C 程序 ----
proc = subprocess.Popen(
    ['./multi_sensor'],
    stdout=subprocess.PIPE,
    stderr=subprocess.STDOUT,
    text=True, bufsize=1,
)

# ---- 数据缓冲区 ----
N = 400
t_data = deque(maxlen=N)
v_data = deque(maxlen=N)
c_data = deque(maxlen=N)
p_data = deque(maxlen=N)
pf_data = deque(maxlen=N)
lock = threading.Lock()

def reader_thread():
    for line in proc.stdout:
        line = line.strip()
        if not line:
            continue
        try:
            t, v, c, p, pf = map(float, line.split(','))
            with lock:
                t_data.append(t)
                v_data.append(v)
                c_data.append(c)
                p_data.append(p)
                pf_data.append(pf)
        except ValueError:
            pass

reader = threading.Thread(target=reader_thread, daemon=True)
reader.start()

# ---- 创建绑图界面 ----
plt.style.use('dark_background')
fig = plt.figure(figsize=(14, 10))
gs = gridspec.GridSpec(2, 2, figure=fig)

# 面板 1: 电压
ax1 = fig.add_subplot(gs[0, 0])
ln1, = ax1.plot([], [], 'cyan', linewidth=1.5)
ax1.set_ylabel('Voltage (V)', color='cyan')
ax1.set_title('Voltage', color='cyan')
ax1.grid(True, alpha=0.2)
ax1.axhline(y=220, color='white', linestyle='--', alpha=0.5)

# 面板 2: 电流
ax2 = fig.add_subplot(gs[0, 1])
ln2, = ax2.plot([], [], 'lime', linewidth=1.5)
ax2.set_ylabel('Current (A)', color='lime')
ax2.set_title('Current', color='lime')
ax2.grid(True, alpha=0.2)

# 面板 3: 功率
ax3 = fig.add_subplot(gs[1, 0])
ln3, = ax3.plot([], [], 'orange', linewidth=1.5)
ax3.set_ylabel('Power (W)', color='orange')
ax3.set_xlabel('Time (s)')
ax3.set_title('Power', color='orange')
ax3.grid(True, alpha=0.2)

# 面板 4: 功率因数
ax4 = fig.add_subplot(gs[1, 1])
ln4, = ax4.plot([], [], 'magenta', linewidth=1.5)
ax4.set_ylabel('Power Factor', color='magenta')
ax4.set_xlabel('Time (s)')
ax4.set_title('Power Factor', color='magenta')
ax4.set_ylim(0.7, 1.0)
ax4.grid(True, alpha=0.2)

fig.suptitle('Real-time Power Monitoring Dashboard', fontsize=16,
             fontweight='bold', color='white')

def init():
    return ln1, ln2, ln3, ln4

def animate(frame):
    with lock:
        if len(t_data) > 1:
            t_list = list(t_data)

            ln1.set_data(t_list, list(v_data))
            ln2.set_data(t_list, list(c_data))
            ln3.set_data(t_list, list(p_data))
            ln4.set_data(t_list, list(pf_data))

            # 自动滚动窗口
            window = max(10, t_list[-1] - t_list[0])
            for ax in [ax1, ax2, ax3, ax4]:
                ax.set_xlim(t_list[-1] - window, t_list[-1] + 0.5)

            for ax in [ax1, ax2, ax3]:
                ax.relim()
                ax.autoscale_view(scalex=False)

    return ln1, ln2, ln3, ln4

ani = animation.FuncAnimation(
    fig, animate, init_func=init,
    interval=50, blit=True,
)

plt.tight_layout()

try:
    plt.show()
except KeyboardInterrupt:
    print("\nShutting down...")
finally:
    proc.terminate()
    proc.wait()
```

```bash
gcc -o multi_sensor multi_sensor.c -lm
python3 dashboard.py
```

5.3 导出实时图表为视频
---------------------

```python
# 修改方法：用 save() 替代 show()
ani = animation.FuncAnimation(fig, animate, init_func=init,
                               interval=50, blit=True, frames=600)
ani.save('dashboard_live.mp4', writer='ffmpeg', fps=20, dpi=100,
         extra_args=['-vcodec', 'libx264'])
```

### 📝 小节练习

> [!question] 选择题 1
> 在实时绑图脚本中使用 `proc.terminate()` 的作用是？
> - [ ] A. 暂停 C 程序的执行
> - [ ] B. 发送 SIGTERM 信号终止子进程
> - [ ] C. 关闭管道连接
> - [ ] D. 重启 C 程序
>
> > [!success]- 点击查看答案
> > > 正确答案: B
> > > **解析**: `proc.terminate()` 向子进程发送 SIGTERM（15）信号——请求正常终止。如果子进程不响应，可以用 `proc.kill()` 发送 SIGKILL（9）强制终止。两者之后都应该调用 `proc.wait()` 等待子进程真正退出并回收僵尸进程。

> [!question] 判断题 1
> C 程序中 `setvbuf(stdout, NULL, _IONBF, 0)` 的效果与 `fflush(stdout)` 完全相同。（ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > > 答案: 错误
> > > **解析**: `setvbuf(stdout, NULL, _IONBF, 0)` 是**一次性**设置 stdout 为无缓冲模式——之后每次 `printf` 都会立即输出，不需要每个 `printf` 后都调用 `fflush()`。`fflush()` 需要每次手动调用。`_IONBF` 更彻底但略微影响性能。

---

## 📋 章节测试

### 一、判断题（正确选✅，错误选❌）

> [!question] 判断题 1
> `subprocess.Popen` 创建的管道在 C 语言层面等价于 `pipe()` 系统调用 + `fork()`。（ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > > 答案: 正确
> > > **解析**: `Popen()` 封装了 `fork()` + `exec()` 组合，`stdout=subprocess.PIPE` 在底层调用 `pipe()` 创建内核管道，然后将子进程的 stdout 重定向到管道写端。

> [!question] 判断题 2
> `matplotlib.animation.FuncAnimation` 的 `interval` 参数单位是秒。（ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > > 答案: 错误
> > > **解析**: `interval` 的单位是**毫秒**（milliseconds）。`interval=100` 表示每 100ms（0.1 秒）更新一帧，即 10 FPS。

> [!question] 判断题 3
> 实时绑图脚本中，后台读取线程和绑图动画线程共享 `deque` 时不需要任何同步机制。（ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > > 答案: 错误
> > > **解析**: Python 的 GIL 使单个 Python 操作是原子的，但 `deque.append()` + `list(deque)` 组合不是原子操作——需要 `threading.Lock` 保护。否则可能在遍历 `deque` 时遇到并发修改，导致数据不一致或 RuntimeError（"deque mutated during iteration"）。

> [!question] 判断题 4
> `ani.save('output.mp4', writer='ffmpeg')` 需要系统安装 ffmpeg。（ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > > 答案: 正确
> > > **解析**: Matplotlib 本身不包含视频编码器。`writer='ffmpeg'` 调用系统安装的 ffmpeg 命令行工具进行编码。也可以使用 `writer='pillow'` 保存为 GIF（不需要 ffmpeg）。

> [!question] 判断题 5
> Python 脚本通过 `subprocess.Popen` 启动的 C 程序崩溃（SIGSEGV）时，Python 会收到异常通知。（ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > > 答案: 错误
> > > **解析**: C 程序崩溃不会主动通知 Python。Python 只会发现管道被关闭（`proc.stdout` 返回 EOF）。要检测异常退出，需要在 `proc.wait()` 后检查 `proc.returncode`——非零值表示异常终止（`-11` 对应 SIGSEGV）。

> [!question] 判断题 6
> `deque` 是线程安全的数据结构。（ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > > 答案: 部分正确
> > > **解析**: `deque` 的单个操作（`append`、`popleft` 等）是**线程安全**的（因为内部用 C 实现且 GIL 保护）。但复合操作（如 `for x in deque` + `deque.append()` 同时发生）不是线程安全的。实际应用中建议始终使用 `Lock` 保护对 `deque` 的访问。

---

### 二、选择题（单项选择题）

> [!question] 选择题 1
> C 程序中，以下哪种方式能确保每次 `printf` 的输出立即到达管道接收端？
> - [ ] A. `printf(...);` 什么都不加
> - [ ] B. `printf(...); fflush(stdout);`
> - [ ] C. `setvbuf(stdout, NULL, _IONBF, 0);` 然后 `printf(...);`
> - [ ] D. B 和 C 都可以
>
> > [!success]- 点击查看答案
> > > 正确答案: D
> > > **解析**: 两种方式都有效。B 是每次刷新，C 是一次性设置为无缓冲（全局生效）。对于持续输出的程序，`_IONBF` 更方便（不需要在每个 `printf` 后调用 `fflush`）。对于偶尔输出一条数据的程序，B 更合适。

> [!question] 选择题 2
> `matplotlib.animation.FuncAnimation` 的 `init_func` 参数的作用是？
> - [ ] A. 设置动画的起始帧号
> - [ ] B. 初始化图表为空白状态（在首帧之前调用）
> - [ ] C. 初始化 C 程序连接
> - [ ] D. 设置数据缓冲区
>
> > [!success]- 点击查看答案
> > > 正确答案: B
> > > **解析**: `init_func` 在动画的第一帧之前被调用一次，用于设置图表的初始状态（通常是将所有线条设为空数据）。它返回一个包含所有需要被更新的 artist 的元组，用于 `blit=True` 的初始渲染。

> [!question] 选择题 3
> Python 的 `threading.Thread(target=func, daemon=True)` 中 `daemon=True` 的作用是？
> - [ ] A. 提高线程优先级
> - [ ] B. 主线程退出时自动终止该线程
> - [ ] C. 使线程不可见
> - [ ] D. 使线程在后台运行（静默模式）
>
> > [!success]- 点击查看答案
> > > 正确答案: B
> > > **解析**: 守护线程（daemon thread）在主线程退出时会被 Python 自动终止。这对于后台读取线程非常重要——如果不用 daemon，用户关闭绑图窗口后进程会挂起（因为读取线程仍在阻塞等待 C 程序的输出）。

> [!question] 选择题 4
> 以下哪种方式能让 Python 启动 C 程序并**限制其运行时间**为 30 秒？
> - [ ] A. `subprocess.run(['./prog'], timeout=30)`
> - [ ] B. `proc = Popen(...); time.sleep(30); proc.terminate()`
> - [ ] C. `subprocess.Popen('timeout 30 ./prog', shell=True, ...)`
> - [ ] D. 以上全部
>
> > [!success]- 点击查看答案
> > > 正确答案: D
> > > **解析**: A 最简单——`subprocess.run()` 的 `timeout` 参数会自动超时终止。B 是手动实现。C 用 shell 的 `timeout` 命令包装。三者都可以实现限时运行，但 `subprocess.run(timeout=30)` 是最 Pythonic 的方式。

> [!question] 选择题 5
> 在实时图表中同时使用多个子图时，`blit=True` 要求什么？
> - [ ] A. 所有子图使用相同的类型
> - [ ] B. `animate` 函数返回所有被修改的 artist 对象的元组
> - [ ] C. 每个子图有独立的 `FuncAnimation`
> - [ ] D. 不需要任何额外要求
>
> > [!success]- 点击查看答案
> > > 正确答案: B
> > > **解析**: 当 `blit=True` 时，`animate()` 函数必须 `return` 一个包含所有被更新的 artist 对象的**元组**。Matplotlib 通过这些返回对象识别哪些区域需要重绘。如果遗漏了某个 artist，该对象在动画中不会更新。

> [!question] 选择题 6
> 将实时图表保存为 MP4 视频文件，除了 `matplotlib` 外，还需要什么外部依赖？
> - [ ] A. OpenCV
> - [ ] B. ffmpeg
> - [ ] C. PIL/Pillow
> - [ ] D. imagemagick
>
> > [!success]- 点击查看答案
> > > 正确答案: B
> > > **解析**: Matplotlib 调用系统的 `ffmpeg` 命令行工具将每一帧编码为 H.264 视频流。Pillow（C）可用于保存 GIF 动画，但不能生成 MP4。OpenCV 和 ImageMagick 不是 matplotlib 保存视频的依赖。

---

### 🛠️ 动手练习题

> [!example] 练习题 1：模拟传感器实时数据
> **难度**: ⭐⭐
>
> 编写 C 程序模拟一个加速度计传感器，持续输出三维加速度值（ax, ay, az），采样率 50Hz（每 20ms 一条），持续输出 30 秒：
> ```
> timestamp,ax,ay,az
> 0.00,0.12,-0.05,9.81
> ...
> ```
> Python 脚本实时绑制 3 面板滚动窗口图（ax/ay/az 各一行子图，共享 X 轴）。要求：
> - 使用 `subprocess.Popen` 启动 C 程序
> - 使用 `matplotlib.animation.FuncAnimation` 实时更新
> - 仅显示最近 5 秒的数据（滚动窗口）
> - 颜色方案：ax=红, ay=绿, az=蓝
>
> 保存为 `accel_live.py`，运行后截图。

> [!example] 练习题 2：C 程序日志实时监控
> **难度**: ⭐⭐
>
> 编写 C 程序模拟网络服务器日志输出（每秒输出 5 条）：
> ```
> timestamp,status_code,response_time_ms
> 0.200,200,12.5
> 0.400,404,8.3
> 0.600,200,15.1
> 0.800,500,200.0
> ...
> ```
> Python 脚本：
> 1. 实时累积统计各状态码的计数器
> 2. 绑制柱状图：状态码 × 数量（实时更新）
> 3. 绑制散点图：响应时间随时间变化，按状态码着色（`c=status_codes`）
> 4. 使用 Python `signal` 模块响应 Ctrl+C 优雅退出

> [!example] 练习题 3：视频导出
> **难度**: ⭐⭐
>
> 使用本章第五节的多传感器仪表盘代码，将其修改为：
> 1. 自动采集 60 秒（而非无限循环）
> 2. 将实时图表保存为 MP4 视频文件（`dashboard_demo.mp4`）
> 3. 同时在采集过程中用简单文本输出记录原始数据到 CSV 文件
> 4. 用 `subprocess.call` 检查 ffmpeg 是否安装，若未安装给出安装提示

> [!example] 练习题 4：C → Python 实时管道的性能测试
> **难度**: ⭐⭐⭐
>
> 编写 C 程序以每秒 10000 条的速度输出数据（高速数据流），测试 Python 端读取的吞吐量：
> 1. C 程序：循环输出 `timestamp,value`，用 `clock_gettime()` 获取高精度时间戳
> 2. Python 程序：通过管道读取，记录每秒实际收到的数据条数
> 3. 尝试不同方案：纯文本读取、`np.loadtxt`、C 输出的二进制格式 + Python `struct` 解析
> 4. 输出三种方案的吞吐量对比图（Matplotlib 柱状图）
>
> 注意：C 程序中禁用缓冲（`_IONBF`）或使用行缓冲。
