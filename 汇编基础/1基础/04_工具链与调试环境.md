# 工具链与调试环境 (Toolchain & Debug Environment)
---

## 📖 章节概述

汇编开发的工具链比 C 语言更底层——没有 libc、没有 ELF 装载器帮你处理一切。本章搭建 NASM 汇编器 + ld 链接器 + GDB 调试 + QEMU 虚拟裸机的完整环境，这是后续所有硬件操作章节的前置基础。如果你从 C 语言教程转过来，会发现这和 `gcc && ./a.out` 的工作方式有本质不同——你需要亲自指定输出格式、入口符号和段布局。

> **核心理念**：工具链搭建的目的不只是"能用"，而是"看清楚每一步发生了什么"。因此本章同时介绍 objdump 反汇编和 GDB layout asm——让你从第一天起就能观察汇编指令如何在 CPU 里跑。

---

### 📚 第一节：NASM 汇编器
---

#### 1.1 安装

```bash
# Linux (Debian/Ubuntu)
sudo apt install nasm binutils gdb qemu-system-x86

# Linux (Arch)
sudo pacman -S nasm binutils gdb qemu-base

# macOS
brew install nasm
```

验证安装：
```bash
nasm --version     # NASM version 2.x
ld --version       # GNU ld (GNU Binutils)
gdb --version
qemu-system-x86_64 --version
```

#### 1.2 第一个 NASM 程序：裸机 Hello World

先看一个最简 NASM 程序（不依赖任何 C 库）：

```asm
; hello.asm — 裸机 Hello World (x86-64 Linux)
; 编译: nasm -f elf64 hello.asm && ld hello.o -o hello && ./hello

section .data
    msg db 'Hello from raw assembly!', 0x0a  ; 0x0a = '\n'
    len equ $ - msg

section .text
    global _start

_start:
    mov rax, 1          ; syscall number: sys_write
    mov rdi, 1          ; fd: stdout
    mov rsi, msg        ; buf
    mov rdx, len        ; count
    syscall             ; write(1, msg, len)

    mov rax, 60         ; syscall number: sys_exit
    xor rdi, rdi        ; exit code: 0
    syscall             ; exit(0)
```

编译运行：
```bash
nasm -f elf64 hello.asm   # 汇编: .asm → .o (ELF64 目标文件)
ld hello.o -o hello       # 链接: .o → 可执行文件
./hello                   # Hello from raw assembly!
```

一步步解析：
1. `-f elf64`：指定输出格式为 ELF64（Linux x86-64）。NASM 支持多种格式（`elf32`、`macho64`、`bin` 等）
2. 没有 `#include`、没有 `main`、没有 libc——起点就是 `_start`
3. `syscall` 直接调用 Linux 内核——这是汇编的力量：不需要任何中间层就能操作系统

> 对比 C 语言：`gcc hello.c` 背后做了预处理→编译→汇编→链接→包含 libc 启动代码（`_start`→`__libc_start_main`→`main`）→动态链接。NASM 程序跳过了这一切，你亲手控制每一个字节。

#### 1.3 NASM 常用命令选项

| 选项 | 作用 |
|------|------|
| `-f elf64` | 输出 ELF64 格式（Linux x86-64） |
| `-f bin` | 输出纯二进制（用于 bootloader） |
| `-f elf32` | 输出 ELF32 格式（x86 32位） |
| `-o file.o` | 指定输出文件名 |
| `-g` | 生成调试信息（DWARF 格式） |
| `-F dwarf` | 显式指定调试格式为 DWARF |
| `-l list.lst` | 生成列表文件（地址+机器码+源码对照） |
| `-D SYMBOL=value` | 定义预处理符号 |
| `-I dir` | 添加 include 搜索路径 |

带调试信息的编译：
```bash
nasm -f elf64 -g -F dwarf hello.asm -o hello.o
ld hello.o -o hello
gdb ./hello
```

### 📝 小节练习

> [!question] 选择题 1
> `nasm -f elf64` 中的 `-f elf64` 含义是？
> - [ ] A. 生成 64 位库文件
> - [ ] B. 指定输出文件名为 elf64
> - [ ] C. 指定输出格式为 ELF64
> - [ ] D. 快速编译模式
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > **解析**: `-f` 是 format 的缩写，`elf64` 是 Linux x86-64 的标准目标文件格式。

> [!question] 判断题 1
> NASM 编译的 `_start` 程序在链接时自动包含 libc 启动代码。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: 裸 NASM 程序从 `_start` 直接开始，无任何 libc 依赖。`syscall` 指令直接与 Linux 内核交互，跳过所有标准库中间层。

---

### 📚 第二节：ld 链接与 ELF 结构
---

#### 2.1 ld 链接器基础

```bash
# 基本链接
ld hello.o -o hello

# 多目标文件链接
ld main.o io.o utils.o -o program

# 指定入口符号（默认为 _start）
ld -e my_entry main.o -o program

# 动态链接 libc
ld -dynamic-linker /lib64/ld-linux-x86-64.so.2 \
   /usr/lib/x86_64-linux-gnu/crt1.o \
   /usr/lib/x86_64-linux-gnu/crti.o \
   hello.o -lc \
   /usr/lib/x86_64-linux-gnu/crtn.o \
   -o hello
```

> 动态链接 libc 的复杂命令解释了为什么 C 程序员习惯 `gcc` 而非直接 `ld`。

#### 2.2 链接脚本（Linker Script）

ld 默认使用内置链接脚本。对于裸机程序（后续章节），你需要自定义：

```ld
/* link.ld — 裸机程序的链接脚本 */
SECTIONS {
    . = 1M;                  /* 加载到 1MB 地址处 */
    .text : { *(.text) }     /* 代码段 */
    .data : { *(.data) }     /* 数据段 */
    .bss  : { *(.bss)  }     /* 未初始化数据 */
}
```

使用自定义脚本：
```bash
ld -T link.ld kernel.o -o kernel.bin
```

### 📝 小节练习

> [!question] 选择题 1
> ld 链接器的默认入口符号是？
> - [ ] A. `main`
> - [ ] B. `_start`
> - [ ] C. `start`
> - [ ] D. `entry`
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: ld 默认查找 `_start` 符号作为程序入口。C 语言程序由 crt1.o 提供 `_start` 然后调用 `main`。

---

### 📚 第三节：objdump 与 readelf 反汇编
---

#### 3.1 objdump：窥视程序的真实面目

```bash
# 反汇编所有代码段
objdump -d hello

# 显示完整机器码+源码混合
objdump -d -M intel -S hello

# 查看段（section）信息
objdump -h hello

# 查看符号表
objdump -t hello
```

典型输出：
```asm
$ objdump -d -M intel hello | grep -A8 '<_start>'
<_start>:
  401000:   b8 01 00 00 00          mov    eax, 0x1
  401005:   bf 01 00 00 00          mov    edi, 0x1
  40100a:   48 be 00 20 40 00 00    movabs rsi, 0x402000
  401014:   ba 18 00 00 00          mov    edx, 0x18
  401019:   0f 05                   syscall
  40101b:   b8 3c 00 00 00          mov    eax, 0x3c
  401020:   31 ff                   xor    edi, edi
  401022:   0f 05                   syscall
```

> 注意：`mov eax, 0x1` 的高 32 位自动清零（x86-64 特性），NASM 源码中写 `mov rax, 1` 但汇编器聪明地生成了更短的 `mov eax` 形式。

#### 3.2 readelf：解析 ELF 结构

```bash
# 查看 ELF 文件头
readelf -h hello

# 查看段头表（Section Headers）
readelf -S hello

# 查看程序头表（Program Headers）
readelf -l hello
```

#### 3.3 NASM 列表文件：源码→机器码逐行对应

```bash
nasm -f elf64 -l hello.lst hello.asm
cat hello.lst
```

```
1          section .data
2 00000000 48656C6C-  msg db 'Hello from raw assembly!', 0x0a
2 00000008 6F206672-
...
9          section .text
10          _start:
11 00000000 B801000000      mov rax, 1
12 00000005 BF01000000      mov rdi, 1
```

> 列表文件是最直观的"汇编→机器码"对照表。每行包含：行号、偏移地址、十六进制机器码、源码。

### 📝 小节练习

> [!question] 选择题 1
> `objdump -d` 的 `-d` 选项含义是？
> - [ ] A. debug（调试）
> - [ ] B. disassemble（反汇编）
> - [ ] C. data（数据段）
> - [ ] D. dynamic（动态链接）
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: `-d` 用于反汇编 `.text` 等可执行段，输出助记符形式的汇编指令。

---

### 📚 第四节：GDB 汇编级调试
---

#### 4.1 编译带调试信息的程序

```bash
nasm -f elf64 -g -F dwarf hello.asm -o hello.o
ld hello.o -o hello
gdb ./hello
```

#### 4.2 核心命令

```gdb
# 汇编级单步 (si = step instruction)
(gdb) si

# 汇编级跳过 (ni = next instruction)
(gdb) ni

# 显示汇编布局 (TUI 模式)
(gdb) layout asm
(gdb) layout regs    # 寄存器 + 汇编窗口

# 打印寄存器
(gdb) info registers
(gdb) p $rax
(gdb) p /x $rax     # 十六进制显示

# 查看内存
(gdb) x /10i $rip   # 当前地址后 10 条指令
(gdb) x /16bx msg   # 以十六进制字节查看 msg 标签处的内存
(gdb) x /s msg      # 以字符串查看 msg

# 断点
(gdb) break *_start
(gdb) break *0x401000

# 反汇编当前函数
(gdb) disassemble _start
```

GDB 汇编调试工作流：
```gdb
$ gdb ./hello
(gdb) break *_start
(gdb) run
(gdb) layout regs      # 打开寄存器窗口
(gdb) si               # 单步执行一条汇编指令
(gdb) info registers rax rdi rsi rdx
(gdb) si               # 继续单步…
(gdb) continue
```

#### 4.3 对比 C 语言调试

| 操作 | GDB for C | GDB for ASM |
|------|-----------|-------------|
| 单步 | `n` (next, 源代码行) | `si` (step instruction) |
| 查看变量 | `p var_name` | `x /4x $rsp+8` (查栈) |
| 函数栈 | `bt` | `bt` + `x /16gx $rsp` (手动分析) |
| 反汇编 | `disas func` | `disas _start` (无函数概念) |

### 📝 小节练习

> [!question] 选择题 1
> GDB 中 `si` 命令与 `ni` 命令的区别是？
> - [ ] A. 没有区别
> - [ ] B. `si` 进入 call，`ni` 跳过 call
> - [ ] C. `ni` 进入 call，`si` 跳过 call
> - [ ] D. `si` 单步汇编指令，`ni` 单步源代码行
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: `si` (step instruction) 会进入 `call` 指令内部继续单步；`ni` (next instruction) 把 `call` 当作整体执行完毕。与 C 调试中的 `s`/`n` 概念一致。

---

### 📚 第五节：QEMU 虚拟裸机环境
---

#### 5.1 为什么需要 QEMU

在 Linux 用户态下，多数"直接操作硬件"的指令被禁止：
- `in`/`out`（端口 I/O）→ 触发段错误
- `hlt`（停机）→ 段错误
- `cli`/`sti`（开关中断）→ 权限不足
- MMIO 访问 → 虚拟地址空间不映射

QEMU 提供一个完整的虚拟 x86 计算机，你可以从 BIOS/UEFI 启动到第一条指令，没有任何限制。

#### 5.2 QEMU 基础命令

```bash
# 从裸机镜像启动
qemu-system-x86_64 -drive file=kernel.bin,format=raw

# 标准配置（无图形输出，串口重定向）
qemu-system-x86_64 \
    -drive file=kernel.bin,format=raw \
    -nographic \
    -serial stdio \
    -monitor telnet:127.0.0.1:4444,server,nowait

# 带 GDB 远程调试
qemu-system-x86_64 \
    -drive file=kernel.bin,format=raw \
    -nographic \
    -s -S       # -s: 在 1234 端口开 GDB 服务, -S: 启动时暂停
```

然后在另一个终端连接：
```bash
gdb
(gdb) target remote :1234
(gdb) layout asm
```

#### 5.3 QEMU Monitor：查看硬件状态

QEMU Monitor 让你在虚拟机运行时检查 CPU 状态：

```bash
# 在 QEMU Monitor 中（Ctrl-A C 进入）：
(qemu) info registers          # 所有 CPU 寄存器
(qemu) xp /8i 0x7c00          # 查看物理地址 0x7c00 的指令
(qemu) xp /16bx 0xb8000       # 查看 VGA 显存
(qemu) info pic                # 中断控制器状态
(qemu) info ioapic             # I/O APIC 状态
(qemu) stop                    # 暂停 CPU
(qemu) cont                    # 恢复执行
```

> QEMU Monitor 是硬件调试的最强工具——你能"停机"后检查 CPU 寄存器、内存和外设寄存器，这在真机上做不到。

#### 5.4 典型工作流

```bash
# 终端 1: 启动 QEMU（挂起等待 GDB）
qemu-system-x86_64 -drive file=kernel.bin,format=raw -nographic -s -S

# 终端 2: GDB 连接并调试
gdb -ex "target remote :1234" -ex "layout asm"
# (gdb) break *0x7c00
# (gdb) continue
# (gdb) si
```

### 📝 小节练习

> [!question] 判断题 1
> 在 Linux 用户态程序中执行 `in al, 0x60`（读键盘端口）不会报错。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: `in`/`out` 是特权指令（IOPL 限定），用户态执行会触发段错误。这也是为什么"直接操作硬件"必须用裸机环境（如 QEMU）或内核模块。

---

## 📋 章节测试

### 一、判断题

> [!question] 判断题 1
> NASM 编译的程序从 `_start` 入口开始执行，不依赖任何 C 标准库。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确

> [!question] 判断题 2
> `nasm -f elf64` 生成的是纯二进制文件（flat binary）。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: `-f elf64` 生成 ELF64 格式目标文件（带符号表、段头）。`-f bin` 才是纯二进制。

> [!question] 判断题 3
> `objdump -d` 只反汇编 `.text` 段，不显示数据段。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > **解析**: `-d` 只反汇编可执行段。如需同时 dump 数据段，使用 `objdump -D`（反汇编所有段）或 `objdump -s`（显示所有段的内容）。

> [!question] 判断题 4
> GDB 的 `si` 命令在遇到 `call` 指令时会进入被调用函数内部单步。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确

> [!question] 判断题 5
> QEMU 的 `-s` 选项在端口 12345 上开启 GDB 调试服务。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > **解析**: 默认端口是 **1234**。可使用 `-gdb tcp::端口号` 指定自定义端口。

### 二、选择题

> [!question] 选择题 1
> NASM 源码中 `section .text` 的作用是？
> - [ ] A. 声明注释
> - [ ] B. 指定代码段
> - [ ] C. 函数定义
> - [ ] D. 数据分配
>
> > [!success]- 点击查看答案
> > 正确答案: B

> [!question] 选择题 2
> 裸 NASM 程序调用 Linux 内核的指令是？
> - [ ] A. `int 0x80`
> - [ ] B. `call kernel`
> - [ ] C. `syscall`
> - [ ] D. `svc 0`
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > **解析**: x86-64 Linux 使用 `syscall`（MSR 快速系统调用）而非 `int 0x80`（32 位旧接口）。ARM 对应 `svc`。

> [!question] 选择题 3
> QEMU Monitor 中查看物理内存的命令是？
> - [ ] A. `x`
> - [ ] B. `xp`
> - [ ] C. `pmem`
> - [ ] D. `dump`
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: `xp` (eXamine Physical memory) 查看物理地址内容——在裸机/内核调试中，GDB 的 `x` 命令只能看虚拟地址。

> [!question] 选择题 4
> 以下哪个选项让 GDB 在 TUI 模式下同时显示寄存器和汇编？
> - [ ] A. `layout src`
> - [ ] B. `layout regs`
> - [ ] C. `layout asm`
> - [ ] D. `tui enable`
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: `layout regs` 在 TUI 中打开寄存器窗口+汇编窗口。`layout asm` 只有汇编，`layout split` 提供源码+汇编对照。

> [!question] 选择题 5
> `nasm -l hello.lst` 生成的列表文件包含？
> - [ ] A. 仅源码
> - [ ] B. 仅机器码十六进制
> - [ ] C. 行号、偏移地址、十六进制机器码、源码
> - [ ] D. 编译错误列表
>
> > [!success]- 点击查看答案
> > 正确答案: C

> [!question] 选择题 6
> 链接脚本中 `. = 1M;` 的含义是？
> - [ ] A. 程序大小限制为 1MB
> - [ ] B. 加载地址设为 1MB 处
> - [ ] C. 栈大小设为 1MB
> - [ ] D. 对齐到 1MB
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > **解析**: 链接脚本中 `.` (location counter) 设置当前位置（加载地址）。`. = 1M` 将代码加载地址定在 1MB 处。

### 🛠️ 动手练习题

> [!example] 练习题 1：亲手编译调试
> **难度**: ⭐
>
> 编写一个 NASM 程序，计算 `1 + 2 + ... + 100`，结果存入 `rax`，然后用 `sys_exit` 以结果为退出码退出。用 `objdump -d` 查看生成的机器码，用 `echo $?` 验证结果（应为 5050）。

> [!example] 练习题 2：GDB 单步跟踪
> **难度**: ⭐⭐
>
> 对练习题 1 的程序，使用 GDB 进行汇编级单步调试：
> 1. `break *_start`
> 2. `layout regs`
> 3. 逐条 `si`，观察 `rax` 的变化
> 4. 在 `syscall` 之前查看最终值

> [!example] 练习题 3：NASM 列表文件对照
> **难度**: ⭐
>
> 使用 `nasm -l` 生成列表文件，对照每一行的机器码（十六进制）和助记符源码。找出 `mov rax, 1` 对应的实际机器码字节。

> [!example] 练习题 4：QEMU 裸机启动
> **难度**: ⭐⭐⭐
>
> 使用 NASM `-f bin` 编译一个极简裸机程序（让 CPU 进入死循环 `jmp $`），用 QEMU 加载并在 Monitor 中查看 `info registers`。参考：
> ```asm
> ; bare.asm
> bits 16
> hlt: jmp hlt
> ```
> ```bash
> nasm -f bin bare.asm -o bare.bin
> qemu-system-x86_64 -drive file=bare.bin,format=raw -nographic -monitor stdio
> ```
