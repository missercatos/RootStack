---
C++ 功能库 — 索引
---

# C++ 功能库 (标准库非容器部分)

> C++ 标准库远超 STL 容器。以下按类别索引核心功能库。

## 字符串

| 文件 | 内容 |
|------|------|
| [[./字符串/string|string]] | `string`、`string_view`、`to_string`、`stoi`/`stod` — 动态字符串与类型转换 |
| [[./字符串/regex|regex]] | `regex`、`smatch`、`regex_search`/`regex_match`/`regex_replace` — 正则表达式 |

## 输入输出

| 文件 | 内容 |
|------|------|
| [[./输入输出/iostream|iostream]] | `cin`/`cout`/`cerr`、流操纵符（格式控制） |
| [[./输入输出/fstream|fstream]] | `ifstream`/`ofstream`/`fstream`、打开模式、二进制读写 |
| [[./输入输出/sstream|sstream]] | `stringstream`/`istringstream`/`ostringstream` — 内存格式化与解析 |
| [[./输入输出/filesystem|filesystem]] | `path`、`directory_iterator`、`create_directory`/`copy`/`remove` — 文件系统操作 |

## 算法

| 文件 | 内容 |
|------|------|
| [[./算法/sort_search|sort / search]] | `sort`、`binary_search`、`lower_bound`、`upper_bound`、`equal_range` |
| [[./算法/find_count|find / count]] | `find`、`find_if`、`count`、`count_if`、`all_of`/`any_of`/`none_of` |
| [[./算法/modify|modify]] | `copy`、`move`、`swap`、`replace`、`remove_if`、`unique` |
| [[./算法/range|C++20 ranges]] | `views`、`filter`/`transform`/`take` 管道、`RANGES::sort` |

## 数值

| 文件 | 内容 |
|------|------|
| [[./数值/random|random]] | `random_device`、`mt19937`、均匀/正态/伯努利分布 |
| [[./数值/chrono|chrono]] | `duration`、`time_point`、`system_clock`/`steady_clock`、时间字面量 |
| [[./数值/numeric|numeric]] | `complex`、`numeric_limits`、`ratio`、`valarray`、`numbers`(C++20) |

## 并发

| 文件 | 内容 |
|------|------|
| [[./并发/thread|thread]] | `thread`、`jthread`、`join`/`detach`、`thread_local` |
| [[./并发/mutex|mutex]] | `mutex`、`lock_guard`、`unique_lock`、`shared_mutex`、`condition_variable`、`semaphore`/`latch`/`barrier` |
| [[./并发/atomic|atomic]] | `atomic`、`memory_order`、`compare_exchange` |
| [[./并发/future|future]] | `future`、`promise`、`async`、`packaged_task`、`shared_future` |

## 内存

| 文件 | 内容 |
|------|------|
| [[./内存/smart_ptr|smart_ptr]] | `unique_ptr`、`shared_ptr`、`weak_ptr`、`make_unique`/`make_shared` |
| [[./内存/allocator|allocator]] | `allocator`、`allocator_traits`、`pmr` (多态内存资源) |

## 函数式

| 文件 | 内容 |
|------|------|
| [[./函数式/function|function / bind]] | `function`、`bind`、`placeholders`、`reference_wrapper` |
| [[./函数式/lambda|lambda]] | lambda 语法、捕获模式、泛型 lambda、立即执行 |
| [[./函数式/hash|hash]] | `hash`、自定义哈希函数对象、哈希组合 |

## 工具

| 文件 | 内容 |
|------|------|
| [[./工具/pair_tuple|pair / tuple]] | `pair`、`tuple`、`tie`、结构化绑定 |
| [[./工具/optional_variant|optional / variant / any / expected]] | `optional`、`variant`、`any`、`expected`(C++23) |
| [[./工具/span_bitset|span / bitset / byte]] | `span`、`bitset`、`byte`(C++17) |
