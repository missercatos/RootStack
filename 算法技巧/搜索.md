## ==========================================================================
C++ 算法技巧 — 搜索 (Search: DFS & BFS)
## ==========================================================================

深度优先搜索(DFS)和广度优先搜索(BFS)是遍历状态空间的基本方法，应用广泛。

**前置知识**: [[数组]]、[[循环]]、[[递推递归]]

## --------------------------------------------------------------------------
### P1605 迷宫
## --------------------------------------------------------------------------

题目: N×M的迷宫，有T个障碍物。从起点到终点的方法总数，要求不经过障碍物且不重复走同一格。

思路: DFS回溯，标记已访问的格子，到达终点则计数+1。

```cpp
#include <iostream>
using namespace std;
int n, m, t, sx, sy, fx, fy, ans;
int maze[10][10];  // 0可走，1障碍
int vis[10][10];
int dx[] = {0, 0, 1, -1};
int dy[] = {1, -1, 0, 0};

void dfs(int x, int y) {
    if (x == fx && y == fy) { ans++; return; }
    for (int i = 0; i < 4; i++) {
        int nx = x + dx[i], ny = y + dy[i];
        if (nx >= 1 && nx <= n && ny >= 1 && ny <= m
            && !maze[nx][ny] && !vis[nx][ny]) {
            vis[nx][ny] = 1;
            dfs(nx, ny);
            vis[nx][ny] = 0;
        }
    }
}

int main() {
    cin >> n >> m >> t >> sx >> sy >> fx >> fy;
    for (int i = 0, x, y; i < t; i++) {
        cin >> x >> y;
        maze[x][y] = 1;
    }
    vis[sx][sy] = 1;
    dfs(sx, sy);
    cout << ans << endl;
    return 0;
}
```

## --------------------------------------------------------------------------
### P1443 马的遍历
## --------------------------------------------------------------------------

题目: N×M的棋盘，马在起点(x,y)走日字。求马到达棋盘上每个点的最少步数。

思路: BFS从起点出发，逐层扩展，记录每个格子最短步数。

```cpp
#include <iostream>
#include <queue>
#include <cstring>
#include <cstdio>
using namespace std;

int n, m, dist[405][405];
int dx[] = {-2, -2, -1, -1, 1, 1, 2, 2};
int dy[] = {-1, 1, -2, 2, -2, 2, -1, 1};

int main() {
    memset(dist, -1, sizeof(dist));
    int sx, sy;
    cin >> n >> m >> sx >> sy;
    queue<pair<int,int>> q;
    dist[sx][sy] = 0;
    q.push({sx, sy});
    while (!q.empty()) {
        auto [x, y] = q.front(); q.pop();
        for (int i = 0; i < 8; i++) {
            int nx = x + dx[i], ny = y + dy[i];
            if (nx >= 1 && nx <= n && ny >= 1 && ny <= m && dist[nx][ny] == -1) {
                dist[nx][ny] = dist[x][y] + 1;
                q.push({nx, ny});
            }
        }
    }
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++)
            printf("%-5d", dist[i][j]);
        printf("\n");
    }
    return 0;
}
```

## --------------------------------------------------------------------------
### P2404 自然数拆分
## --------------------------------------------------------------------------

题目: 将自然数n拆分成若干个大于0的自然数之和，按字典序输出所有拆分方案。

思路: DFS递归拆分，用数组记录当前拆分数，从小到大选数避免重复。

```cpp
#include <iostream>
using namespace std;
int n, a[15];

void dfs(int sum, int pre, int idx) {
    if (sum == n) {
        cout << n << "=";
        for (int i = 0; i < idx; i++) {
            if (i) cout << "+";
            cout << a[i];
        }
        cout << endl;
        return;
    }
    for (int i = pre; i <= n - sum && i < n; i++) {
        a[idx] = i;
        dfs(sum + i, i, idx + 1);
    }
}

int main() {
    cin >> n;
    dfs(0, 1, 0);
    return 0;
}
```

## --------------------------------------------------------------------------
### P2392 kkksc03考前临时抱佛脚
## --------------------------------------------------------------------------

题目: 有4科，每科有s_i道题，每道题需要t_i时间。每科可以同时开始做，但同一科只能一道一道做。
问完成所有题的最短时间。

思路: 对每科使用01背包的思想，将题目分成两组使得两组时间差最小（即尽量平均分配）。

```cpp
#include <iostream>
#include <cstring>
using namespace std;
int s[5], t[25], dp[1205];

int main() {
    for (int i = 0; i < 4; i++) cin >> s[i];
    int ans = 0;
    for (int i = 0; i < 4; i++) {
        int sum = 0;
        for (int j = 0; j < s[i]; j++) {
            cin >> t[j];
            sum += t[j];
        }
        memset(dp, 0, sizeof(dp));
        for (int j = 0; j < s[i]; j++)
            for (int k = sum / 2; k >= t[j]; k--)
                dp[k] = max(dp[k], dp[k - t[j]] + t[j]);
        ans += sum - dp[sum / 2];
    }
    cout << ans << endl;
    return 0;
}
```

## ==========================================================================
### 📝 章节测试
## ==========================================================================

#### 判断题

> [!question] 判断题 1
> DFS使用栈（或递归调用栈）实现。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: DFS的回溯特性天然对应栈的后进先出结构。

> [!question] 判断题 2
> BFS可以求无权图的最短路径。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: BFS按层扩展，第一次到达某节点的路径就是最短路径（无权图）。

> [!question] 判断题 3
> DFS回溯时需要恢复状态（如取消标记）。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 回溯时需要撤销之前的操作，否则会影响其他分支的搜索。

> [!question] 判断题 4
> BFS使用队列实现。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: BFS按层扩展需要先进先出的特性，用queue实现。

> [!question] 判断题 5
> 迷宫问题中，DFS可以找到所有路径。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: DFS遍历所有可能的路径，配合回溯可以找出全部方案。

> [!question] 判断题 6
> BFS的空间复杂度通常比DFS高。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: BFS需要存储整层的节点，最坏O(分支数^深度)；DFS只需O(深度)栈空间。

> [!question] 判断题 7
> 马的遍历问题适合用BFS求最短步数。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 每步代价相同（无权），BFS天然求最短步数。

> [!question] 判断题 8
> DFS的时间复杂度是指数级的。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > 
> > **解析**: 不一定。图上DFS是O(V+E)，只有状态空间为指数级时DFS才是指数级。

> [!question] 判断题 9
> 自然数拆分问题中，从小到大选数可以避免重复。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 要求下一个数≥上一个数，确保不产生重复排列。

> [!question] 判断题 10
> 剪枝可以显著减少DFS的搜索空间。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 在搜索过程中提前判断不可能到达目标的分支并剪去，大幅提高效率。

#### 选择题

> [!question] 选择题 1
> BFS中标记visited的最佳时机是？
> - [ ] A. 出队时
> - [ ] B. 入队时
> - [ ] C. 处理完时
> - [ ] D. 无需标记
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 入队时标记可避免重复入队，比出队时标记更高效。

> [!question] 选择题 2
> 马走日字有几个方向？
> - [ ] A. 4个
> - [ ] B. 6个
> - [ ] C. 8个
> - [ ] D. 12个
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: 马走日字有8个可能方向：(±1,±2)和(±2,±1)。

> [!question] 选择题 3
> DFS搜索树中，回溯发生在？
> - [ ] A. 到达终点时
> - [ ] B. 所有子节点都已访问时
> - [ ] C. A和B都对
> - [ ] D. 开始搜索时
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: 到达终点或无路可走时都需要回溯到上一层继续搜索其他分支。

> [!question] 选择题 4
> 自然数拆分n=4的方案数（不含4本身）？
> - [ ] A. 2
> - [ ] B. 3
> - [ ] C. 4
> - [ ] D. 5
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: 4=1+3=1+1+2=1+1+1+1=2+2，共4种（不含4本身）。

> [!question] 选择题 5
> P2392考前临时抱佛脚问题本质是？
> - [ ] A. 图搜索
> - [ ] B. 每科将题目分两组使时间差最小
> - [ ] C. 排序
> - [ ] D. 贪心选择
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 每科的题目分成两部分同时做，总时间=两部分中较大的那个，需要最小化较大值。

> [!question] 选择题 6
> BFS的层次性质保证了什么？
> - [ ] A. 找到的路径最短
> - [ ] B. 找到的路径最长
> - [ ] C. 路径唯一
> - [ ] D. 空间最小
>
> > [!success]- 点击查看答案
> > 正确答案: A
> > 
> > **解析**: BFS逐层扩展，第一次到达目标的层数就是最短距离。

> [!question] 选择题 7
> 以下哪种情况适合用DFS而非BFS？
> - [ ] A. 求最短路径
> - [ ] B. 求所有方案数
> - [ ] C. 求最少步数
> - [ ] D. 判断是否连通
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 求所有方案需要遍历全部路径，DFS的回溯特性更合适。判断连通和求最短路两者都行。

> [!question] 选择题 8
> vis[sx][sy]=1 放在dfs调用前是为了？
> - [ ] A. 标记起点已访问
> - [ ] B. 初始化数组
> - [ ] C. 计算距离
> - [ ] D. 排序
>
> > [!success]- 点击查看答案
> > 正确答案: A
> > 
> > **解析**: 起点在搜索开始前就应标记为已访问，避免走回起点。

> [!question] 选择题 9
> DFS栈溢出通常是因为？
> - [ ] A. 递归深度过大
> - [ ] B. 数组太大
> - [ ] C. 循环太多
> - [ ] D. 输入数据太大
>
> > [!success]- 点击查看答案
> > 正确答案: A
> > 
> > **解析**: 递归每层占用栈空间，深度太大（如10^6层）会导致栈溢出。

> [!question] 选择题 10
> 以下哪个不是搜索的优化方法？
> - [ ] A. 剪枝
> - [ ] B. 记忆化
> - [ ] C. 排序
> - [ ] D. 增加状态空间
>
> > [!success]- 点击查看答案
> > 正确答案: D
> > 
> > **解析**: 增加状态空间只会增加搜索量，不是优化。剪枝、记忆化、排序都能减少搜索量。

#### 动手练习题

> [!question] 动手练习 1
> **题目**: 实现迷宫DFS：n×m网格有障碍，求从(sx,sy)到(fx,fy)的路径总数。
> 
> **提示**: DFS+回溯标记
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
using namespace std;
int n, m, sx, sy, fx, fy, ans;
int maze[15][15], vis[15][15];
int dx[]={0,0,1,-1}, dy[]={1,-1,0,0};
void dfs(int x, int y) {
    if (x==fx && y==fy) { ans++; return; }
    for (int i=0; i<4; i++) {
        int nx=x+dx[i], ny=y+dy[i];
        if (nx>=1&&nx<=n&&ny>=1&&ny<=m&&!maze[nx][ny]&&!vis[nx][ny]) {
            vis[nx][ny]=1;
            dfs(nx,ny);
            vis[nx][ny]=0;
        }
    }
}
int main() {
    int t;
    cin>>n>>m>>t>>sx>>sy>>fx>>fy;
    while(t--){int x,y;cin>>x>>y;maze[x][y]=1;}
    vis[sx][sy]=1;
    dfs(sx,sy);
    cout<<ans<<endl;
    return 0;
}
> > ```

> [!question] 动手练习 2
> **题目**: BFS求马从(sx,sy)到棋盘每点的最少步数，无法到达输出-1。
> 
> **提示**: BFS+dist数组初始化为-1
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
#include <queue>
#include <cstring>
#include <cstdio>
using namespace std;
int dist[405][405];
int dx[]={-2,-2,-1,-1,1,1,2,2};
int dy[]={-1,1,-2,2,-2,2,-1,1};
int main() {
    int n,m,sx,sy;
    cin>>n>>m>>sx>>sy;
    memset(dist,-1,sizeof(dist));
    queue<pair<int,int>> q;
    dist[sx][sy]=0; q.push({sx,sy});
    while(!q.empty()) {
        auto [x,y]=q.front(); q.pop();
        for(int i=0;i<8;i++) {
            int nx=x+dx[i],ny=y+dy[i];
            if(nx>=1&&nx<=n&&ny>=1&&ny<=m&&dist[nx][ny]==-1){
                dist[nx][ny]=dist[x][y]+1;
                q.push({nx,ny});
            }
        }
    }
    for(int i=1;i<=n;i++){for(int j=1;j<=m;j++)printf("%-5d",dist[i][j]);printf("\n");}
    return 0;
}
> > ```

> [!question] 动手练习 3
> **题目**: DFS输出自然数n的所有拆分方案（按字典序，不含n本身）。
> 
> **示例**: 输入4，输出1+1+1+1, 1+1+2, 1+3, 2+2
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
using namespace std;
int n, a[20];
void dfs(int sum, int pre, int idx) {
    if (sum == n) {
        for (int i=0;i<idx;i++) { if(i)cout<<"+"; cout<<a[i]; }
        cout<<endl;
        return;
    }
    for (int i=pre; i<=n-sum && i<n; i++) {
        a[idx]=i;
        dfs(sum+i, i, idx+1);
    }
}
int main() { cin>>n; dfs(0,1,0); return 0; }
> > ```

### 🔗 推荐练习题（洛谷）

- [P1605 迷宫](https://www.luogu.com.cn/problem/P1605)
- [P1443 马的遍历](https://www.luogu.com.cn/problem/P1443)
- [P1030 求先序排列](https://www.luogu.com.cn/problem/P1030)


## --------------------------------------------------------------------------
### 相关技巧
## --------------------------------------------------------------------------

- **[[递推递归]]**: DFS 本质是递归
- **[[动态规划]]**: 记忆化搜索 = 递归 + 缓存 = DP 前身
- **[[图]]**: BFS/DFS 在图遍历中的应用
- **[[连通性]]**: 搜索判断图的连通性
- **[[暴力枚举]]**: 搜索是高效化的暴力枚举

### 相关数据结构

- **[[../数据结构/B_栈_Stack]]**: DFS 用栈(递归调用栈)
- **[[../数据结构/F_队列_Queue]]**: BFS 用队列逐层扩展
- **[[../数据结构/I_树_Tree_BST_AVL]]**: 树的 DFS/BFS 遍历
- **[[../数据结构/H_图_Graph]]**: 图上的搜索遍历
