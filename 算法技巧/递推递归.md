## ==========================================================================
C++ 算法技巧 — 递推与递归 (Iteration & Recursion)
## ==========================================================================

递推通过已知值逐步推导未知值；递归将大问题分解为同结构的小问题。

**前置知识**: [[数组]]、[[循环]]、[[函数结构体]]

## --------------------------------------------------------------------------
### P1028 [NOIP2001 普及组] 数的计算
## --------------------------------------------------------------------------

题目: 给出n，求有多少个数列满足：第一个数为n，后面每个数不超过前一个数的一半，
直到不能再加数为止。

思路: 递推公式 f[n] = 1 + Σ_{i=1}^{n/2} f[i]，其中f[1]=1。或者递归加记忆化。

```cpp
#include <iostream>
using namespace std;
int f[1001];

int main() {
    int n;
    cin >> n;
    f[1] = 1;
    for (int i = 2; i <= n; i++) {
        for (int j = 1; j <= i / 2; j++)
            f[i] += f[j];
        f[i]++; // 包含自身
    }
    cout << f[n] << endl;
    return 0;
}
```

## --------------------------------------------------------------------------
### P1036 [NOIP2002 普及组] 选数
## --------------------------------------------------------------------------

题目: 已知n个整数，任选k个相加，求和为素数的方案数。

思路: DFS递归枚举组合，对每个组合判断和是否为素数。

```cpp
#include <iostream>
using namespace std;
int n, k, a[21], ans;

bool isPrime(int x) {
    if (x < 2) return false;
    for (int i = 2; i * i <= x; i++)
        if (x % i == 0) return false;
    return true;
}

void dfs(int idx, int cnt, int sum) {
    if (cnt == k) {
        if (isPrime(sum)) ans++;
        return;
    }
    if (idx >= n) return;
    dfs(idx + 1, cnt + 1, sum + a[idx]); // 选
    dfs(idx + 1, cnt, sum);               // 不选
}

int main() {
    cin >> n >> k;
    for (int i = 0; i < n; i++) cin >> a[i];
    dfs(0, 0, 0);
    cout << ans << endl;
    return 0;
}
```

## --------------------------------------------------------------------------
### P1044 [NOIP2003 普及组] 栈
## --------------------------------------------------------------------------

题目: 一个操作数序列1,2,⋯,n，通过一个栈进行push和pop操作。问可能的输出序列总数。

思路: 这就是卡特兰数问题。递推公式: f[0]=1, f[n] = Σ_{i=0}^{n-1} f[i] * f[n-1-i]。

```cpp
#include <iostream>
using namespace std;
int f[20];

int main() {
    int n;
    cin >> n;
    f[0] = f[1] = 1;
    for (int i = 2; i <= n; i++)
        for (int j = 0; j < i; j++)
            f[i] += f[j] * f[i - 1 - j];
    cout << f[n] << endl;
    return 0;
}
```

## --------------------------------------------------------------------------
### P1164 小A点菜
## --------------------------------------------------------------------------

题目: 小A有M元钱，N种菜每种有且只有一份，价格分别为a_i。问刚好花完M元的点菜方案数。

思路: 01背包方案数问题。dp[j]表示花费j元的方案数，dp[0]=1，转移: dp[j] += dp[j-a[i]]。

```cpp
#include <iostream>
using namespace std;
int dp[10001];

int main() {
    int n, m, a;
    cin >> n >> m;
    dp[0] = 1;
    for (int i = 0; i < n; i++) {
        cin >> a;
        for (int j = m; j >= a; j--)
            dp[j] += dp[j - a];
    }
    cout << dp[m] << endl;
    return 0;
}
```

## ==========================================================================
### 📝 章节测试
## ==========================================================================

#### 判断题

> [!question] 判断题 1
> 递推是从已知条件逐步推导出未知结果。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 递推从基础情况出发，利用递推公式逐步计算到目标值。

> [!question] 判断题 2
> 递归函数必须有基准情况（终止条件）。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 没有终止条件的递归会无限调用导致栈溢出。

> [!question] 判断题 3
> 记忆化搜索可以避免递归中的重复计算。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 将已计算的结果存储起来，下次直接查表而非重新计算。

> [!question] 判断题 4
> 卡特兰数 f[n] = C(2n,n)/(n+1)。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 卡特兰数的通项公式为C(2n,n)/(n+1)，递推公式为f[n]=Σf[i]*f[n-1-i]。

> [!question] 判断题 5
> 01背包问题中，每个物品只能选择一次。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 01背包中每件物品只有选或不选两种状态。

> [!question] 判断题 6
> 递归比递推的空间效率通常更高。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > 
> > **解析**: 递归需要栈空间存储每层调用，通常比递推（只用数组）占用更多空间。

> [!question] 判断题 7
> P1028数的计算中，f[n]包含数列只有n本身的情况。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: f[n]包含所有以n开头的数列数量，其中包含只有n一个数的情况（f[n]末尾+1）。

> [!question] 判断题 8
> DFS递归枚举组合时，"选或不选"是标准的分支方式。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 对每个元素做选/不选的二叉决策，是枚举组合的经典DFS方式。

> [!question] 判断题 9
> 01背包的状态转移从大到小遍历容量是为了保证每个物品只选一次。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 逆序遍历保证dp[j-a[i]]还是未更新的（不含当前物品的）值。

> [!question] 判断题 10
> 斐波那契数列是递推的经典例子。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: f[n]=f[n-1]+f[n-2]是最典型的递推公式。

#### 选择题

> [!question] 选择题 1
> 卡特兰数f[4]的值是？
> - [ ] A. 4
> - [ ] B. 8
> - [ ] C. 14
> - [ ] D. 42
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: f[4]=f[0]*f[3]+f[1]*f[2]+f[2]*f[1]+f[3]*f[0]=1*5+1*2+2*1+5*1=14。

> [!question] 选择题 2
> 递归和递推的关系是？
> - [ ] A. 完全相同
> - [ ] B. 递推是自底向上，递归是自顶向下
> - [ ] C. 互不相关
> - [ ] D. 递归更高效
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 递推从小到大计算，递归从目标向下分解再回溯。

> [!question] 选择题 3
> 01背包转移方程dp[j] += dp[j-a[i]]计算的是？
> - [ ] A. 最大价值
> - [ ] B. 方案数
> - [ ] C. 最小花费
> - [ ] D. 物品数量
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: dp[j]表示花费恰好为j的方案数，+=是累加方案。

> [!question] 选择题 4
> P1036选数问题的时间复杂度是？
> - [ ] A. O(n)
> - [ ] B. O(n²)
> - [ ] C. O(C(n,k))
> - [ ] D. O(2^n)
>
> > [!success]- 点击查看答案
> > 正确答案: D
> > 
> > **解析**: DFS枚举每个元素选或不选，最坏情况遍历2^n种状态。

> [!question] 选择题 5
> 递推公式f[n]=f[n-1]+f[n-2]需要几个初始值？
> - [ ] A. 0个
> - [ ] B. 1个
> - [ ] C. 2个
> - [ ] D. 3个
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: 需要f[0]和f[1]两个初始值才能开始递推。

> [!question] 选择题 6
> 下列哪个问题不适合用递推解决？
> - [ ] A. 斐波那契
> - [ ] B. 数楼梯
> - [ ] C. 最短路径
> - [ ] D. 组合数
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: 最短路径通常用图算法(Dijkstra/BFS)，而非简单递推。

> [!question] 选择题 7
> 记忆化搜索与递推DP的区别是？
> - [ ] A. 结果不同
> - [ ] B. 记忆化只计算需要的状态
> - [ ] C. 递推更准确
> - [ ] D. 没有区别
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 记忆化搜索按需计算（懒惰求值），递推计算所有状态（有时包含无用状态）。

> [!question] 选择题 8
> P1164小A点菜中，dp[0]=1的含义是？
> - [ ] A. 没有菜可选
> - [ ] B. 花费0元有1种方案（什么都不点）
> - [ ] C. 初始花费为1
> - [ ] D. 只选一道菜
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: dp[0]=1表示花费恰好为0元有唯一方案：不点任何菜。

> [!question] 选择题 9
> 栈的输出序列数（卡特兰数）当n=3时为？
> - [ ] A. 3
> - [ ] B. 4
> - [ ] C. 5
> - [ ] D. 6
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: f[3]=f[0]*f[2]+f[1]*f[1]+f[2]*f[0]=1*2+1*1+2*1=5。

> [!question] 选择题 10
> 递推数组通常需要初始化的是？
> - [ ] A. 所有元素
> - [ ] B. 只有第一个
> - [ ] C. 边界条件对应的元素
> - [ ] D. 不需要初始化
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: 递推需要初始化边界条件（如f[0]、f[1]等），其他元素通过递推公式计算。

#### 动手练习题

> [!question] 动手练习 1
> **题目**: 用递推计算斐波那契数列的第n项（f[1]=f[2]=1）。
> 
> **示例**: 输入 `10`，输出 `55`
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
using namespace std;
int main() {
    int n;
    cin >> n;
    long long f[105];
    f[1] = f[2] = 1;
    for (int i = 3; i <= n; i++) f[i] = f[i-1] + f[i-2];
    cout << f[n] << endl;
    return 0;
}
> > ```

> [!question] 动手练习 2
> **题目**: 用DFS递归枚举从n个数中选k个数的所有组合，输出每个组合。
> 
> **示例**: n=4, k=2，输出所有C(4,2)=6种组合
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
using namespace std;
int n, k, a[25];
void dfs(int idx, int cnt) {
    if (cnt == k) {
        for (int i = 0; i < k; i++) cout << a[i] << " ";
        cout << endl;
        return;
    }
    if (idx > n) return;
    a[cnt] = idx;
    dfs(idx + 1, cnt + 1);
    dfs(idx + 1, cnt);
}
int main() {
    cin >> n >> k;
    dfs(1, 0);
    return 0;
}
> > ```

> [!question] 动手练习 3
> **题目**: 实现01背包：n件物品各有重量w[i]和价值v[i]，背包容量为W，求最大价值。
> 
> **提示**: dp[j]表示容量为j时的最大价值
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
using namespace std;
int dp[10005];
int main() {
    int n, W;
    cin >> n >> W;
    for (int i = 0; i < n; i++) {
        int w, v;
        cin >> w >> v;
        for (int j = W; j >= w; j--)
            dp[j] = max(dp[j], dp[j-w] + v);
    }
    cout << dp[W] << endl;
    return 0;
}
> > ```

### 🔗 推荐练习题（洛谷）

- [P1255 数楼梯](https://www.luogu.com.cn/problem/P1255)
- [P1028 数的计算](https://www.luogu.com.cn/problem/P1028)


## --------------------------------------------------------------------------
### 相关技巧
## --------------------------------------------------------------------------

- **[[暴力枚举]]**: 递归枚举组合/排列
- **[[搜索]]**: DFS 本质是递归
- **[[贪心]]**: 递推常与贪心结合（如卡特兰数）
- **[[排序]]**: 记忆化搜索配合排序
