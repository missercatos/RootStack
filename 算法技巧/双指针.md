## ==========================================================================
C++ 算法技巧 — 双指针 (Two Pointers)
## ==========================================================================

双指针（又称尺取法）通过两个指针在数组上协同移动，将 O(n²) 暴力优化为 O(n)，
常用于有序数组的查找、区间统计等问题。

**前置知识**: [[数组]]、[[循环]]、[[排序]]

## --------------------------------------------------------------------------
### 同向双指针（快慢指针）
## --------------------------------------------------------------------------

两个指针同向移动，常用于去重、删除元素、链表操作等。

```cpp
// 示例：移除有序数组中重复元素，返回新长度
int removeDuplicates(int a[], int n) {
    int slow = 0;
    for (int fast = 1; fast < n; fast++)
        if (a[fast] != a[slow])
            a[++slow] = a[fast];
    return slow + 1;
}
```

## --------------------------------------------------------------------------
### 对撞双指针
## --------------------------------------------------------------------------

两个指针从两端向中间移动，常用于有序数组中查找两数之和。

```cpp
// 示例：在有序数组中找两数之和等于 target
bool twoSum(int a[], int n, int target) {
    int l = 0, r = n - 1;
    while (l < r) {
        int sum = a[l] + a[r];
        if (sum == target) return true;
        if (sum < target) l++;
        else r--;
    }
    return false;
}
```

## --------------------------------------------------------------------------
### P1102 A-B 数对
## --------------------------------------------------------------------------

题目: 给出 N 个正整数和一个数 C，求满足 A - B = C 的数对 (A,B) 个数。

思路: 排序后用对撞双指针或二分。转化为 B = A - C，对每个 A 统计 B 的出现次数。
详见 [[二分查找]] 中的同题解法。

```cpp
#include <iostream>
#include <algorithm>
using namespace std;
int a[200005];

int main() {
    int n, c;
    long long ans = 0;
    cin >> n >> c;
    for (int i = 0; i < n; i++) cin >> a[i];
    sort(a, a + n);
    for (int i = 0; i < n; i++) {
        int target = a[i] - c;
        ans += upper_bound(a, a + n, target) - lower_bound(a, a + n, target);
    }
    cout << ans << endl;
    return 0;
}
```

## --------------------------------------------------------------------------
### P1147 连续自然数和
## --------------------------------------------------------------------------

题目: 把一个自然数 M 表示为连续的若干自然数之和，输出所有方案。

思路: 右指针 r 向右移动，维护区间 [l, r] 的和。若和大于 M 则移动左指针 l；等于 M 则记录答案。
双指针左右端点均单调右移，O(n)。

```cpp
#include <iostream>
using namespace std;

int main() {
    int M;
    cin >> M;
    int l = 1, r = 1, sum = 1;
    while (l <= r && r <= M / 2 + 1) {
        if (sum == M) {
            cout << l << " " << r << endl;
            sum -= l++;
        } else if (sum < M) {
            sum += ++r;
        } else {
            sum -= l++;
        }
    }
    return 0;
}
```

## --------------------------------------------------------------------------
### P1638 逛画展
## --------------------------------------------------------------------------

题目: 博览会有 n 幅画排成一行，共有 m 位画家。求一个最短的连续区间，使得该区间内包含所有 m 位画家的作品。

思路: 滑动窗口（同向双指针）。右指针扩展，统计区间内画家数量；
当包含全部画家时，收缩左指针，记录最短区间。

```cpp
#include <iostream>
using namespace std;
int a[1000005], cnt[2005];

int main() {
    int n, m, kinds = 0;
    cin >> n >> m;
    for (int i = 1; i <= n; i++) cin >> a[i];
    int l = 1, minLen = n + 1, ansL = 1, ansR = n;
    for (int r = 1; r <= n; r++) {
        if (cnt[a[r]] == 0) kinds++;
        cnt[a[r]]++;
        while (kinds == m && l <= r) {
            int len = r - l + 1;
            if (len < minLen) {
                minLen = len;
                ansL = l;
                ansR = r;
            }
            cnt[a[l]]--;
            if (cnt[a[l]] == 0) kinds--;
            l++;
        }
    }
    cout << ansL << " " << ansR << endl;
    return 0;
}
```

## ==========================================================================
### 📝 章节测试
## ==========================================================================

#### 判断题

> [!question] 判断题 1
> 双指针可以将O(n²)暴力优化为O(n)。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 每个指针最多移动n次，总O(n)。

> [!question] 判断题 2
> 对撞双指针要求数组有序。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 利用有序性决定移动方向。

> [!question] 判断题 3
> 同向双指针中快指针始终在慢指针前面或同位置。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 快指针速度≥慢指针。

> [!question] 判断题 4
> A-B数对可转化为对每个A查找B=A-C的个数。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 用二分统计等于A-C的元素个数。

> [!question] 判断题 5
> 连续自然数和问题中左右端点都单调右移。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 保证O(n)时间。

> [!question] 判断题 6
> 逛画展问题中窗口内画家种类=m时可尝试收缩左指针。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 包含所有画家后尝试缩短区间。

> [!question] 判断题 7
> 双指针只能用于数组不能用于链表。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > 
> > **解析**: 广泛用于链表如检测环。

> [!question] 判断题 8
> upper_bound-lower_bound可得某值在有序数组中出现次数。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 两者差值即为个数。

> [!question] 判断题 9
> 同向双指针时间复杂度是O(n)而非O(n²)。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 左右指针各最多移动n次。

> [!question] 判断题 10
> 双指针需要满足单调性条件才能正确使用。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 依赖单调性确定移动方向。

#### 选择题

> [!question] 选择题 1
> 有序数组找两数之和=target，对撞双指针做法？
> - [ ] A. 两指针都从左端
> - [ ] B. 左从左端右从右端根据和调整
> - [ ] C. 随机移动
> - [ ] D. 固定一个移动另一个
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 和小于target则左移右，和大于target则右移左。

> [!question] 选择题 2
> 哪个不是双指针典型应用？
> - [ ] A. 有序数组两数之和
> - [ ] B. 最长不重复子串
> - [ ] C. 快排partition
> - [ ] D. 以上都是
>
> > [!success]- 点击查看答案
> > 正确答案: D
> > 
> > **解析**: 三个都是经典应用。

> [!question] 选择题 3
> 连续自然数和中sum>M时应？
> - [ ] A. 右指针右移
> - [ ] B. 左指针右移
> - [ ] C. 左指针左移
> - [ ] D. 结束循环
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 和太大缩小区间左移。

> [!question] 选择题 4
> 逛画展时间复杂度？
> - [ ] A. O(n²)
> - [ ] B. O(n log n)
> - [ ] C. O(n)
> - [ ] D. O(nm)
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: 左右指针各最多移动n次。

> [!question] 选择题 5
> cnt[a[r]]++后kinds++的条件？
> - [ ] A. cnt==0（增加前）
> - [ ] B. cnt==1
> - [ ] C. cnt>1
> - [ ] D. kinds<m
>
> > [!success]- 点击查看答案
> > 正确答案: A
> > 
> > **解析**: 从0变1说明新增一位画家。

> [!question] 选择题 6
> 去重中slow指针指向？
> - [ ] A. 最后一个重复元素
> - [ ] B. 最后一个不重复元素
> - [ ] C. 数组末尾
> - [ ] D. 数组开头
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 已去重部分的最后一个元素。

> [!question] 选择题 7
> 双指针与暴力的本质区别？
> - [ ] A. 利用单调性避免重复遍历
> - [ ] B. 使用更多内存
> - [ ] C. 代码更短
> - [ ] D. 只能处理有序
>
> > [!success]- 点击查看答案
> > 正确答案: A
> > 
> > **解析**: 每元素最多访问常数次。

> [!question] 选择题 8
> A-B数对中用long long ans因为？
> - [ ] A. A和B很大
> - [ ] B. 数对数量可能超int范围
> - [ ] C. C很大
> - [ ] D. 数组元素很大
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: n²级别可能超int。

> [!question] 选择题 9
> 哪种情况适合对撞双指针？
> - [ ] A. 无序数组找两数之和
> - [ ] B. 有序数组找两数之和
> - [ ] C. 统计连续子数组
> - [ ] D. 字符串匹配
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 需要有序数组。

> [!question] 选择题 10
> 同向双指针中r扩展的条件通常是？
> - [ ] A. 窗口不满足条件
> - [ ] B. 窗口满足条件
> - [ ] C. r<n（未遍历完）
> - [ ] D. l==r
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: 右指针通常无条件遍历整个数组。

#### 动手练习题

> [!question] 动手练习 1
> **题目**: 有序数组对撞双指针找所有两数之和=target的下标对。
> 
> **示例**: 1 2 3 4 5 6, target=7→(0,5)(1,4)(2,3)
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
using namespace std;
int main(){
    int n,t,a[100005];cin>>n>>t;
    for(int i=0;i<n;i++)cin>>a[i];
    int l=0,r=n-1;
    while(l<r){int s=a[l]+a[r];if(s==t){cout<<"("<<l<<","<<r<<")"<<endl;l++;r--;}else if(s<t)l++;else r--;}
    return 0;
}
> > ```

> [!question] 动手练习 2
> **题目**: 正整数M输出所有连续自然数和=M的方案（至少两个数）。
> 
> **示例**: M=15→1 5, 4 6, 7 8
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
using namespace std;
int main(){
    int M;cin>>M;
    int l=1,r=1,sum=1;
    while(l<=M/2){
        if(sum==M&&l<r){cout<<l<<" "<<r<<endl;sum-=l++;}
        else if(sum<M) sum+=++r;
        else sum-=l++;
    }
    return 0;
}
> > ```

> [!question] 动手练习 3
> **题目**: 用双指针判断字符串是否为回文（忽略大小写和非字母字符）。
> 
> **示例**: A man a plan a canal Panama→Yes
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
#include <string>
#include <cctype>
using namespace std;
int main(){
    string s;getline(cin,s);
    int l=0,r=s.size()-1;
    while(l<r){
        while(l<r&&!isalnum(s[l]))l++;
        while(l<r&&!isalnum(s[r]))r--;
        if(tolower(s[l])!=tolower(s[r])){cout<<"No";return 0;}
        l++;r--;
    }
    cout<<"Yes";return 0;
}
> > ```

### 🔗 推荐练习题（洛谷）

- [P1102 A-B数对](https://www.luogu.com.cn/problem/P1102)
- [P1381 单词背诵](https://www.luogu.com.cn/problem/P1381)


## --------------------------------------------------------------------------
### 相关技巧
## --------------------------------------------------------------------------

- **[[滑动窗口]]**: 双指针的特化，定长或不定长窗口问题
- **[[二分查找]]**: 有序数组中另一种 O(log n) 查找方式
- **[[排序]]**: 双指针通常需要数组有序
- **[[前缀和]]**: 可配合双指针快速求区间和
