## ==========================================================================
C++ 算法技巧 — 滑动窗口 (Sliding Window)
## ==========================================================================

滑动窗口是双指针的特化形式，维护一个动态区间（窗口），在数组上平滑移动，
常用于解决满足某种性质的连续区间问题。

**前置知识**: [[数组]]、[[循环]]、[[双指针]]、[[前缀和]]

## --------------------------------------------------------------------------
### 定长滑动窗口
## --------------------------------------------------------------------------

窗口长度固定为 k，每次右移一格，只需要更新一个进入元素和一个离开元素。

```cpp
// 求长度为 k 的连续子数组的最大和
#include <iostream>
using namespace std;
int a[100005];

int main() {
    int n, k;
    cin >> n >> k;
    for (int i = 0; i < n; i++) cin >> a[i];
    int sum = 0, ans = -1e9;
    for (int i = 0; i < n; i++) {
        sum += a[i];
        if (i >= k) sum -= a[i - k];
        if (i >= k - 1) ans = max(ans, sum);
    }
    cout << ans << endl;
    return 0;
}
```

## --------------------------------------------------------------------------
### 不定长滑动窗口
## --------------------------------------------------------------------------

窗口两端均单调移动，常用于"满足条件的最短/最长区间"问题。

核心框架：
```cpp
int l = 0;
for (int r = 0; r < n; r++) {
    加入 a[r] 到窗口;
    while (窗口不满足条件) {
        移除 a[l];
        l++;
    }
    更新答案;
}
```

## --------------------------------------------------------------------------
### P1886 滑动窗口 / 单调队列
## --------------------------------------------------------------------------

题目: 有一个长为 n 的序列 a，以及一个大小为 k 的窗口。窗口从最左端滑到最右端，
每次输出窗口内的最小值和最大值。

思路: 用单调队列（deque）维护窗口最值，均摊 O(n)。这是滑动窗口+[[队列]]的经典结合。

```cpp
#include <iostream>
#include <deque>
using namespace std;
int a[1000005];
deque<int> q; // 存下标，保证队头是最值

int main() {
    int n, k;
    cin >> n >> k;
    for (int i = 1; i <= n; i++) cin >> a[i];

    // 最小值：维护递增队列，队头最小
    for (int i = 1; i <= n; i++) {
        while (!q.empty() && a[q.back()] >= a[i])
            q.pop_back();
        q.push_back(i);
        if (q.front() <= i - k) q.pop_front();
        if (i >= k) cout << a[q.front()] << " ";
    }
    cout << endl;
    q.clear();

    // 最大值：维护递减队列，队头最大
    for (int i = 1; i <= n; i++) {
        while (!q.empty() && a[q.back()] <= a[i])
            q.pop_back();
        q.push_back(i);
        if (q.front() <= i - k) q.pop_front();
        if (i >= k) cout << a[q.front()] << " ";
    }
    return 0;
}
```

## --------------------------------------------------------------------------
### P2032 扫描
## --------------------------------------------------------------------------

题目: 有一个 n 个数的数列，对于每个长度为 k 的连续区间，求出区间最大值。

思路: 同 P1886，单调队列维护滑动窗口最大值。

```cpp
#include <iostream>
#include <deque>
using namespace std;
int a[2000005];
deque<int> dq;

int main() {
    int n, k;
    cin >> n >> k;
    for (int i = 1; i <= n; i++) cin >> a[i];
    for (int i = 1; i <= n; i++) {
        while (!dq.empty() && a[dq.back()] <= a[i])
            dq.pop_back();
        dq.push_back(i);
        if (dq.front() <= i - k) dq.pop_front();
        if (i >= k) cout << a[dq.front()] << endl;
    }
    return 0;
}
```

## --------------------------------------------------------------------------
### P1638 逛画展（再探）
## --------------------------------------------------------------------------

题目: 求包含所有 m 位画家的最短连续区间。

思路: 不定长滑动窗口。右指针扩展纳入画家，当窗口内画家种类 = m 时收缩左指针。
详见 [[双指针]] 同题解法。

## ==========================================================================
### 📝 章节测试
## ==========================================================================

#### 判断题

> [!question] 判断题 1
> 滑动窗口是双指针的特化形式。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 本质是同向双指针维护的连续区间。

> [!question] 判断题 2
> 单调队列可以在O(1)时间获取窗口最值。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 队头始终是最值O(1)获取。

> [!question] 判断题 3
> 定长滑动窗口每次移动只需更新一个进入和一个离开的元素。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 增量更新即可。

> [!question] 判断题 4
> 单调递增队列的队头是最小值。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 从头到尾递增，队头最小。

> [!question] 判断题 5
> 不定长滑动窗口可以解决最短区间问题。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 右扩展满足条件后左收缩找最短。

> [!question] 判断题 6
> 单调队列中存储的是元素值而非下标。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 错误
> > 
> > **解析**: 通常存下标以判断是否滑出窗口。

> [!question] 判断题 7
> P1886滑动窗口时间复杂度O(n)。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 每元素最多入队出队各一次。

> [!question] 判断题 8
> 滑动窗口左端只能向右移动。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 两端单调右移保证O(n)。

> [!question] 判断题 9
> 判断队头过期用q.front()<=i-k。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: 队头下标已不在窗口内需弹出。

> [!question] 判断题 10
> 定长窗口和可用前缀和O(1)计算。 （ ）
> - [ ] ✅ 正确
> - [ ] ❌ 错误
>
> > [!success]- 点击查看答案
> > 答案: 正确
> > 
> > **解析**: pre[i]-pre[i-k]。

#### 选择题

> [!question] 选择题 1
> 维护窗口最大值应使用？
> - [ ] A. 递增单调队列
> - [ ] B. 递减单调队列
> - [ ] C. 普通队列
> - [ ] D. 栈
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 递减队列队头最大。

> [!question] 选择题 2
> if(i>=k-1)的含义是？
> - [ ] A. 数组遍历完
> - [ ] B. 窗口已完全形成（至少k个元素）
> - [ ] C. 到达中间
> - [ ] D. 溢出
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 窗口内恰好有k个元素。

> [!question] 选择题 3
> 不定长窗口框架中while(窗口不满足)的作用？
> - [ ] A. 扩展窗口
> - [ ] B. 收缩直到满足条件
> - [ ] C. 结束循环
> - [ ] D. 更新答案
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 移除左端直到重新满足。

> [!question] 选择题 4
> while(!q.empty()&&a[q.back()]>=a[i])q.pop_back()维护？
> - [ ] A. 递减队列求最大
> - [ ] B. 递增队列求最小
> - [ ] C. 先进先出
> - [ ] D. 优先队列
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 弹出≥当前值保持递增。

> [!question] 选择题 5
> P2032与P1886的区别？
> - [ ] A. P2032只求最大值
> - [ ] B. P2032数据更大
> - [ ] C. P2032是二维
> - [ ] D. A和B都对
>
> > [!success]- 点击查看答案
> > 正确答案: D
> > 
> > **解析**: 只需最大值且数据范围可能更大。

> [!question] 选择题 6
> 逛画展中什么时候收缩左指针？
> - [ ] A. 画家不足m位
> - [ ] B. 已包含所有m位画家
> - [ ] C. 右指针到末尾
> - [ ] D. l==r
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 包含全部画家时尝试缩短。

> [!question] 选择题 7
> deque支持的操作？
> - [ ] A. 只能从前端进出
> - [ ] B. 只能从后端进出
> - [ ] C. 两端都可以进出
> - [ ] D. 只能中间访问
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: push_front/pop_front和push_back/pop_back。

> [!question] 选择题 8
> 滑动窗口与前缀和的关系？
> - [ ] A. 完全替代
> - [ ] B. 定长窗口和可用前缀和代替
> - [ ] C. 没有关系
> - [ ] D. 前缀和是特例
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 求和时都O(1)，求最值需单调队列。

> [!question] 选择题 9
> 哪个问题不适合滑动窗口？
> - [ ] A. 定长子数组最大和
> - [ ] B. 最短包含所有字母子串
> - [ ] C. 数组排序
> - [ ] D. 滑动窗口最大值
>
> > [!success]- 点击查看答案
> > 正确答案: C
> > 
> > **解析**: 排序不涉及连续子区间。

> [!question] 选择题 10
> 单调队列空间复杂度？
> - [ ] A. O(1)
> - [ ] B. O(k)窗口大小
> - [ ] C. O(n)
> - [ ] D. O(n²)
>
> > [!success]- 点击查看答案
> > 正确答案: B
> > 
> > **解析**: 最多存k个元素。

#### 动手练习题

> [!question] 动手练习 1
> **题目**: 给定数组和窗口大小k，输出每个窗口的最小值。
> 
> **示例**: 1 3 -1 -3 5 3 6 7, k=3→-1 -3 -3 -3 3 3
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
#include <deque>
using namespace std;
int a[1000005];
int main(){
    int n,k;cin>>n>>k;
    for(int i=1;i<=n;i++)cin>>a[i];
    deque<int> q;
    for(int i=1;i<=n;i++){
        while(!q.empty()&&a[q.back()]>=a[i])q.pop_back();
        q.push_back(i);
        if(q.front()<=i-k)q.pop_front();
        if(i>=k)cout<<a[q.front()]<<" ";
    }
    return 0;
}
> > ```

> [!question] 动手练习 2
> **题目**: 正整数数组找和≥S的最短连续子数组长度，不存在输出0。
> 
> **示例**: 2 3 1 2 4 3, S=7→2
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
using namespace std;
int main(){
    int n,S,a[100005];cin>>n>>S;
    for(int i=0;i<n;i++)cin>>a[i];
    int l=0,sum=0,ans=n+1;
    for(int r=0;r<n;r++){sum+=a[r];while(sum>=S){ans=min(ans,r-l+1);sum-=a[l++];}}
    cout<<(ans>n?0:ans);
    return 0;
}
> > ```

> [!question] 动手练习 3
> **题目**: 给定字符串找最长不含重复字符的子串长度。
> 
> **示例**: abcabcbb→3
> 
> > [!success]- 点击查看参考答案
> > ```cpp
> > #include <iostream>
#include <string>
using namespace std;
int main(){
    string s;cin>>s;
    int cnt[128]={},l=0,ans=0;
    for(int r=0;r<(int)s.size();r++){
        cnt[(int)s[r]]++;
        while(cnt[(int)s[r]]>1){cnt[(int)s[l]]--;l++;}
        ans=max(ans,r-l+1);
    }
    cout<<ans;return 0;
}
> > ```

### 🔗 推荐练习题（洛谷）

- [P1886 滑动窗口/单调队列](https://www.luogu.com.cn/problem/P1886)


## --------------------------------------------------------------------------
### 相关技巧
## --------------------------------------------------------------------------

- **[[双指针]]**: 滑动窗口是双指针的特化，区别在于窗口连续
- **[[前缀和]]**: 定长窗口和可直接用前缀和 O(1) 求
- **[[差分]]**: 窗口移动时可用差分维护区间操作
- **[[队列]]**: 单调队列是滑动窗口最值的利器
- **[[贪心]]**: 部分滑动窗口问题需要贪心决定窗口扩展/收缩策略
