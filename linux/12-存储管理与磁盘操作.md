# 07 - 存储管理进阶

---

## 7.1 分区工具

### fdisk（MBR/GPT）

```bash
sudo fdisk -l # 列出所有磁盘
sudo fdisk /dev/sda # 交互式分区

# fdisk 交互命令
# m 帮助
# p 打印分区表
# g 创建 GPT 分区表
# n 创建新分区
# d 删除分区
# t 更改分区类型
# w 写入并退出
# q 不保存退出
```

### parted（GPT 推荐）

```bash
sudo parted /dev/sda print
sudo parted /dev/sda mklabel gpt
sudo parted /dev/sda mkpart primary ext4 1MiB 100%
sudo parted /dev/sda set 1 boot on
```

---

## 7.2 创建文件系统

```bash
# ext4 — 最通用
sudo mkfs.ext4 /dev/sda1

# btrfs — CoW、快照、子卷
sudo mkfs.btrfs /dev/sda1

# FAT32 — EFI 分区
sudo mkfs.fat -F32 /dev/sda1

# swap
sudo mkswap /dev/sda2
sudo swapon /dev/sda2
```

### 文件系统检查与修复

```bash
sudo fsck /dev/sda1 # 检查（必须先卸载）
sudo fsck -y /dev/sda1 # 自动修复
sudo fsck.ext4 -f /dev/sda1 # 强制检查
```

---

## 7.3 挂载管理

### 临时挂载

```bash
mount /dev/sda1 /mnt
umount /mnt
umount -l /mnt # 延迟卸载（繁忙时）
mount -t ext4 /dev/sda1 /mnt
mount -o remount,rw / # 重新挂载为读写
```

### 查看

```bash
mount # 当前挂载
df -h # 磁盘使用
df -i # inode 使用
lsblk -f # 树状展示 + 文件系统类型
blkid # UUID 信息
findmnt # 挂载树
```

### 永久挂载 — /etc/fstab

```bash
# 格式：设备 挂载点 文件系统 选项 dump fsck
# <device> <mount> <fstype> <options> <dump> <pass>
```

```bash
# /etc/fstab 示例
# 设备 挂载点 类型 选项 dump pass
UUID=abc123-def456 / ext4 defaults 0 1
UUID=7890-ABCD /boot vfat defaults 0 2
UUID=def789-ghi012 /home ext4 defaults,noatime 0 2
UUID=ghi345-jkl678 none swap defaults 0 0

# 常用选项
# defaults = rw,suid,dev,exec,auto,nouser,async
# noatime 不记录访问时间（SSD 推荐）
# discard SSD TRIM 支持
# autodefrag btrfs 自动碎片整理
# compress=zstd btrfs 透明压缩
```

### 测试 fstab

```bash
sudo mount -a # 挂载 fstab 所有（测试配置）
```

---

## 7.4 LVM（逻辑卷管理）

### 概念

```
Physical Volume (PV) → Volume Group (VG) → Logical Volume (LV)
 物理卷 卷组 逻辑卷
 /dev/sda1 ──┐
 /dev/sdb1 ──┼── VG_name ──┬── lv_root (/)
 │ ├── lv_home (/home)
 │ └── lv_swap (swap)
```

### 创建 LVM

```bash
# 1. 创建 PV
sudo pvcreate /dev/sda2 /dev/sdb1
sudo pvs # 查看物理卷

# 2. 创建 VG
sudo vgcreate vg0 /dev/sda2 /dev/sdb1
sudo vgs # 查看卷组

# 3. 创建 LV
sudo lvcreate -L 30G -n lv_root vg0 # 30G 固定大小
sudo lvcreate -L 8G -n lv_swap vg0
sudo lvcreate -l 100%FREE -n lv_home vg0 # 剩余全部空间
sudo lvs # 查看逻辑卷

# 4. 格式化并挂载
sudo mkfs.ext4 /dev/vg0/lv_root
sudo mount /dev/vg0/lv_root /mnt
```

### 动态扩展

```bash
# 扩展 LV
sudo lvextend -L +5G /dev/vg0/lv_root
sudo lvextend -l +100%FREE /dev/vg0/lv_root
sudo resize2fs /dev/vg0/lv_root # 扩展文件系统（ext4）

# 添加新磁盘到 VG
sudo pvcreate /dev/sdc1
sudo vgextend vg0 /dev/sdc1

# 缩减 LV（需先缩减文件系统，有风险）
sudo umount /dev/vg0/lv_home
sudo e2fsck -f /dev/vg0/lv_home
sudo resize2fs /dev/vg0/lv_home 20G
sudo lvreduce -L 20G /dev/vg0/lv_home
sudo mount /dev/vg0/lv_home /home
```

---

## 7.5 磁盘配额

```bash
# 安装
sudo pacman -S quota-tools

# 在 /etc/fstab 中启用
# UUID=xxx /home ext4 defaults,usrquota,grpquota 0 2

# 重新挂载
sudo mount -o remount /home

# 创建配额数据库
sudo quotacheck -cug /home

# 开启配额
sudo quotaon /home

# 设置配额
sudo edquota -u alice
# 编辑：软限制、硬限制、inode 限制

# 查看配额
sudo quota alice
sudo repquota /home
```

---

## 7.6 知识点概要

1. fdisk 和 parted 区别？ → fdisk 适合 MBR；parted 支持 GPT 和更大磁盘
2. mount 后重启失效怎么办？ → 写到 `/etc/fstab`
3. LVM 有什么优势？ → 在线扩容、跨磁盘组合、快照
4. 怎么查看分区 UUID？ → `blkid` 或 `lsblk -f`
5. ext4 和 btrfs 核心区别？ → btrfs 支持 CoW、快照、子卷、压缩；ext4 更稳定成熟

---

## 7.7 自测练习

> [!example] 自测练习

> [!question]- 选择题 1：LVM 中正确的层级关系是？
> - A. LV → VG → PV
> - B. PV → LV → VG
> - C. PV → VG → LV
> - D. VG → PV → LV
>
> > [!success]- 点击查看答案
> > **C**
> > 物理卷 (PV) 组成卷组 (VG)，卷组上划分逻辑卷 (LV)

> [!question]- 选择题 2：`/etc/fstab` 中 `pass` 字段值为 0 表示？
> - A. 最先检查
> - B. 启动时不进行 fsck 检查
> - C. 只读挂载
> - D. 延迟挂载
>
> > [!success]- 点击查看答案
> > **B**
> > pass=0 表示不检查，pass=1 用于根分区（优先），pass=2 用于其他分区

> [!question]- 判断题 3：`mount -a` 会挂载 `/etc/fstab` 中所有未挂载的文件系统
> - A. 正确
> - B. 错误
>
> > [!success]- 点击查看答案
> > **A. 正确**
> > `mount -a` 读取 fstab 并挂载所有标记为 auto 的未挂载条目，常用于验证 fstab 配置

> [!question]- 选择题 4：扩展 ext4 文件系统到 LV 的新大小应使用？
> - A. `mkfs.ext4`
> - B. `resize2fs`
> - C. `fsck`
> - D. `tune2fs`
>
> > [!success]- 点击查看答案
> > **B**
> > `lvextend` 扩展逻辑卷后，需要 `resize2fs` 将 ext4 文件系统扩展到新的大小

> [!question]- 选择题 5：查看磁盘分区的 UUID 应使用哪个命令？
> - A. `fdisk -l`
> - B. `blkid`
> - C. `df -h`
> - D. `mount`
>
> > [!success]- 点击查看答案
> > **B**
> > `blkid` 显示块设备的 UUID、文件系统类型等属性信息

> [!question]- 判断题 6：`fsck` 检查文件系统时，目标分区必须先卸载
> - A. 正确
> - B. 错误
>
> > [!success]- 点击查看答案
> > **A. 正确**
> > 在挂载状态下运行 fsck 可能损坏文件系统，必须先 umount

> [!question]- 选择题 7：创建 swap 分区的正确步骤是？
> - A. `mkfs.swap` → `swapon`
> - B. `mkswap` → `swapon`
> - C. `swapon` → `mkswap`
> - D. `fdisk` → `swapon`
>
> > [!success]- 点击查看答案
> > **B**
> > 先用 `mkswap` 格式化为交换分区，再用 `swapon` 启用

> [!question]- 判断题 8：btrfs 支持透明压缩和写时复制（CoW），而 ext4 不支持
> - A. 正确
> - B. 错误
>
> > [!success]- 点击查看答案
> > **A. 正确**
> > btrfs 的核心特性包括 CoW、快照、子卷、透明压缩，ext4 是传统日志文件系统

> [!question]- 选择题 9：向已有卷组 vg0 添加新物理卷的命令是？
> - A. `vgcreate vg0 /dev/sdc1`
> - B. `vgextend vg0 /dev/sdc1`
> - C. `pvcreate vg0 /dev/sdc1`
> - D. `lvextend vg0 /dev/sdc1`
>
> > [!success]- 点击查看答案
> > **B**
> > `vgextend` 将新的物理卷加入已有的卷组，`vgcreate` 是创建新卷组

> [!question]- 选择题 10：fstab 中 `noatime` 选项的作用是？
> - A. 禁止写入
> - B. 不记录文件访问时间
> - C. 不自动挂载
> - D. 禁用 TRIM
>
> > [!success]- 点击查看答案
> > **B**
> > `noatime` 不更新访问时间戳，可减少写入次数，对 SSD 寿命有益
