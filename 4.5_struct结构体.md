# 第4.5章: struct 结构体 📚
## ==========================================================================

## 📋 章节概述

`struct` 是 C 语言遗留给 C++ 的数据聚合工具。在 C++ 中，struct 与 class 仅有**默认访问级别**不同（struct 默认 public，class 默认 private），其余特性完全等价—— struct 同样支持成员函数、构造/析构、继承和多态。

struct 的核心使用场景是**纯数据聚合**（POD 类型），尤其在数据结构与算法中：链表节点、树节点、图边、哈希表桶等均以 struct 承载。理解 struct 的内存布局也为后续 [[05_面向对象(一)类与对象基础]] 的对象模型打下基础。

> 本章大量内容与后续章节交叉。建议学完本章后立即进入 [[05_面向对象(一)类与对象基础]]，将 struct 知识自然过渡到 class。C 语言背景知识见 [[C兼容/C_01_C语言速览]]。

## ==========================================================================
### 📖 第一节: 基础示例代码 + 计算机底层原理
## ==========================================================================

4.5.1 struct 基本定义与使用
---------------------------------

```cpp
#include <iostream>
#include <cstring>

// C 风格 struct —— 纯数据聚合
struct Point {
    int x;
    int y;
};

// C++ 风格 struct —— 带成员函数
struct Student {
    char  name[32];
    int   age;
    double score;

    void print() const {
        std::cout << name << " (" << age << ") : " << score << std::endl;
    }
};

int main() {
    // 聚合初始化（C++11 起可省略 =）
    Point p1 = {10, 20};
    Point p2{30, 40};            // C++11 统一初始化

    Student s1 = {"Alice", 20, 95.5};
    Student s2{"Bob", 22, 88.0};

    p1.x = 100;                  // 直接访问成员
    s1.print();                  // 调用成员函数
    s2.print();

    // struct 数组
    Point triangle[3] = {{0,0}, {10,0}, {5,10}};
    for (auto& pt : triangle)
        std::cout << "(" << pt.x << "," << pt.y << ") ";

    return 0;
}
```

【原理4.5-1】struct 内存布局与对齐
------------------------------------

struct 成员按声明顺序排列在内存中，其对齐规则与 class 完全一致。详见 [[05_面向对象(一)类与对象基础#综合示例|对象内存布局]]。

```cpp
struct Layout {
    char  a;    // 1B, offset 0
    int   b;    // 4B, offset 4 (offset 1 不整除 4 → pad 3)
    short c;    // 2B, offset 8
    double d;   // 8B, offset 16 (offset 10 不整除 8 → pad 6)
};
// sizeof(Layout) = 24
```

> 成员排列顺序影响 sizeof 大小。将大类型放前面可减少填充，详见 [[05_面向对象(一)类与对象基础#减少内存浪费|减少内存浪费]]。

【原理4.5-2】struct 与 class 的底层等价性
------------------------------------------

编译器对 struct 和 class 生成的汇编代码完全一致。两者仅在**语法层面**有区别：

| | struct | class |
|:--|:-------|:------|
| 默认成员访问 | public | private |
| 默认继承访问 | public | private |

以下两份代码生成完全相同的机器码：

```cpp
struct S { int x; void f() { x = 1; } };   // 默认 public
class C { public: int x; void f() { x = 1; } }; // 显式 public
// sizeof(S) == sizeof(C)，f() 的汇编相同
```

> 约定：**纯数据聚合用 struct，有封装逻辑用 class**。STL 中 `std::pair`、`std::is_same` 等均以 struct 实现。

## ==========================================================================
### 📖 第二节: 所有用法大全（标准库/关键字/函数）
## ==========================================================================

2.1 聚合初始化（Aggregate Initialization）
--------------------------------------------

```cpp
struct Vec3 { float x, y, z; };

// C++98: 等号初始化
Vec3 v1 = {1.0f, 2.0f, 3.0f};

// C++11: 统一初始化（推荐）
Vec3 v2{1.0f, 2.0f, 3.0f};
Vec3 v3{};   // 零初始化 → {0,0,0}

// C++20: 指定初始化器
Vec3 v4{.x = 1.0f, .z = 3.0f};  // y 自动为 0
```

2.2 struct 嵌套与匿名 struct
-------------------------------

```cpp
struct Rectangle {
    struct { int x, y; } topLeft;     // 匿名嵌套 struct
    struct { int x, y; } bottomRight;
};

Rectangle r = {{0, 0}, {100, 200}};
r.topLeft.x = 10;
```

2.3 C 遗留：`typedef` 与 struct
--------------------------------

C 语言中需要 `typedef` 才能省略 `struct` 关键字：

```cpp
// C 风格（C++ 中仍合法但不必用）
typedef struct { int x, y; } Point_C;
Point_C p1 = {1, 2};   // 无需写 struct

// C++ 风格（推荐）
struct Point { int x, y; };
Point p2{3, 4};         // C++ 直接省略 struct 关键字
```

关于 `typedef` 的完整用法（含函数指针别名等），参见 [[基础教程/05_变量与数据类型]] 和 [[C兼容/C_03_struct_typedef_宏]]。

2.4 C 遗留：`#define` 与 struct
--------------------------------

旧式 C 代码用 `#define` 定义结构体相关常量。预处理宏的完整机制见 [[02_预处理器]] 和 [[C兼容/C_03_struct_typedef_宏]]。

```cpp
#define MAX_NAME_LEN 32

struct User {
    char name[MAX_NAME_LEN];   // 宏常量
    int  id;
};
```

2.5 POD 类型与 `std::is_pod`
-------------------------------

POD（Plain Old Data）是可以与 C 语言二进制兼容的类型。C++20 起 `std::is_pod` 被弃用，改用更精确的 trivial + standard-layout 判断：

```cpp
#include <type_traits>

struct PlainData {
    int x;
    double y;
};

// PlainData 是 trivial 且 standard-layout → 与 C 兼容
// 可以用 memcpy 安全复制
PlainData src{1, 2.0}, dst;
std::memcpy(&dst, &src, sizeof(PlainData));  // 安全
```

2.6 struct 与数据结构（重点）
-------------------------------

struct 是构建所有数据结构的基石。以下为各数据结构的典型 struct 节点定义：

**链表节点** → 详见 [[数据结构/D_链表_LinkedList]]
```cpp
struct ListNode {
    int       value;
    ListNode* next;     // 自引用指针
};
```

**二叉树节点** → 详见 [[数据结构/I_树_Tree_BST_AVL]]
```cpp
struct TreeNode {
    int       value;
    TreeNode* left;
    TreeNode* right;
};
```

**哈希表桶** → 详见 [[数据结构/G_哈希表_HashTable]]
```cpp
struct HashNode {
    int       key;
    int       value;
    HashNode* next;     // 链地址法
};
```

**栈/队列元素** → 详见 [[数据结构/B_栈_Stack]] 和 [[数据结构/F_队列_Queue]]

**图的边与邻接表节点** → 详见 [[数据结构/H_图_Graph]]

**并查集元素** → 详见 [[数据结构/K_并查集_UnionFind]]

**堆节点 / 树状数组 / 线段树 / 字典树 / 跳表 / B树** → 分别参见
[[数据结构/C_堆_Heap]]、[[数据结构/M_树状数组_BIT]]、[[数据结构/L_线段树_SegmentTree]]、
[[数据结构/J_字典树_Trie]]、[[数据结构/N_跳表_SkipList]]、[[数据结构/O_B树_BTree]]

2.7 struct 与 STL 容器
------------------------

struct 对象可作为容器元素，需注意比较操作的定义：

```cpp
#include <vector>
#include <map>
#include <algorithm>

struct Item {
    int  id;
    char name[32];

    // 为 map 提供排序依据
    bool operator<(const Item& o) const { return id < o.id; }
};

std::vector<Item> items;              // 见 [[容器类/02_vector]]
items.push_back({1, "Apple"});

std::map<Item, int> inventory;        // 见 [[容器类/10_map_multimap]]
inventory[{2, "Pen"}] = 100;

// Lambda 排序
std::sort(items.begin(), items.end(),
    [](const Item& a, const Item& b) { return a.id < b.id; });
```

详见 [[容器类/01_pair]]（`std::pair` 本质是 struct）、[[容器类/02_vector]]、[[容器类/09_set_multiset]]、[[容器类/10_map_multimap]]。

2.8 struct 与算法
--------------------

几乎每个算法都依赖 struct 组织数据。算法基础用法见 [[算法技巧/函数结构体]]，具体场景：

- **排序**：对 struct 数组按某字段排序 → [[算法技巧/排序]]
- **贪心**：按性价比排序 struct 数组 → [[算法技巧/贪心]]
- **搜索**：用 struct 表示状态（坐标+步数） → [[算法技巧/搜索]]
- **图论**：struct 表示边（起点+终点+权重） → [[算法技巧/图]]
- **前缀和/差分**：struct 维护区间信息 → [[算法技巧/前缀和]]、[[算法技巧/差分]]

2.9 struct 与模板
--------------------

struct 可以模板化，与 class template 完全等价：

```cpp
template<typename T>
struct DataNode {
    T        value;
    DataNode* next;
};

DataNode<int>    intNode{42, nullptr};
DataNode<double> dblNode{3.14, nullptr};
```

> 模板详见 [[09_函数模板]]。

2.10 struct 的构造/析构/继承
-------------------------------

struct 可以拥有构造函数、析构函数、继承体系（与 class 一致）：

```cpp
struct Base {
    int id;
    Base(int i) : id(i) {}
    virtual ~Base() = default;       // 虚析构
};

struct Derived : Base {
    double value;
    Derived(int i, double v) : Base(i), value(v) {}
    void show() const {
        std::cout << id << ": " << value << std::endl;
    }
};
```

> 继承与虚函数的完整机制见 [[06_面向对象(二)继承]] 和 [[07_面向对象(三)多态与虚函数]]。

## ==========================================================================
### 📖 第三节: 实用代码案例（生活场景举例）
## ==========================================================================

案例1：三维向量运算（Vec3 结构体）
--------------------------------------

```cpp
#include <iostream>
#include <cmath>

struct Vec3 {
    double x, y, z;

    // 默认零向量
    Vec3() : x(0), y(0), z(0) {}
    Vec3(double a, double b, double c) : x(a), y(b), z(c) {}

    double length() const {
        return std::sqrt(x*x + y*y + z*z);
    }

    Vec3 normalize() const {
        double len = length();
        return {x/len, y/len, z/len};
    }

    Vec3 operator-(const Vec3& o) const {
        return {x - o.x, y - o.y, z - o.z};
    }

    void print() const {
        std::cout << "(" << x << ", " << y << ", " << z << ")" << std::endl;
    }
};

int main() {
    Vec3 position(10.0, 0.0, 5.0);
    Vec3 target(4.0, 8.0, 5.0);

    Vec3 direction = (target - position).normalize();
    std::cout << "距离: " << (target - position).length() << std::endl;
    std::cout << "方向: "; direction.print();

    return 0;
}
```

案例2：学生成绩管理系统
--------------------------

```cpp
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

struct Student {
    std::string name;
    int   id;
    int   math, english, cs;
    float average;   // 缓存平均分

    Student(const std::string& n, int i, int m, int e, int c)
        : name(n), id(i), math(m), english(e), cs(c),
          average((m + e + c) / 3.0f) {}

    void print() const {
        std::cout << id << " " << name
                  << " | 数学:" << math
                  << " 英语:" << english
                  << " CS:" << cs
                  << " 均分:" << average << std::endl;
    }
};

int main() {
    std::vector<Student> students;
    students.emplace_back("Alice",   1001, 95, 88, 92);
    students.emplace_back("Bob",     1002, 78, 85, 90);
    students.emplace_back("Charlie", 1003, 92, 96, 88);
    students.emplace_back("Diana",   1004, 85, 79, 94);

    // 按均分排序
    std::sort(students.begin(), students.end(),
        [](const Student& a, const Student& b) {
            return a.average > b.average;  // 降序
        });

    std::cout << "===== 成绩排名 =====\n";
    for (size_t i = 0; i < students.size(); ++i) {
        std::cout << "第" << (i+1) << "名: ";
        students[i].print();
    }
    // → 学生成绩管理见 [[容器类/02_vector]] 和 [[算法技巧/排序]]
    return 0;
}
```

案例3：配置解析结构体
------------------------

```cpp
#include <iostream>
#include <string>
#include <map>
#include <sstream>

struct ServerConfig {
    std::string host;
    int         port;
    int         maxConnections;
    bool        enableSSL;
    std::string logPath;
};

// 从配置文件解析 → 详见 [[11_文件与流]]
ServerConfig parseConfig(const std::string& filename) {
    ServerConfig cfg{"localhost", 8080, 100, false, "/var/log/server.log"};
    // 实际项目中用 fstream 读取文件逐行解析
    return cfg;
}

int main() {
    ServerConfig cfg = parseConfig("server.conf");
    std::cout << "服务器: " << cfg.host << ":" << cfg.port
              << " (SSL:" << (cfg.enableSSL ? "ON" : "OFF") << ")"
              << std::endl;
    return 0;
}
```

## ==========================================================================
### 📖 第四节: 章节测试
## ==========================================================================

### 选择题

> **题目1**：在 C++ 中，struct 与 class 的主要区别是什么？

A. struct 不能有成员函数，class 可以

B. struct 不能有构造函数/析构函数，class 可以

C. struct 默认访问级别为 public，class 默认访问级别为 private

D. struct 不能用于继承，class 可以

> [!NOTE]- 答案
> **正确答案：C**
> C++ 中 struct 和 class 的唯一区别是默认访问级别不同。struct 支持成员函数、构造/析构、继承、多态等所有 class 特性。约定俗成用 struct 做纯数据聚合。

---

> **题目2**：以下 struct 在 64 位系统上的 sizeof 是？

```cpp
struct Data {
    char   a;
    double b;
    int    c;
};
```

A. 13 (1+8+4)

B. 16

C. 24

D. 20

> [!NOTE]- 答案
> **正确答案：C**
> 推导：a(1B, offset 0) → pad 7B → b(8B, offset 8~15) → c(4B, offset 16~19) → pad 4B (尾部对齐至 8 的倍数) → 24。详见 [[05_面向对象(一)类与对象基础#综合示例|内存对齐推导]]。

---

> **题目3**：在 C++ 中，能否用 `memcpy` 安全复制以下 struct？

```cpp
struct Safe {
    int x;
    double y;
    char z[10];
};
```

A. 可以，因为 Safe 是 trivial 类型

B. 不可以，struct 不能直接 memcpy

C. 可以，但必须保证目标内存已初始化

D. A 和 C 都对

> [!NOTE]- 答案
> **正确答案：D**
> Safe 只含基本类型，是 trivial + standard-layout，可以安全 memcpy。但目标地址必须指向有效内存。

---

> **题目4**：关于聚合初始化，以下哪条语句在 C++17 中是错误的？

```cpp
struct Point { int x, y; };
```

A. `Point p1 = {1, 2};`

B. `Point p2{1, 2};`

C. `Point p3{.x = 1, .y = 2};`

D. `Point p4();`

> [!NOTE]- 答案
> **正确答案：C**
> 指定初始化器（Designated Initializers）是 C++20 特性，C++17 不支持。D 实际上是声明了一个返回 Point 的函数而非构造对象。

---

> **题目5**：以下哪种场景最适合使用 struct 而非 class？

A. 实现一个带有不变量检查的银行账户类

B. 定义一个链表节点（只有数据和 next 指针）

C. 实现一个 RAII 文件句柄包装类

D. 定义一个需要严格控制访问权限的单例类

> [!NOTE]- 答案
> **正确答案：B**
> 链表节点是纯数据聚合，用 struct 最自然。A/C/D 涉及封装逻辑和数据保护，更适合用 class。详见 [[05_面向对象(一)类与对象基础#2.1 class vs struct 的区别|class vs struct]]。

### 判断题

> **题目6**：在 C++ 中，`struct` 不能包含虚函数。 （ ）

> [!NOTE]- 答案
> **错误**。C++ 的 struct 与 class 几乎等价，同样支持虚函数、虚析构函数、纯虚函数等。

---

> **题目7**：`typedef struct { int x; } Point;` 在 C++ 中是推荐的写法。 （ ）

> [!NOTE]- 答案
> **错误**。C++ 中直接 `struct Point { int x; };` 即可，声明变量时无需 `struct` 关键字。`typedef struct` 是 C 语言的遗留写法，在 C++ 中多余。

---

> **题目8**：struct 成员在内存中按声明顺序排列，编译器可能会为了节省空间而重排。 （ ）

> [!NOTE]- 答案
> **错误**。C++ 标准保证同一访问控制段内成员按声明顺序排列，编译器不会自动重排。详见 [[05_面向对象(一)类与对象基础#声明顺序|声明顺序]]。

> 📌 **拓展阅读**：深入 struct/class 对象模型、构造函数、析构函数、拷贝控制等，请继续学习 [[05_面向对象(一)类与对象基础]]。
