# 

## 

Rust 

### panic! — 

 `std::panic!` unwindingabort `drop` `panic_handler`  `set_hook`  Cargo.toml  `panic = "abort"` 

```text
panic! :

fn main() {
    fn a() { fn b() { panic!("boom"); } b(); }  // ← panic 
    a();
}

:
thread 'main' panicked at 'boom', src/main.rs:1:30
note: run with RUST_BACKTRACE=1 env var to display backtrace
 code 101

 ():
1. b()  panic! 
2. b()  ""
3. b()  drop() 
4.  a()a()  drop 
5.  main()main()  drop 
6. panic handler 
7. 
```

### Result\<T, E\> — 



```rust
enum Result<T, E> { Ok(T), Err(E) }
```

### ? 

`?`  `match` + `return Err` `From` trait  `?` early return

```rust
// 
fn read_file() -> Result<String, io::Error> {
    let mut f = File::open("data.txt")?;
    let mut s = String::new();
    f.read_to_string(&mut s)?;
    Ok(s)
}

//  ()
fn read_file() -> Result<String, io::Error> {
    let mut f = match File::open("data.txt") {
        Ok(val) => val,
        Err(e) => return Err(From::from(e)),  // From::from 
    };
    let mut s = String::new();
    match f.read_to_string(&mut s) {
        Ok(val) => val,
        Err(e) => return Err(From::from(e)),
    };
    Ok(s)
}
```

###  (From trait)

```rust
//  Result<T, E2> ?  E1 
//  impl From<E1> for E2
//  E1::into() 

fn read_config() -> Result<Config, ConfigError> {
    let content = fs::read_to_string("config.toml")?;  // io::Error
    // : ConfigError::from(io::Error)
    let config: Config = toml::from_str(&content)?;    // toml::de::Error
    // : ConfigError::from(toml::de::Error)
    Ok(config)
}
```

`unwrap` / `expect`  `match result { Ok(v) => v, Err(e) => panic!(...)}`

`Option<T>`  `Result<T, E>` `None`  `Err(())`  `?` 

###  C/C++ 

|  | C (errno) | C++ (exceptions) | Rust (Result) |
|------|-----------|-------------------|---------------|
|  |  |  | `?`  |
|  |  | catch |  |
|  | if  | + |  match |
|  |  | catch by type | Result  |
|  |  |  |  |
|  | `abort()` | `std::terminate` | `panic!` |

```c
// C: 
FILE* f = fopen("data.txt", "r");
if (f == NULL) {  // 
    //  f → 
}
char buf[100];
size_t n = fread(buf, 1, 100, f);
if (n < 100 && ferror(f)) {  // 
    // 
}
```

```cpp
// C++: 
try {
    std::ifstream f("data.txt");
    if (!f) throw std::runtime_error("file not found");
    // ...
} catch (const std::exception& e) {
    std::cerr << e.what() << std::endl;
}
//  5-15%
// try 
```

---

## 

### panic!

```rust
panic!("");
// 

//  Cargo.toml 
// [profile.release]
// panic = "abort"  // 
```

### Result 

```rust
use std::fs::File;

let f = match File::open("data.txt") {
    Ok(file) => file,
    Err(e) => {
        eprintln!("{}", e);
        return;
    }
};
```

### unwrap / expect

```rust
let f = File::open("data.txt").unwrap(); // Err  panic
let f = File::open("data.txt").expect(""); //  panic 
// expect  unwrap panic 
```

### ?  — 

```rust
use std::fs;
use std::io::{self, Read};

fn read_file() -> Result<String, io::Error> {
    let mut f = File::open("data.txt")?; // Err 
    let mut s = String::new();
    f.read_to_string(&mut s)?;
    Ok(s)
}

fn main() -> Result<(), Box<dyn std::error::Error>> {
    let content = fs::read_to_string("data.txt")?;
    println!("{}", content);
    Ok(())
    // main  Result Err  Debug 
}
```

### 

```rust
use std::io::ErrorKind;

match File::open("data.txt") {
    Ok(f) => f,
    Err(e) => match e.kind() {
        ErrorKind::NotFound => File::create("data.txt").unwrap(),
        ErrorKind::PermissionDenied => {
            eprintln!("");
            return;
        }
        _ => panic!("{}", e),
    },
};
```

### unwrap_or / unwrap_or_else / unwrap_or_default

```rust
let x: Result<i32, &str> = Err("fail");
x.unwrap_or(0); // → 0
x.unwrap_or_else(|e| { println!(": {}", e); 0 }); // 
x.unwrap_or_default(); // → 0 (i32::default())

File::open("c.toml").unwrap_or_else(|e| {
    if e.kind() == ErrorKind::NotFound {
        File::create("c.toml").unwrap()
    } else { panic!("{}", e); }
});
```

### 

```rust
use std::fmt;
use std::num::ParseIntError;

#[derive(Debug)]
enum AppError {
    Io(std::io::Error),
    Parse(ParseIntError),
    Custom(String),
}

impl fmt::Display for AppError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match self {
            Self::Io(e) => write!(f, "IO : {}", e),
            Self::Parse(e) => write!(f, ": {}", e),
            Self::Custom(s) => write!(f, ": {}", s),
        }
    }
}

impl std::error::Error for AppError {}

//  From  ? 
impl From<std::io::Error> for AppError {
    fn from(e: std::io::Error) -> Self { Self::Io(e) }
}

impl From<ParseIntError> for AppError {
    fn from(e: ParseIntError) -> Self { Self::Parse(e) }
}

//  ? 
fn process() -> Result<i32, AppError> {
    let content = std::fs::read_to_string("data.txt")?;  // io::Error → AppError
    let num: i32 = content.trim().parse()?;  // ParseIntError → AppError
    Ok(num * 2)
}
```

### thiserror vs anyhow

```rust
// thiserror: 
use thiserror::Error;

#[derive(Error, Debug)]
enum AppError {
    #[error("IO : {0}")]
    Io(#[from] std::io::Error),    //  From
    #[error(": {0}")]
    Parse(#[from] ParseIntError),
    #[error(": {0}")]
    Custom(String),
}

// anyhow: 
use anyhow::{Context, Result};

fn process() -> Result<()> {  // Result<T, anyhow::Error>
    let content = std::fs::read_to_string("data.txt")
        .context("")?;  // 
    let num: i32 = content.trim().parse()
        .context("")?;
    Ok(())
}
```

### 

|  |  |
|------|------|
| / | `unwrap()` / `expect()` |
|  | `Result<T, E>` + `?`  |
|  | `panic!` |
|  | main  match  |
|  | `unwrap()` +  |
|  | `anyhow::Context` |

---

## 

###  1unwrap 

```rust
//  :  unwrap
pub fn parse_config(s: &str) -> Config {
    let v: Vec<&str> = s.split('=').collect(); //  panic!
    // ...
}

//  :  Result
pub fn parse_config(s: &str) -> Result<Config, ConfigError> {
    let v: Vec<&str> = s.split('=').collect();
    if v.len() != 2 {
        return Err(ConfigError::InvalidFormat);
    }
    // ...
}
```

###  2`?` 

```rust
//  Result<T, AppError>
//  ?  From<E> for AppError
// 
fn process() -> Result<(), AppError> {
    let s: Result<i32, _> = "abc".parse(); // ParseIntError
    //  impl From<ParseIntError> for AppError
    let n = s?;
    Ok(())
}
```

---

## 

### 

:  — `unwrap_or` 

```rust
let val = map.get(&num).copied().unwrap_or(0);
```

:  — `?` 

```rust
fn get_node_at(head: &Option<Box<ListNode>>, index: usize) -> Result<i32, &'static str> {
    let mut current = head.as_ref().ok_or("")?;
    for _ in 0..index {
        current = current.next.as_ref().ok_or("")?;
    }
    Ok(current.val)
}
```
