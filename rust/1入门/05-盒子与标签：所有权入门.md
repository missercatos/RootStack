# 

## 

 Rust  GC 

`memcpy`Move `drop` 

`Copy` trait stack-only  `Copy` "" use-after-move  MIRMid-level IR move-checker  non-lexical lifetime 

 double-free `drop` →  `free` → 

[[../../c/2/01_|C: ]]

---

## 

### 

Rust 

```text
 1Rust owner
 2
 3drop
```

** 1  2 **  C++  Rust ""borrow

```rust
let s1 = String::from("hello");
let s2 = s1;
// s1 s2 
//  MIR  s1  "moved"
//  s1 
```

** 3 ** `drop` —— C++  RAIIResource Acquisition Is Initialization Rust  drop 

### Move 

```rust
let s1 = String::from("hello");
let s2 = s1;
```

```text
 s1
                     
   
 s1                                   
 ptr →  h  e  l  l  o 
 len: 5              
 cap: 5           


 s2move 
                     
   
 s1                          
 [undefined]          h  e  l  l  o 
                     
         ↑
 s2                  s2  ptr 
 ptr →
 len: 5           
 cap: 5           

```

**** move  24 ptr + len + caps1 

### Clone vs Copy 

```text

                 Copy trait                    
                                               
  • memcpy                         
  •                    
  •           
  •  CloneCopy ⊂ Clone        
  •                         
                                               
  i32, f64, bool, char, &T           



                Clone trait                    
                                               
  •  .clone()                          
  •  clone   
  •  Vec clone    
  •                         
                                               
  String, Vec<T>, Box<T>             

```

** `String`  `Copy`**

```rust
// String 
// ptr (8B) + len (8B) + cap (8B) = 24B
// 

//  String  Copy
let s1 = String::from("hello");
let s2 = s1;  //  Copys1  s2 
// s1  s2  →  drop → double-free
```

```text
Copy 

 42        ← i32

memcpy 4 

 Copy  + 
              
   
 ptr → data    
 len        
 cap     

 ptr  → double-free
 move  clone
```

### Clone  Copy 

```rust
//  derive Copy  Clone
#[derive(Copy, Clone)]
struct Point {
    x: i32,
    y: i32,
}

//   Copy  Copy
// #[derive(Copy)]
// struct Wrapper {
//     name: String,  // String  Copy
// }

//   Clone
#[derive(Clone)]
struct Wrapper {
    name: String,
}

let w1 = Wrapper { name: String::from("hi") };
let w2 = w1.clone();  // name  clone
```

### String vs &str 

```text
String

 24            
   
  ptr  len  cap   
   
                      
    ↓                  
            
  
  h  e  l  l  o  
  


&str

 16            
         
  ptr  len         
         
                      
    ↓                  
 /          
  
  h  e  l  l  o  
  

   cap &str 
```

```rust
// String 
let mut s = String::from("hello");
s.push_str(" world");  //  

// &str 
let s: &str = "hello";
// s.push_str(" world");  //  no method named `push_str`
```

### 

```rust
fn take_ownership(s: String) {
    println!("{}", s);
}   // s String  drop

fn borrow_immutably(s: &String) {
    println!("{}", s);
}   // s  drop 

fn main() {
    let s = String::from("hello");

    take_ownership(s);       // s 
    // println!("{}", s);    //  s  move

    let s2 = String::from("world");
    borrow_immutably(&s2);  // 
    println!("{}", s2);     //  s2 
}
```

****

```rust
fn create_string() -> String {
    let s = String::from("hello");
    s   // 
}   // s  drop

fn modify_and_give_back(mut s: String) -> String {
    s.push_str(" world");
    s   // 
}

fn main() {
    let s1 = create_string();
    let s2 = modify_and_give_back(s1);
    // s1  move s2 
}
```

###  move

```rust
let s1 = String::from("hello");
let s2 = s1;
```

x86-64
```asm
; s1 = String::from("hello")
lea     rdi, [rip + .str.0]    ; 
mov     qword ptr [rbp - 24], rdi  ; ptr
mov     qword ptr [rbp - 16], 5    ; len = 5
mov     qword ptr [rbp - 8], 5     ; cap = 5

; s2 = s1move
mov     rax, qword ptr [rbp - 24]  ;  ptr
mov     qword ptr [rbp - 48], rax
mov     rax, qword ptr [rbp - 16]  ;  len
mov     qword ptr [rbp - 40], rax
mov     rax, qword ptr [rbp - 8]   ;  cap
mov     qword ptr [rbp - 32], rax
; s1 
```

**** move  `mov`  24 

### C/C++ 

```c
// C 
char* s1 = malloc(6);
strcpy(s1, "hello");
char* s2 = s1;  // s2 
free(s1);
free(s2);  // double-free

// C++  RAII
std::string s1 = "hello";
std::string s2 = s1;  // Copy 
// 

// Rust  move
let s1 = String::from("hello");
let s2 = s1;  // moves1 
//  s2  drop
```

---

## 

### Move 

```rust
let s1 = String::from("hello");
let s2 = s1; // s1  s2
// println!("{}", s1); // use of moved value
```

### Copy 

```rust
let x = 5;
let y = x; // x i32  Copy
println!("{} {}", x, y);
```

 `Copy` 

|  |
|------|
| `i8`, `i16`, `i32`, `i64`, `i128`, `isize` |
| `u8`, `u16`, `u32`, `u64`, `u128`, `usize` |
| `f32`, `f64` |
| `bool`, `char` |
| `&T` |
| `(T1, T2, ...)`  Copy |

### Clone — 

```rust
let s1 = String::from("hello");
let s2 = s1.clone(); // s1 
```

### 

```rust
fn consume(s: String) { } // 

fn main() {
 let s = String::from("hi");
 consume(s);
 // println!("{}", s); // s 
}
```

```rust
fn give_back(s: String) -> String { s } // 

let s = give_back(String::from("hi")); // 
```

###  Copy

```rust
let t1 = (1, 2, 3); //  Copy →  Copy
let t2 = (1, String::from("h")); //  Copy →  Copy
```

---

## 

### 1.  move 
```rust
let s = String::from("hello");
let s2 = s;
// println!("{}", s);  //  use of moved value `s`
```

### 2.  `.clone()`  move
```rust
let s = String::from("hello");
let s2 = s.clone();  //  
// let s2 = s;       //  move  s 
```

### 3.  `Copy` 
```rust
let x = 5;
let y = x;  // Copy memcpy""
//  i32 
```

### 4.  Copy  Copy
```rust
#[derive(Copy, Clone)]  //  the trait `Copy` may not be implemented
struct User {
    name: String,  // String  Copy
    age: u32,      // u32  Copy
}
//  Copy Copy
```

---

## 

### 

:  — 

```rust
fn main() {
 let mut k: i64 = read_one();
 let mut day = 1;
 let mut sum = 0;
 while k > 0 {
 for _ in 0..day {
 sum += day;
 k -= 1;
 if k == 0 { break; }
 }
 day += 1;
 }
 println!("{}", sum);
}
```
