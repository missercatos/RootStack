# 

## 

### 

Rust  `.rs`  `mod`  crate `src/main.rs`  `src/lib.rs`  crate root

Package > Crate > Module

```text
Package ( Cargo.toml):
 Crate 1: lib crate (src/lib.rs)
 Crate 2: bin crate (src/main.rs)
 Crate 3: bin crate (src/bin/tool.rs)
 ...

Crate ( .rlib ):
 Module: src/lib.rs (crate root)
 Module: src/utils.rs
 Module: src/net/mod.rs
    Module: src/net/tcp.rs
    Module: src/net/udp.rs
 ...

Module ():
 pub fn public_fn()
 fn private_fn()
 pub struct PublicStruct { pub field: Type }
 struct PrivateStruct { field: Type }
```

### 

```text
src/
 main.rs           ← crate root (bin crate)
 lib.rs            ← crate root (lib crate)
 utils.rs          ← mod utils ( main.rs )
 net/              ← mod net ()
    mod.rs        ← mod net 
    tcp.rs        ← net::tcp
    udp.rs        ← net::udp
 old/              ←  (mod.rs )
     mod.rs
     stuff.rs
```

### 

```rust
// : 
fn foo() { self::bar(); }  //  bar
fn baz() { super::bar(); } //  bar

// :  crate root 
crate::utils::helper();
// 
my_crate::utils::helper();  //  crate

// self/super/crate 
mod network {
    pub fn connect() {
        super::log("connecting");  // 
        self::handshake();          // 
        crate::config::get();      // crate root
    }
    fn handshake() { }
}
```

`pub`  `private``pub(crate)`  crate `pub(super)` `pub(in path)` 

```text
:
 pub                  → 
 pub(crate)           →  crate 
 pub(super)           → 
 pub(in crate::path)  → 
 ()           → 
```

`use`  C++  `using`

### 

```rust
// 
crate::  //  crate root  ()
self::   //  ()
super::  //  ()

// use 
use crate::net::tcp;    // 
use self::config;       //  config
use super::utils;       //  utils
```

---

## 

### 

```rust
mod network {
    pub fn connect() { }

    fn handshake() { } // 

    // 
    pub mod http {
        pub fn get() { }
        fn internal() { } // network::http::internal
    }
}

fn main() {
    network::connect();
    network::http::get();
    // network::http::internal(); // : 
}
```

### 

|  |  |  |
|--------|----------|------|
|  |  | `fn helper()` |
| `pub` |  | `pub fn connect()` |
| `pub(crate)` |  crate  | `pub(crate) fn internal()` |
| `pub(super)` |  | `pub(super) fn from_parent()` |
| `pub(in path)` |  | `pub(in crate::net) fn server_fn()` |

```rust
// 
pub struct User {
    pub name: String,    // : 
    pub(crate) id: u32,  // crate : 
    password: String,     // : 
}

// : 
impl User {
    pub fn new(name: String, password: String) -> Self {
        User {
            name,
            id: next_id(),
            password,
        }
    }
    pub fn name(&self) -> &str { &self.name }
    // password  getter → 
}
```

### 

```rust
// main.rs
mod animals; //  animals.rs  animals/mod.rs

fn main() {
    animals::Dog::bark();
}
```

```rust
// animals.rs
pub struct Dog { pub name: String }
impl Dog { pub fn bark(&self) { println!("Woof!"); } }
```

### 

```text
src/
 main.rs
 school/
    mod.rs          # pub mod student; pub mod teacher;
    student.rs      # pub struct Student { ... }
    teacher.rs      # pub struct Teacher { ... }
```

```rust
// src/main.rs
mod school;

fn main() {
    let s = school::student::Student::new("Alice");
    let t = school::teacher::Teacher::new("Bob");
}
```

### use 

```rust
use std::collections::HashMap; // 
use std::io::{self, Read, Write}; // self = io 
use std::collections::*; // 
use std::fmt::Result as FmtResult; // 

//  use
use std::{fs, io};
use std::io::{self, BufRead, BufReader};

// : 
pub use crate::net::tcp::Connection;
//  use my_crate::Connection
```

### 

```toml
# Cargo.toml
[dependencies]
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
```

```rust
use rand::Rng;
fn main() {
    let n = rand::thread_rng().gen_range(1..=100);
}
```

### 

|  |  | Cargo.toml |
|---|------|-----------|
| `rand` |  | `"0.8"` |
| `serde` |  | `{ version="1.0", features=["derive"] }` |
| `regex` |  | `"1"` |
| `chrono` |  | `"0.4"` |
| `clap` | CLI  | `"4"` |
| `tokio` |  | `"1"` |

### 

```rust
/// 
///
/// # Examples
///
/// ```
/// let result = gcd(14, 21);
/// assert_eq!(result, 7);
/// ```
///
/// # Panics
///
///  `a`  `b`  0 panic
pub fn gcd(a: u64, b: u64) -> u64 {
    if b == 0 { a } else { gcd(b, a % b) }
}

//! crate  ( crate root)
//!
//! # My Library
//!
//! 

// cargo doc --open  HTML 
// cargo test 
```

### Workspace

```toml
#  Cargo.toml
[workspace]
members = [
    "core",
    "cli",
    "web",
]

[workspace.dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
```

```toml
# core/Cargo.toml
[package]
name = "my-core"
[dependencies]
tokio.workspace = true  # 
serde.workspace = true
```

Workspace 
-  `Cargo.lock`
-  `target/` 
- 
- /

---

## 

###  1`x.rs`  `x/mod.rs` 

```text
 src/x.rs  src/x/mod.rs:
→ !

 src/x.rs ()  src/x/mod.rs ()
: src/x.rs  src/x/mod.rs
```

###  2 `pub`

```rust
//   pub
pub struct Config {
    pub host: String,
    pub port: u16,
    pub password: String,  // !
}

//  
pub struct Config {
    pub host: String,
    pub port: u16,
    password: String,  // 
}

impl Config {
    pub fn new(host: String, port: u16, password: String) -> Self {
        Self { host, port, password }
    }
    pub fn host(&self) -> &str { &self.host }
    pub fn port(&self) -> u16 { self.port }
    // password  getter
}
```

### 

1.  `pub(crate)`  `pub`
2.  `pub use`  API
3.  `///`  `//!` `cargo doc`  HTML
4.  workspace  crate
5.  `src/x.rs`  `src/x/mod.rs`

---

## 

### 

:  — 

```rust
// src/lib.rs
pub mod data_structures;
pub mod algorithms;
pub mod utils;

// src/data_structures/mod.rs
pub mod stack;
pub mod queue;
pub mod heap;

// src/data_structures/stack.rs
pub struct Stack<T> {
    elements: Vec<T>,
}

impl<T> Stack<T> {
    pub fn new() -> Self {
        Stack { elements: Vec::new() }
    }
    pub fn push(&mut self, item: T) {
        self.elements.push(item);
    }
    pub fn pop(&mut self) -> Option<T> {
        self.elements.pop()
    }
}
```
