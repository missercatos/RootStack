# 

## 

Rust  `enum` tagged union / sum type discriminant+  uniondiscriminant 

### 

```text
enum TrafficLight { Red, Yellow, Green }

 ():

 discriminant: u8  (: 0, 1,  2)       
                             

: 1 

enum Message {
    Quit,                       // variant 0
    Move { x: i32, y: i32 },   // variant 1 ()
    Write(String),              // variant 2 ()
    ChangeColor(i32, i32, i32), // variant 3 ()
}

:

 discriminant: u8  (0~3)                     

 data: union {                               
   (empty),                                  
   struct { x: i32, y: i32 },                
   String { ptr, len, cap },                 
   (i32, i32, i32),                          
 }                                           
  = max(0, 8, 24, 12) = 24           

```

Rust  discriminant discriminant niche optimization

### Niche Optimization

 Rust  `Option<&T>`  null 0  `None` discriminant 

```text
Option<&T>  (64 ):

Some(&T):

 ptr:             8 


None:

 ptr: 0x000000000000    8 


 = 8  (!)
```

 C++  `std::optional<T>` bool discriminant `std::optional<unique_ptr<int>>`  16  Rust  `Option<Box<i32>>`  8 

`match` exhaustiveness checkingdecision tree `switch` 

`Option<T>`  `max(sizeof(T), 0) + discriminant` `None`  discriminant  niche optimization `Option<bool>`  1  2  `bool`  0/1 `None`

```text
Option<bool> :

bool : 0x00 = false, 0x01 = true, 0x02~0xFF 
         ↑ niche ()

Option<bool>:
  None = 0x02 ( niche)
  Some(false) = 0x00
  Some(true) = 0x01

: 1  ( discriminant!)
```

`match` `if n > 5`

###  C/C++ 

|  | C enum | C++ std::variant | Rust enum |
|------|--------|-------------------|-----------|
|  |  int |  |  |
|  |  union |  std::visit |  |
|  |  |  |  |
|  |  int |  |  |
|  | N/A | N/A |  |

### C/C++ 

```c
// C: 
enum TrafficLight { RED = 0, YELLOW = 1, GREEN = 2 };
enum TrafficLight light = RED;
int x = light;  //  int
light = 42;     // 

// C: 
struct Message {
    enum { QUIT, MOVE, WRITE } tag;
    union {
        struct { int x, y; } move_data;
        char* write_data;
    };
};
```

```cpp
// C++: std::variant 
#include <variant>
#include <string>
struct Quit {};
struct Move { int x, y; };
struct Write { std::string text; };
using Message = std::variant<Quit, Move, Write>;

//  std::visit 
std::visit([](auto&& arg) {
    using T = std::decay_t<decltype(arg)>;
    if constexpr (std::is_same_v<T, Move>)
        std::cout << arg.x << "," << arg.y;
}, msg);
```

---

## 

### 

```rust
enum TrafficLight {
    Red, // 
    Yellow,
    Green,
}

enum Message {
    Quit,
    Move { x: i32, y: i32 }, // 
    Write(String), // 
    ChangeColor(i32, i32, i32),
}

//  Box 
enum List {
    Cons(i32, Box<List>),
    Nil,
}
// :
// Cons: [i32(4) + Box(8)] = 12 
// Nil:  0  →  1 
```

### match 

`match`  Rust ——" case"

```rust
let light = TrafficLight::Red;
match light {
    TrafficLight::Red => "stop",
    TrafficLight::Yellow => "slow",
    TrafficLight::Green => "go",
    // 
    //  match 
}
```

```rust
match msg {
    Message::Quit => { },
    Message::Move { x, y } => println!("({},{})", x, y),
    Message::Write(text) => println!("{}", text),
    Message::ChangeColor(r, g, b) => println!("{},{},{}", r, g, b),
}
```

 MIRMid-level IR decision tree

### Option

`Option<T>`  Rust "" C  NULL  C++  nullptr

```rust
enum Option<T> { Some(T), None }

let x: Option<i32> = Some(5);
// let y = x + 1; // Option<i32> != i32
//  None 

let y = match x {
    Some(n) => n + 1,
    None => 0,
};

// 
x.unwrap(); // panic on None — 
x.unwrap_or(0); // None 
x.unwrap_or_else(|| compute_default()); // 
x.is_some(); // 
x.is_none(); // 
x.map(|v| v * 2); // Some 
x.and_then(|v| if v > 0 { Some(v) } else { None }); // 
x.as_ref(); // Option<&T> from &Option<T>
```

### if let —  match 

`if let`  `match` ——

```rust
if let Some(n) = x {
    println!("{}", n);
} else {
    println!("");
}

// 
match x {
    Some(n) => println!("{}", n),
    _ => println!(""),
}
```

### let-else — 

```rust
// let-else: break/return/continue/panic
let Some(n) = x else {
    println!("");
    return;
};
//  n  i32
println!(" {}", n);
```

###  `_` 

```rust
match n {
    1 => "one",
    2..=9 => "small",  // 
    10..=99 => "medium",
    _ => "other", // 
}

match opt {
    Some(n) if n > 0 => "",   // : 
    Some(_) => "",
    None => "",
}

// _ _x 
match x {
    Some(_) => println!(""),  // 
    None => println!(""),
}
```

### @ 

`@` 

```rust
match age {
    0 => println!(""),
    n @ 1..=12 => println!(": {}", n),   // 
    n @ 13..=17 => println!(": {}", n),
    n @ 18.. => println!(": {}", n),
    _ => unreachable!(), //  i32 
}

//  @ :
match age {
    1..=12 => println!(": {}", age),  //  age
    13..=17 => println!(": {}", age),
    18.. => println!(": {}", age),
    _ => unreachable!(),
}
```

### 

```rust
// 
let (x, y, z) = (1, 2, 3);

// 
struct Point { x: i32, y: i32 }
let Point { x, y } = Point { x: 1, y: 2 };

// 
enum Shape {
    Circle(f64),
    Rectangle(f64, f64),
}
let Shape::Circle(radius) = Shape::Circle(3.14);

// 
let [a, b, ..] = [1, 2, 3, 4, 5]; // a=1, b=2, 

// 
let (x, 0, y) = (1, 0, 3); // : x=1, y=3
// let (x, 1, y) = (1, 0, 3); // 
```

### 

```rust
impl TrafficLight {
    fn duration(&self) -> u32 {
        match self {
            Self::Red => 60,
            Self::Yellow => 5,
            Self::Green => 45,
        }
    }

    // 
    fn next(&self) -> Self {
        match self {
            Self::Red => Self::Green,
            Self::Green => Self::Yellow,
            Self::Yellow => Self::Red,
        }
    }
}

// 
impl TrafficLight {
    fn all() -> Vec<Self> {
        vec![Self::Red, Self::Yellow, Self::Green]
    }
}
```

### match 

```rust
let opt: Option<String> = Some(String::from("hello"));

// match 
// match opt {
//     Some(s) => println!("{}", s),  // s 
//     None => println!("none"),
// }
// opt 

//  &opt 
match &opt {
    Some(s) => println!("{}", s),  // s  &String
    None => println!("none"),
}
// opt 
```

---

## 

###  1match 

```rust
let x: Option<i32> = Some(5);
// 
match x {
    Some(n) if n > 0 => println!(""),
    Some(_) => println!(""),
    None => println!(""),
}
// 
```

###  2 C 

```rust
// C  Rust  repr 
#[repr(C)]
enum CColor { Red = 0, Green = 1, Blue = 2 }

// C  int  Rust 
// 
let x: i32 = 5;
let color = match x {
    0 => Some(CColor::Red),
    1 => Some(CColor::Green),
    2 => Some(CColor::Blue),
    _ => None,
};
```

### 

1.  `match`  `if let`
2.  `@` 
3.  enum  `#[non_exhaustive]` 
4.  `_` 

---

## 

### 

:  — 

```rust
#[derive(Debug, PartialEq)]
enum State { Playing, Paused, Over }

impl State {
    fn transition(&self, event: &str) -> Self {
        match (self, event) {
            (State::Playing, "pause") => State::Paused,
            (State::Playing, "stop") => State::Over,
            (State::Paused, "play") => State::Playing,
            (State::Paused, "stop") => State::Over,
            (state, _) => state.clone(),
        }
    }
}
```

:  — match 

```rust
fn count_matches(nums: &[i32]) -> (usize, usize, usize) {
    let mut neg = 0;
    let mut zero = 0;
    let mut pos = 0;
    for &n in nums {
        match n.cmp(&0) {
            std::cmp::Ordering::Less => neg += 1,
            std::cmp::Ordering::Equal => zero += 1,
            std::cmp::Ordering::Greater => pos += 1,
        }
    }
    (neg, zero, pos)
}
```
