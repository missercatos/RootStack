# 

## 

Rust  `fn main()` CRTC Runtime`let` immutable by default C  `const int` 

`println!`  `std::io::_print`  `format_args!`  C `printf` 

 debug  panicrelease two's complement wrapping

[[../../c/1/03_|C: ]]

---

## Rust 

### `fn` 

`fn`  Rust Rust 

```rust
// 
fn greet() {
    println!("hello");
}

// 
fn add(a: i32, b: i32) -> i32 {
    a + b  // 
}

// 
<'a, T: std::fmt::Display> fn print_ref(r: &'a T) {
    println!("{}", r);
}

// pub  crate 
pub fn public_api() -> i32 {
    42
}
```

**Rust ** C  `int` 

```rust
//  
fn square(x: i32) -> i32 {
    x * x
}

//  
// fn square(x) -> i32 { x * x }
//            ^^ expected one of `:`, `@`, or `|`
```

### `println!` 

`println!`  **declarative macro** `macro_rules!` 

```rust
// 
println!("{}{}", name, age);

// 
{
    // format_args! 
    let _args = format_args!("{}{}", name, age);
    std::io::_print(_args);
}
```

****  Rust  `println!` 
1. `{}``{:.2}``{:?}` 
2. 
3.  C `printf` 

```rust
// C 
// printf("%s %d", name);  // 

// Rust 
println!("{}", name);      //  
// println!("{}", name, age);  //  too few arguments
```

### `;`Expression

 Rust ****

```rust
// Rust  `{}` 
let x = {
    let a = 5;
    let b = 10;
    a + b  // ←  15
};
// x == 15

// statement ()
let y = {
    let a = 5;
    let b = 10;
    a + b;  // ←  ()
};
// y == ()
```

```text
Expression          Statement
          
                      ()
             
                     

5 + 3           → 8         let x = 5;    → ()
if a { 1 } else { 0 } → 1  println!("hi"); → ()
{ a + b }       → 15        x = 5;        → ()
```

** = ** 

```rust
fn double(x: i32) -> i32 {
    x * 2   //   x*2
}

fn double_broken(x: i32) -> i32 {
    x * 2;  //  expected `i32`, found `()`
            //     ()
}
```

### main 

`main`  `()`unit type `Result`

```rust
// 
fn main() {
    println!("Hello!");
}

//  Result main  ? 
fn main() -> Result<(), Box<dyn std::error::Error>> {
    let content = std::fs::read_to_string("config.toml")?;
    println!("{}", content);
    Ok(())
}

//  !
// fn main() -> ! {
//     loop { }  // 
// }
```

`!`  **never type**`loop {}``panic!()``std::process::exit()`  `!`

###  vs 

```rust
//  Rust 
fn add(a: i32, b: i32) -> i32 {
    a + b
}

// 
fn add_explicit(a: i32, b: i32) -> i32 {
    return a + b;
}

//  return
fn find_first_negative(arr: &[i32]) -> Option<i32> {
    for &val in arr {
        if val < 0 {
            return Some(val);  // 
        }
    }
    None  // 
}
```

---

## 

### 

```rust
fn main() {
 let name = "";
 let age: i32 = 18; // 
 println!("{}{}", name, age);
}
```

### 

```rust
let x = 5;
// x = 6; // cannot assign twice to immutable variable

let mut y = 5;
y = 6; // 
```

###  (Shadowing)

```rust
let x = 5;
let x = x + 1; //  x  x
let x = "hello"; // 
```

**Shadowing  `mut` **

| | Shadowing | `mut` |
|--|-----------|-------|
|  |  |  |
|  |  |  |
|  |  |  |
|  |  |  |

```rust
let spaces = "   ";          // &str 
let spaces = spaces.len();   // usize 
// mut 
// let mut spaces = "   ";
// spaces = spaces.len();     //  
```

### 

|  |  |  |
|------|------|------|
| `i32` | 32 | `42` |
| `f64` | 64 | `3.14` |
| `bool` |  | `true`, `false` |
| `char` | Unicode 4 | `''` |
| `&str` |  | `"hello"` |

### 

```rust
let a = 10;
let b = 3;
println!("{}+{}={}", a, b, a + b); // 10+3=13
println!("{}/{}={}", a, b, a / b); // 10/3=3
println!("{}/{}={}", 10.0/3.0, f64); // 3.333...
```

### 

```rust
println!("{0} + {1} = {2}", x, y, x + y); // 
println!("{name} {age}", name="", age=18); // 
println!("{:#?}", some_struct); // 
```

### 

```rust
// 
/*  */
///  rustdoc
//! 
```

### 

|  |  |
|------|------|
|  |  `;`  |
|  | `''`  char `""` |
|  | `let x: i32 = "hello"`  |
|  |  |

---

## 

### 1. `println!` 
```rust
let fmt = "{}";
// println!(fmt, 42);  //  
println!("{}", 42);     //  
```

### 2. 
```rust
let result = 7 / 2;   // result == 3 3.5
let result = 7.0 / 2.0; // result == 3.5
```

### 3. 
```rust
//  
// fn example(x: bool) -> i32 {
//     if x { 1 } else { "hello" }  // 
// }

//  
fn example(x: bool) -> i32 {
    if x { 1 } else { 0 }
}
```

---

## 

### 

: 

```rust
fn main() {
 let c = "*";
 println!(" {}", c);
 println!(" {}{}{}", c, c, c);
 println!("{}{}{}{}{}", c, c, c, c, c);
}
```

: 

```rust
fn main() {
 let mut input = String::new();
 std::io::stdin().read_line(&mut input).unwrap();
 let nums: Vec<i64> = input
 .split_whitespace()
 .map(|s| s.parse().unwrap())
 .collect();
 println!("{}", nums[0] + nums[1]);
}
```
