# 

## 

### 

lifetime MIR  borrow checker 

`'a` ""`'static`  `.rodata` `"hello"`  `&'static str`

```text
:

fn longest<'a>(x: &'a str, y: &'a str) -> &'a str

'a "":
" <= x  y "

:
  lifetime(x) >= lifetime(return_value)
  lifetime(y) >= lifetime(return_value)
  → return_value  = min(lifetime(x), lifetime(y))
```

### NLLNon-Lexical Lifetimes

NLL 

```text
 (Rust 2015):
{
    let r = &x;       // r 
    // ...  r ...
    let y = 5;        // ←  r 
}                     // r 

NLL (Rust 2018+):
{
    let r = &x;
    // ...  r ...
    // ← r  ()
    let y = 5;        //  x
    // x 
}
```

```rust
// NLL  (Rust 2015 ):
let mut data = vec![1, 2, 3];
let first = &data[0];    // 
println!("{}", first);    //  first
// ← first  (NLL)
data.push(4);             // : !
```

### Polonius

Polonius  NLL 

```rust
// Polonius  NLL :
fn process(condition: bool, data: &mut Vec<i32>) {
    let r = &data[0];        // 
    if condition {
        println!("{}", r);    // 
    }
    // NLL: r 
    // Polonius: r  if 
    //  condition  falser 
    data.push(4);
}
```

 Rust  90%+ 

1. 
2.  → 
3. `&self` / `&mut self` →  self 

NLL borrowck""

###  C/C++ 

```c
// C: 
char* create() {
    char buffer[100];
    sprintf(buffer, "hello");
    return buffer;  //  → UB!
}

// 
char* s = create();
printf("%s\n", s);  // : 
```

```cpp
// C++: std::reference_wrapper 
std::string& longest(const std::string& a, const std::string& b) {
    return (a.length() > b.length()) ? a : b;
    //  a 
}

// C++ 
auto ptr = std::make_shared<int>(42);
//  shared_ptr ptr  ()
```

Rust ****

---

## 

### 

```rust
// let r;
// {
//     let x = 5;
//     r = &x;  // : x does not live long enough
// }
// println!("{}", r);  // r  x

// :
// r  >= println! 
// x 
// r  > x  →  → 
```

### 

```rust
fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
    if x.len() > y.len() { x } else { y }
}
//  = min((x), (y))

// :
let s1 = String::from("long string");
let result;
{
    let s2 = String::from("xyz");
    result = longest(s1.as_str(), s2.as_str());
    println!("{}", result);  //  s2 
}
// println!("{}", result);  //  s2 result 
```

```rust
// 
fn first<'a>(x: &'a str, _y: &str) -> &'a str { x }
// _y 
```

### 

```rust
struct Excerpt<'a> {
    part: &'a str, // 
}

impl<'a> Excerpt<'a> {
    fn get(&self) -> &str { self.part } // 3
}

// :
let novel = String::from("Call me Ishmael. Some years ago...");
let first_sentence;
{
    let i = novel.as_str().find('.').unwrap_or(novel.len());
    first_sentence = Excerpt { part: &novel[..i] };
}
println!("{}", first_sentence.part);  //  novel 
```

### 

|  |  |  |
|------|-----------|------|
| `fn f(s: &str) -> &str` |  | 1+2 |
| `fn f(&self, s: &str) -> &str` |  | 1+3 |
| `fn f(x: &str, y: &str) -> &str` |  |  |
| `struct S { r: &str }` |  |  |
| `fn f() -> &str` |  |  |

### 

```rust
let s: &'static str = "hello"; // 
// 

//  'static
const MAX: i32 = 100;           // &'static i32
static GREETING: &str = "hi";  // &'static str

// Box::leak  'static 
let s = Box::leak(Box::new(String::from("hello")));
// s: &'static mut String
// : 
```

###  bound

```rust
// 
fn print_ref<'a, T: Display + 'a>(x: &'a T) {
    println!("{}", x);
}
// T: 'a  T  'a 

// 
fn merge<'a, 'b, T>(a: &'a T, b: &'b T) -> &'a T
where
    'b: 'a,  // 'b  'a 
{
    a
}
```

### 

```rust
//  1:  ()
fn first<'a>(x: &'a str, y: &'a str) -> &'a str { x }

//  2: 
fn first_str<'a, 'b>(x: &'a str, _y: &'b str) -> &'a str { x }

//  3: 
struct Parser<'input> {
    input: &'input str,
    pos: usize,
}

impl<'input> Parser<'input> {
    fn new(input: &'input str) -> Self {
        Parser { input, pos: 0 }
    }
    fn next_token(&mut self) -> Option<&'input str> {
        //  input 
        let start = self.pos;
        // ...  ...
        Some(&self.input[start..self.pos])
    }
}

//  4: 
fn get_len(s: &str) -> usize { s.len() }
// : fn get_len<'a>(s: &'a str) -> usize
//  usize
```

---

## 

###  1

```rust
//  
fn create_ref() -> &str {
    let s = String::from("hello");
    &s  // s  drop
    // 
}

//  
fn create_owned() -> String {
    String::from("hello")  // 
}
```

###  2

```rust
// 
fn process<F>(data: &str, f: F) -> usize
where
    F: Fn(&str) -> usize,
{
    f(data)
}

// :
let result = process("hello", |s| s.len());
//  data
```

### 

1.  90% 
2. 
3.  `String`/`Vec` 
4. `'static` """"

---

## 

### 

:  — 

```rust
fn find_lcs<'a>(a: &'a str, b: &'a str) -> &'a str {
    let a: Vec<char> = a.chars().collect();
    let b: Vec<char> = b.chars().collect();
    // LCS 
    &a[0..0]  // 
}
```

:  — 

```rust
struct Lexer<'a> {
    input: &'a [u8],
    pos: usize,
}

impl<'a> Lexer<'a> {
    fn new(input: &'a [u8]) -> Self {
        Lexer { input, pos: 0 }
    }

    fn next_token(&mut self) -> Option<&'a [u8]> {
        if self.pos >= self.input.len() {
            return None;
        }
        let start = self.pos;
        while self.pos < self.input.len() && self.input[self.pos] != b' ' {
            self.pos += 1;
        }
        let token = &self.input[start..self.pos];
        self.pos += 1; // skip space
        Some(token)
    }
}
```
