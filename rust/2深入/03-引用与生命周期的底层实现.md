# 

## 

### 

Rust  C —— 8B borrow checker  `&T`  noalias  LLVM `&mut T`  unique + noalias 

****

- **noalias **LLVM IR 
- **alias**C/C++ 
- **strict aliasing **C/C++ Rust 
- **NLLNon-Lexical Lifetimes**use-sitescope

```text
 vs :

 ():
  &T:      noalias 
  &mut T:  unique + noalias 
  

 ():
  &T:     8  (x86_64)
  &mut T: 8  (x86_64)
   C 

LLVM IR :
  define void @foo(i32* noalias %a, i32* noalias %b) { ... }
  ; noalias : a  b 

  define void @bar(i32* noalias %a, i32* %b) { ... }
  ;  a  noaliasb  a 
```

### LLVM noalias 

`noalias` 

- Load/store 
- 
- 

```text
noalias :

:
fn add(a: &mut i32, b: &i32) {
    *a += *b;
    *a += *b;  //  *b
}

LLVM  (a  noalias):
fn add(a: *mut i32 noalias, b: *const i32 noalias) {
    let r1 = *b;     // 
    let r2 = *a;     //  a
    let r3 = r1 + r2; // r1 + r2
    *a = r3;         //  a ( b )
    let r4 = *b;     //  b  noalias b !
    *a = r3 + r4;    //  r1  r4
}

:
fn add(a: *mut i32 noalias, b: *const i32 noalias) {
    let r1 = *b;     // 
    let r2 = *a;
    let r3 = r1 + r2;
    *a = r3 + r1;    //  r1  *b
}

:
   noalias: 2  (load b, load b)
   noalias: 1  (load b)
  
```

###  (Borrow Checker)

 MIR NLL borrowckvariable `&`  borrow `&mut`  unique borrowborrows  disjoint intervals 

```text
NLL :

1.  MIR ()
    (basic block) 

2. :
   &x       →  region 'a: x  'a 
   &mut x   →  region 'a: x  'a 

3. :
    =  (region)
    = 

4. :
   

5. :
    → 

:
let mut s = String::from("hello");
let r1 = &s;           // borrow(s, 'a) 
println!("{}", r1);     // 'a  (NLL: )
let r2 = &mut s;       // borrow_mut(s, 'b) 
// r1 'a  'b  → OK
println!("{}", r2);
```

### NLL 

```text
NLL :

:
let mut s = String::from("hello");
let r1 = &s;
let x = 5;              // 
println!("{} {}", r1, x);
let r2 = &mut s;        // NLL: r1 
println!("{}", r2);

 ():
  'a =  r1 
  'b =  r2 
  'a  'b  →  ()

NLL :
  'a =  r1  r1  (println! )
  'b =  r2  r2 
  'a  'b  → OK

:
  bb0: {
      s = String::new();
      r1 = &s;              // 'a 
      x = 5;
  }
  bb1: {
      println!("{}", r1);   // 'a  ( r1)
      r2 = &mut s;          // 'b 
  }
  bb2: {
      println!("{}", r2);   // 'b 
  }
```

### Polonius

Polonius  Rust  NLL Polonius ****

```text
NLL vs Polonius :

NLL:
  -  (region) 
  - 
  - ""

Polonius:
  - 
  - """"
  - 

 (Polonius  NLL ):
fn example(cond: bool) {
    let mut s = String::from("hello");
    let r1 = &s;
    if cond {
        println!("{}", r1);  // r1 
    }
    // NLL: r1  ()
    // Polonius: r1  cond=true 
    let r2 = &mut s;  // NLL: ! Polonius: OK
    println!("{}", r2);
}

Polonius :
  1.  "origin" ()
  2.  origin  "loan" ()  "liveness" ()
  3. :  loan  origin  → 
  4.  "dataflow" 
```

###  (Fat Pointers)

`&str`  `&[T]`  fat pointer16B`&dyn Trait`  vtable 

```text
Fat pointer :

&[i32] (slice ):
  
   ptr  (8 bytes)    → [1, 2, 3, 4, 5]
   len  (8 bytes)    = 5
  
  : 16 

&str ():
  
   ptr  (8 bytes)    → "hello" (UTF-8)
   len  (8 bytes)    = 5
  
  : 16 

&dyn Trait (trait object):
      
   data_ptr (8B)    →  T              
   vtable_ptr (8B)  →  [drop, size, align, 
        method1, method2,  
                            ...]               
                          
  : 16 

 C :
  C  char*  strlen()  → O(n) 
  Rust  &str  → O(1) 
  C  void*  → 
  Rust  &dyn Trait  vtable → 
```

###  C/C++ 

```text
C/C++  vs Rust :

C/C++:
  - strict aliasing :  ()
  -  (void* )
  -  →  (UB)
  -  strict aliasing 

   (C, ):
  int x = 1;
  float* fp = (float*)&x;  // strict aliasing 
  *fp = 2.0f;              // UB!

Rust:
  - : &T  &mut T 
  - :  void*
  -  →  ( UB)
  -  unsafe 

   (Rust, ):
  let mut x = 1i32;
  let r1 = &x;
  let r2 = &mut x;  // :  &x  &mut x

:
  C/C++: ""
  Rust:   noalias
```

---

## 

### 

```rust
let x = 5;
let r: &i32 = &x; //  *const i32
let v = *r; // 

// unsafe
let p: *const i32 = &x as *const i32;
unsafe { println!("{}", *p); }
```

### 

|  |  |
|------|------|
|  | `let r1 = &s; let r2 = &s;` |
|  | `let r = &mut s;` |
|  | `&s`  `&mut s`  |
|  move |  `&s`  `let t = s` |

### noalias 

```rust
fn add(a: &mut i32, b: &i32) { *a += *b; }

//  a  noalias load/store
//  &mut 
```

### fat pointer 

|  |  |  |
|------|--------|----------|
| `&T` | 8B |  |
| `&[T]` | 16B | ptr + len |
| `&str` | 16B | ptr + len |
| `&dyn Trait` | 16B | data ptr + vtable ptr |
| `&mut T` | 8B |  |

### 

```rust
// 
fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
    if x.len() > y.len() { x } else { y }
}

// 
let x = 5;
let r = &x;
assert_eq!(5, *r);  // 
assert_eq!(&5, r);  // 

// 
let mut v = vec![1, 2, 3];
let first = &mut v[0];
*first = 10;  // 
// v.push(4); // : first  v
```

### 

```rust
// 
fn dangle() -> &String { // : 
    let s = String::from("hello");
    &s // s  drop
}

// : 
fn no_dangle() -> String {
    let s = String::from("hello");
    s //  drop
}

// :
// 
// 
```

---

## 

### 

```text
:

fn example() {
    let mut s = String::from("hello");
    let r1 = &s;           // : borrow(s, region 'a)
    println!("{}", r1);     //  r1: region 'a 
    let r2 = &mut s;       // : borrow_mut(s, region 'b)
    println!("{}", r2);     //  r2: region 'b 
}

:
  1. borrow(s, 'a)      → s  'a 
  2. borrow_mut(s, 'b)  → s  'b 
  3. 'a  'b   → 'a  'b 

:
  'a = [ r1,  r1] = [line 2, line 3]
  'b = [ r2,  r2] = [line 4, line 5]
  'a  'b  → 

:
   'a  'b  → 
  : "cannot borrow `s` as mutable because it is also borrowed as immutable"
```

### 

```text
:

Rust :
fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
    if x.len() > y.len() { x } else { y }
}

 MIR:
fn longest(x: &str, y: &str) -> &str {
    //  'a  MIR 
    // 
    // 
}

LLVM IR:
define i8* @longest(i8* noalias %x, i64 %x_len,
                     i8* noalias %y, i64 %y_len) {
    ; 
    ;  noalias  ()
    ; 
}

:
  mov rax, rdi    ; x 
  mov rdx, rsi    ; x 
  cmp rdx, rcx    ; 
  cmovle rax, r8  ;  x  y
  ret

: 
```

### 

```rust
use std::ptr;

// Rust  vs 
let x = 42;
let r: &i32 = &x;           // 
let p: *const i32 = &x as *const i32;  // 

// : 
// : 

// unsafe 
let mut x = 42;
let r1: &mut i32 = &mut x;
let r2: &mut i32 = unsafe { &mut *(&mut x as *mut i32) };
// r1  r2  &mut i32
// ! Rust  safe 
```

---

## 

### 

: DFS — 

```rust
fn dfs(graph: &Vec<Vec<usize>>, node: usize, visited: &mut Vec<bool>) {
    visited[node] = true;
    for &next in &graph[node] {
        if !visited[next] {
            dfs(graph, next, visited);
        }
    }
}
// graph: 
// visited: 
// : graph  visited 
```
