# 

## 



****MESI CPU  L1/L2 Modified → Exclusive → Shared → Invalid 

****Memory OrderCPU x86 Load-Load, Load-Store, Store-Store Store-Load ARM ——

**** x86  `LOCK`  RMWread-modify-write`std::sync::atomic`  `Ordering::Relaxed/Acquire/Release/AcqRel/SeqCst`

**Mutex **`std::sync::Mutex`  `futex`Linux `SRWLOCK`Windows——spinlock

Rust  `Send` / `Sync` trait `Send` = `Sync` = 

---

## MESI

### 

 CPU  L1/L2  A  `x` B  `x`  `x` ——****

```text

                   (DRAM)                    
              x = 42 (address 0x1000)             

                             
           
        Core 0            Core 1   
       L1 Cache          L1 Cache  
       x = 42           x = 42   
           
          T0:  x=42

          Core 0  x = 99 

           
        Core 0                    Core 1   
       L1 Cache                  L1 Cache  
       x = 99 (Modified)         x = 42    ← 
             (Invalid) 
                                 
```

### MESI 

MESI  4 

|  |  |  |
|------|------|-------------------|
| **M** (Modified) |  |  |
| **E** (Exclusive) |  |  |
| **S** (Shared) |  |  |
| **I** (Invalid) |  | - |

```text
                      
            
      E  S                E  M      
      →  E/S            →  M    
            

        
    
      S → I       
      E → I       
      M → I        ()
    
```

**——Store Store Forwarding**L1  store buffer  x86  Store-Load ——Store  store buffer 

### Cache Line

 L1  **64 **
- `false sharing`
-  `#[repr(align(64))]`  `std::mem::size_of::<T>()` 

```rust
use std::sync::atomic::{AtomicU64, Ordering};

// 
struct BadLayout {
    reads: AtomicU64,   // offset 0..8
    writes: AtomicU64,  // offset 8..16  ← 
}

// padding 
#[repr(align(64))]
struct PaddedAtomic {
    value: AtomicU64,
    _padding: [u8; 56],
}

struct GoodLayout {
    reads: PaddedAtomic,   // 
    writes: PaddedAtomic,  // 
}
```

### MESI 

MESI **Bus Snooping**

1. **Read ** Modified 
2. **Invalidate ** Invalid
3. **Write-back**Modified 

```text
 A  x ( 0x1000):

  Core A                          Core B
                                   
     Invalidate(0x1000)   Core B  I
                                   
      x = 99                       
      (: M)                    
                                   
                                   
   Core B  x:
                                   
     Read(0x1000) 
                                   
     Data(x=99)   Core A 
      (: S)                      (: S)
```

---

## Memory Ordering

### 

CPU 

```text
 a=0, b=0:

Thread 1:                  Thread 2:
  a = 1                      b = 1
  if (b == 0) print "X"     if (a == 0) print "Y"

X  Y 
 a=1  b=1 
```

### 

```text
Relaxed:     
Acquire:     
Release:     
AcqRel:       Acquire + Release 
SeqCst:      
```

**Relaxed**

```rust
use std::sync::atomic::{AtomicI32, AtomicBool, Ordering};

static DATA: AtomicI32 = AtomicI32::new(0);
static READY: AtomicBool = AtomicBool::new(false);

// Thread 1
DATA.store(42, Ordering::Relaxed);
READY.store(true, Ordering::Relaxed);

// Thread 2
if READY.load(Ordering::Relaxed) {
    // Relaxed  DATA  READY 
    //  READY=true  DATA=0 
    let x = DATA.load(Ordering::Relaxed); // 
}
```

**Acquire/Release**Release  Acquire 

```rust
use std::sync::atomic::{AtomicBool, Ordering};
use std::sync::Arc;
use std::thread;

let data = Arc::new((AtomicBool::new(false), Vec::<i32>::new()));
let (flag, vec) = (&data.0, &data.1);

//  Mutex  Vec

// Thread 1: Producer
flag.store(true, Ordering::Release);  // Release: 

// Thread 2: Consumer
if flag.load(Ordering::Acquire) {     // Acquire:  Release 
    //  Thread 1  Release 
}
```

**SeqCst** SeqCst total order

```rust
use std::sync::atomic::{AtomicBool, AtomicI32, Ordering};
use std::thread;

let a = AtomicBool::new(false);
let b = AtomicBool::new(false);
let x = AtomicI32::new(0);
let y = AtomicI32::new(0);

// Thread 1
a.store(true, Ordering::SeqCst);
let r1 = y.load(Ordering::SeqCst);
x.store(r1, Ordering::SeqCst);

// Thread 2
b.store(true, Ordering::SeqCst);
let r2 = x.load(Ordering::SeqCst);
y.store(r2, Ordering::SeqCst);

// SeqCst  r1==0  r2==0 
//  SeqCst 
```

### x86 TSO (Total Store Order) 

x86 

```text

  Load-Load:    
  Load-Store:   
  Store-Store:  


  Store-Load:   


           x86 Store Buffer            
                                       
  Core 0:   Store x=99   Store Buf 
            Load x       Store Buf  (Store-Load )
                                       
  Core 1:    x              
            x                

```

 x86  SeqCst  Store  `LOCK`  `MFENCE`  `XCHG` 

### ARM 

ARM  Load/Store `DMB`, `DSB`, `ISB`

```text
ARM 
  DMB (Data Memory Barrier):   
  DSB (Data Synchronization Barrier): 
  ISB (Instruction Synchronization Barrier): 

Rust  Ordering::SeqCst  ARM  DMB ISH + DMB ISHLD
```

---

## 

### x86  LOCK 

 x86  RMW  `LOCK` 

```text
; fetch_add(1, SeqCst)  x86_64 
lock add dword ptr [rcx], 1

; 
; 1. LOCK 
; 2.  read-modify-write 
; 3. 

;  OrderingRelaxed LOCK 
;  x86  RMW 
```

### ARM 

ARM  `LDXR`/`STXR`Load-Exclusive/Store-Exclusive

```text
; fetch_add(1, AcqRel)  ARM64 
loop:
    ldaxr   w1, [x0]      ; Load-Acquire Exclusive
    add     w1, w1, #1
    stlxr   w2, w1, [x0]  ; Store-Release Exclusive
    cbnz    w2, loop       ; 

; Relaxed  a/l 
; ldxr + add + stxr
```

### std::sync::atomic 

```rust
use std::sync::atomic::{AtomicI32, Ordering};

let a = AtomicI32::new(0);

// Relaxed: x86  LOCK 
a.fetch_add(1, Ordering::Relaxed);

// Acquire:  load + lfence x86  load x86 Load 
a.load(Ordering::Acquire);

// Release:  store + mfence x86  store
a.store(1, Ordering::Release);

// SeqCst:  load + mfence + storex86  MFENCE
a.compare_exchange(0, 1, Ordering::SeqCst, Ordering::SeqCst);
```

---

## Mutex 

### futexFast Userspace Mutex

Linux  `futex` 

```text

              Mutex                     
                                           
  Unlocked (0):                        
  Locked (1):                  
  Locked + 2:                  


:
  1. CAS(0 → 1): 
  2.  → spinlock
  3.  → futex(FUTEX_WAIT): 
  4.  →  CAS

:
  1. AtomicStore(0): 
  2.  → futex(FUTEX_WAKE): 
```

### RwLock 

`RwLock` 

```text

              RwLock                    
                                               
  : Writer Count ()              
  : Reader Count ()              
                                               
  lock():                                      
    : Reader Count += 1                  
    : CAS(0 → 1),         
                                               
  -:                             
    :            
    :         

```

### Arc<Mutex<T>> 

 Rust 

```rust
use std::sync::{Arc, Mutex};
use std::thread;

// Arc (Atomic Reference Count) — 
// Mutex<T> — 

let shared = Arc::new(Mutex::new(Vec::new()));

let mut handles = vec![];
for i in 0..10 {
    let shared = Arc::clone(&shared);  // Arc::clone 
    handles.push(thread::spawn(move || {
        let mut guard = shared.lock().unwrap();  //  MutexGuardRAII
        guard.push(i);
        // MutexGuard  drop
    }));
}

for h in handles { h.join().unwrap(); }

let data = shared.lock().unwrap();
assert_eq!(data.len(), 10);
```

### Mutex vs RwLock 

```text
:

                  Mutex         RwLock      

  (99% )                                   
                                  ()    
  (50/50)                                
                                          
               ()                     


:
  -  (>90% ): RwLock 
  - : Mutex 
  - : Mutex  ( RwLock )
  - : Mutex  (RwLock )
```

---

## Send + Sync 

### 

```text
Send:   T 
Sync:  &T 


  T: Send  ⇒  &T: Sync   T  &T 
  T: Sync  ⇒  &T: Send  

 Send/Sync:
  - Rc<T>: !Send ()
  - Rc<T>: !Sync ()
  - Cell<T>: !Sync ()
  - RefCell<T>: !Sync ()
```

### 

```text
 T  Send T  Send
 T  Sync T  Sync

:
  Arc<T>:  Send ()
  Arc<T>:  Sync ( T: Send + Sync)
  Mutex<T>: Send ()
  Mutex<T>: Sync ( T: Send)  ← T  Send  Sync
  RwLock<T>: Send + Sync ( T: Send + Sync)
```

###  Send/Sync

```rust
use std::marker::PhantomData;
use std::rc::Rc;

// Rc<T>  Send
//  unsafe impl

struct MyWrapper<T> {
    inner: Rc<T>,
}

//  unsafe  Rc<T> 
// 
// unsafe impl<T: Send> Send for MyWrapper<T> {}
// ← 

//  Arc<T> 
struct SafeWrapper<T> {
    inner: std::sync::Arc<T>,
}
// Arc<T>  Send + Sync
```

### PhantomData  Send/Sync

```rust
use std::marker::PhantomData;

// PhantomData<T> "" T
//  Send/Sync

struct MyType {
    _marker: PhantomData<*const ()>,  // *const ()  Send/Sync
}

//  PhantomData<*const ()>MyType  !Send + !Sync
//  FFI  C 
```

---

##  C pthreads 

### API 

```c
// C pthreads
#include <pthread.h>

pthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;

void* worker(void* arg) {
    pthread_mutex_lock(&mutex);
    // 
    pthread_mutex_unlock(&mutex);
    return NULL;
}

int main() {
    pthread_t threads[10];
    for (int i = 0; i < 10; i++) {
        pthread_create(&threads[i], NULL, worker, NULL);
    }
    for (int i = 0; i < 10; i++) {
        pthread_join(threads[i], NULL);
    }
}
```

```rust
// Rust equivalent
use std::sync::{Arc, Mutex};
use std::thread;

fn main() {
    let mutex = Arc::new(Mutex::new(0));
    let mut handles = vec![];

    for _ in 0..10 {
        let mutex = Arc::clone(&mutex);
        handles.push(thread::spawn(move || {
            let mut guard = mutex.lock().unwrap();
            // 
            // MutexGuard  drop  — 
        }));
    }

    for h in handles { h.join().unwrap(); }
}
```

### 

```text

                     C pthreads        Rust std       

                                    
                               
                                  
                 void*                  
                        RAII    
                           API  
                             +  

```

### RAII 

```rust
use std::sync::Mutex;

fn example() {
    let m = Mutex::new(42);

    {
        let guard = m.lock().unwrap();
        //  *guard 
        // /panic
        // MutexGuard  drop
    }
    // 

    // lock()  panic  PoisonError
    //  .unwrap_or_else(|e| e.into_inner()) 
    let val = m.lock().unwrap_or_else(|poisoned| {
        poisoned.into_inner() // 
    });
}
```

---

##  vs 

### Data Race

```text



  1. 
  2. 
  3. Mutex, Atomic, Channel 

Rust 
  - 
  - Send/Sync 
```

### Race Condition

```text


 ≠ 


  - 
  - 

TOCTOU (Time of Check to Time of Use)
```

```rust
use std::sync::{Arc, Mutex};
use std::thread;

let balance = Arc::new(Mutex::new(1000));

// 
// 
for _ in 0..2 {
    let balance = Arc::clone(&balance);
    thread::spawn(move || {
        let b = balance.lock().unwrap();
        if *b >= 500 {
            // 
            drop(b); // 
            let mut b = balance.lock().unwrap();
            *b -= 500; //  0  500
        }
    });
}

// 
```

---

## 

### Channel 

```rust
use std::sync::mpsc;
use std::thread;

// mpsc: Multiple Producer, Single Consumer
let (tx, rx) = mpsc::channel();

// 
for i in 0..5 {
    let tx = tx.clone();
    thread::spawn(move || {
        tx.send(i).unwrap();
    });
}
drop(tx); // 

// 
while let Ok(val) = rx.recv() {
    println!("received: {}", val);
}
```

### scoped threads

```rust
use std::thread;

let mut data = vec![1, 2, 3, 4, 5];

// scoped threads 
//  join 
thread::scope(|s| {
    let half = &mut data[0..2];
    let other_half = &mut data[2..];

    s.spawn(|| {
        for x in half.iter_mut() { *x *= 2; }
    });
    s.spawn(|| {
        for x in other_half.iter_mut() { *x *= 3; }
    });
}); //  join

println!("{:?}", data); // [2, 4, 9, 12, 15]
```

### Once — 

```rust
use std::sync::Once;
use std::sync::OnceLock;

static INIT: Once = Once::new();
static mut VALUE: Option<String> = None;

// 
fn get_value() -> &'static str {
    unsafe {
        INIT.call_once(|| {
            VALUE = Some(String::from("initialized"));
        });
        VALUE.as_ref().unwrap()
    }
}

//  OnceLockRust 1.70+
static VALUE: OnceLock<String> = OnceLock::new();

fn get_value_safe() -> &'static str {
    VALUE.get_or_init(|| String::from("initialized"))
}
```

---

## 

### 

```text
Coffman 
  1. Mutual Exclusion
  2. Hold and Wait
  3. No Preemption
  4. Circular Wait


  - 
  - 
  -  try_lock 
  -  Rust 
```

### 

```bash
# ThreadSanitizer (TSan) — 
RUSTFLAGS="-Z sanitizer=thread" cargo run

# Valgrind + Helgrind — 
valgrind --tool=helgrind ./target/debug/my_program

# MIRI —  MIR
cargo +nightly miri test
```

---

## 

```rust
use std::thread;
use std::sync::{Arc, Mutex};
use std::sync::atomic::{AtomicI32, Ordering};

// 
let handle = thread::spawn(|| { println!("worker"); });
handle.join().unwrap();

// Arc + Mutex — 
let counter = Arc::new(Mutex::new(0));
let mut handles = vec![];
for _ in 0..10 {
 let c = Arc::clone(&counter);
 handles.push(thread::spawn(move || {
 let mut n = c.lock().unwrap();
 *n += 1;
 }));
}
for h in handles { h.join().unwrap(); }

// 
let a = AtomicI32::new(0);
a.fetch_add(1, Ordering::SeqCst);

// mpsc 
use std::sync::mpsc;
let (tx, rx) = mpsc::channel();
thread::spawn(move || { tx.send(42).unwrap(); });
assert_eq!(rx.recv().unwrap(), 42);
```

### 

| Ordering |  |  |
|----------|------|------|
| `Relaxed` |  |  |
| `Acquire/Release` |  |  |
| `AcqRel` |  |  |
| `SeqCst` |  |  |

---

## 

### 

:  — 
