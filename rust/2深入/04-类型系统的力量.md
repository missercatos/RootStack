# 

## 

### Hindley-Milner 

Rust  Hindley-MilnerHMlifetime subtypingtrait unification`Vec::<_>`  `push(42i32)`  `?0 = i32`

****

- **Hindley-MilnerHM** Roger Hindley (1969)  Robin Milner (1978) ""——
- **Unification**"" `?T`  `i32`  `?T = i32`
- **Type Variable** `?T`
- **Monomorphization**

```text
HM  (Algorithm W):

:  e
:  S  T

W(Γ, x) = (id, lookup(Γ, x))           // 
W(Γ, λx.e) = (S, S(τ) → T)            // lambda
              (S1, T1) = W(Γ ∪ {x: ?α}, e)
W(Γ, e1 e2) = (S2 ∘ S1, S2(T1))       // 
              (S1, T1) = W(Γ, e1)
                   (S2, T2) = W(S1(Γ), e2)
                   S3 = unify(T1, T2 → ?β)

:
unify(τ1, τ2) = {
    if τ1 == τ2: return id                    // 
    if τ1 is type var: return bind(τ1, τ2)    // 
    if τ2 is type var: return bind(τ2, τ1)
    if τ1 = T1→T2, τ2 = T3→T4:
        S1 = unify(T1, T3)
        S2 = unify(S1(T2), S1(T4))
        return S2 ∘ S1
}

Rust :
  -  trait : T: Display
  - : &'a T
  - : &'a T <: &'b T ( 'a: 'b)
```

### Curry-Howard 

Curry-Howard  =  = 

|  |  | Rust  |
|------|------|-----------|
| $(A \land B)$ |  `(A, B)` | `(i32, String)` |
| $(A \lor B)$ |  | `Result<T, E>` |
| $(A \to B)$ |  `fn(A) -> B` | `fn f(x: i32) -> bool` |
| $\forall x. P(x)$ |  `for<T>` | `fn foo<T>(x: T)` |
| $\exists x. P(x)$ |  | `dyn Trait` |
| $\perp$ () | Never  | `!` |
| $\top$ () | Unit  | `()` |

```text
Curry-Howard :

 (Product Type):
  : A ∧ B (A  B)
  : (A, B)  struct { a: A, b: B }
  :  A  B 
  Rust: let pair: (i32, bool) = (42, true);

 (Sum Type):
  : A ∨ B (A  B)
  : enum { Left(A), Right(B) }
  :  A  B 
  Rust: let result: Result<i32, String> = Ok(42);

:
  : A → B ( A  B)
  : fn(A) -> B
  :  A  B 
  Rust: fn add_one(x: i32) -> i32 { x + 1 }

 (Universal Quantification):
  : ∀x. P(x) ( xP(x) )
  : for<T> fn(T) -> P<T>
  :  T P<T> 
  Rust: fn identity<T>(x: T) -> T { x }

 (Existential Quantification):
  : ∃x. P(x) ( x  P(x) )
  : dyn Trait ()
  : 
  Rust: let d: &dyn Display = &42; //  T: Display

 (Bottom Type):
  : ⊥ ()
  : ! (Never )
  :  (ex falso quodlibet)
  Rust: let x: i32 = panic!(); // panic!  !

 (Top Type):
  : ⊤ ()
  : () (Unit )
  :  ()
  Rust: let u: () = ();
```

###  (Monomorphization)

—— C++  monomorphization  Rust 

```text
:

Rust :
fn add<T: std::ops::Add<Output=T>>(a: T, b: T) -> T {
    a + b
}

let x = add(1i32, 2i32);
let y = add(1.0f64, 2.0f64);

:
fn add_i32(a: i32, b: i32) -> i32 { a + b }
fn add_f64(a: f64, b: f64) -> f64 { a + b }

let x = add_i32(1, 2);
let y = add_f64(1.0, 2.0);

:
  .text:
  add_i32:
      lea eax, [rdi + rsi]   ; 
      ret
  add_f64:
      addsd xmm0, xmm1      ; 
      ret

:
   ():  → 
   (trait object):  →  vtable 
```

### Trait 

```text
Trait :

:
fn print_all<T: Display + Debug>(items: &[T]) {
    for item in items {
        println!("{:?} {}", item, item);
    }
}

:
  1.  T : [Display, Debug]
  2. :
     - Display →  fmt 
     - Debug →  fmt 
  3.  ()

LLVM IR ():
  define void @print_all_i32(i32* %items, i64 %len) {
      ; T = i32 
      ; i32  Display  Debug
      ;  fmt 
  }

  define void @print_all_string({ ptr, i64, i64 }* %items, i64 %len) {
      ; T = String 
      ; String  Display  Debug
  }
```

### 

```rust
// :  Item 
trait Iterator {
    type Item;  // 
    fn next(&mut self) -> Option<Self::Item>;
}

// : 
trait Convertible<T> {
    fn convert(&self) -> T;
}

// i32  f64  String
impl Convertible<f64> for i32 {
    fn convert(&self) -> f64 { *self as f64 }
}
impl Convertible<String> for i32 {
    fn convert(&self) -> String { self.to_string() }
}

//  vs :
// - : "" → Iterator
// - :  → Convertible
```

### Newtype 

```rust
// Newtype: 
struct Meters(f64);
struct Seconds(f64);

fn speed(distance: Meters, time: Seconds) -> f64 {
    distance.0 / time.0
}

// speed(Meters(100.0), Seconds(9.58)) // OK
// speed(Seconds(9.58), Meters(100.0)) // : 

// Newtype :
// 1. : 
// 2. : 
// 3.  newtype  trait
// 4.  (orphan rule)
```

### Phantom Types ()

```rust
use std::marker::PhantomData;

// : 
struct QrCode<Data> {
    data: Vec<u8>,
    _marker: PhantomData<Data>,  // 
}

struct RawData;
struct ValidatedData;

impl QrCode<RawData> {
    fn new(data: Vec<u8>) -> Self {
        QrCode { data, _marker: PhantomData }
    }
}

impl QrCode<RawData> {
    fn validate(self) -> Result<QrCode<ValidatedData>, String> {
        if self.data.is_empty() {
            Err("empty data".to_string())
        } else {
            Ok(QrCode { data: self.data, _marker: PhantomData })
        }
    }
}

impl QrCode<ValidatedData> {
    fn render(&self) -> String {
        //  QR 
        "QR code rendered".to_string()
    }
}

// :  QR 
// let qr = QrCode::new(vec![1, 2, 3]);
// qr.render();  // : QrCode<RawData>  render 
// let qr = qr.validate().unwrap();
// qr.render();  // OK: QrCode<ValidatedData>  render 
```

### Typestate Pattern ()

```rust
use std::marker::PhantomData;

// : 
struct Door<State> {
    _marker: PhantomData<State>,
}

struct Closed;
struct Open;

impl Door<Closed> {
    fn new() -> Self {
        println!("");
        Door { _marker: PhantomData }
    }

    fn open(self) -> Door<Open> {
        println!("");
        Door { _marker: PhantomData }
    }
}

impl Door<Open> {
    fn close(self) -> Door<Closed> {
        println!("");
        Door { _marker: PhantomData }
    }
}

// :
//   Closed → open() → Open
//   Open → close() → Closed
//
// : 
// let door = Door::<Closed>::new();
// door.close();  // : Closed  close 
// let door = door.open();
// door.open();   // : Open  open 
// let door = door.close();
```

###  (ZST)

```rust
use std::mem;

// :  0 
struct Marker;  // 
enum Void {}    //  (never type )

assert_eq!(mem::size_of::<Marker>(), 0);
assert_eq!(mem::size_of::<()>(), 0);  // Unit 
assert_eq!(mem::size_of::<[i32; 0]>(), 0);  // 

// ZST :
// 1.  (PhantomData )
// 2. : 
// 3.  trait: Send, Sync, Sized  ZST

// Vec<ZST> :
// Vec<()>  0
let v: Vec<()> = vec![(), (), ()];
println!("len: {}", v.len());  // 3
println!("capacity: {}", v.capacity());  //  > 3
//  0 !
```

---

## 

### 

```rust
let v: Vec<i32> = vec![]; //  vec![] 
let x: i32 = "42".parse().unwrap(); // parse 
```

### Never  `!`

```rust
fn exit() -> ! { std::process::exit(1); } // 
// !  ⊥ 

let x: i32 = loop { break 42; }; // loop  break 
```

### ""

```rust
use std::mem;
println!("{}", mem::size_of_val(&[1, 2, 3])); // &[i32] = 16B (ptr+len)
```

### 

```rust
trait Iterator {
 type Item; // 
 fn next(&mut self) -> Option<Self::Item>;
}
```

### Trait 

```rust
let v: Vec<Box<dyn Display>> = vec![Box::new(1), Box::new("hi")];
//  vtable  fmt 
```

### 

```rust
// 

//  1: 
struct GameBuilder<HasName: bool, HasAge: bool> {
    name: Option<String>,
    age: Option<u32>,
    _marker: std::marker::PhantomData<(HasName, HasAge)>,
}

//  2: 
struct Email(String);
struct Phone(String);

fn send_email(email: Email) { /* ... */ }
fn send_sms(phone: Phone) { /* ... */ }

// let email = Email("user@example.com".to_string());
// let phone = Phone("+1234567890".to_string());
// send_email(phone);  // : 
// send_sms(email);    // : 
```

---

## 

### 

```text
Rust :

1. :
   let x = 5;                    // ?T = i32 ( i32)
   let v: Vec<i32> = vec![];    // Vec<?U> = Vec<i32> → ?U = i32
   v.push(x);                    // Vec<i32>::push(i32) → 

2.  (Unification):
   
   ?T = i32
   ?U = i32

3. :
   let x = "42".parse().unwrap();
   // parse::<?T>() → ?T
   // 

4. :
   fn foo<T: Display>(x: T) { ... }
   foo(42);  // i32: Display → 

:
   let x = vec![];
   // vec![]  Vec<?T>
   //  ?T
   // : "type annotations needed"
```

### 

```text
:

:
fn process<T: Display>(items: &[T]) { ... }

:
process(&[1i32, 2, 3]);      //  process_i32
process(&[1.0f64, 2.0, 3.0]); //  process_f64
process(&["a", "b", "c"]);    //  process_str

:
  
  

Rust :
  1. : 
  2. : 
  3. LTO (Link-Time Optimization):  crate 

:
  C++ : 
  Rust : 
```

---

## 

### 

:  — 

```rust
// 
fn dijkstra<T, F>(graph: &Vec<Vec<(usize, T)>>, start: usize, dist_fn: F) -> Vec<T>
where
    T: Copy + Ord + std::ops::Add<Output=T> + Default,
    F: Fn(usize, usize) -> T,
{
    let n = graph.len();
    let mut dist = vec![T::default(); n];
    // ... Dijkstra 
    dist
}
```
