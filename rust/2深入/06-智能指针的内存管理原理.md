# 

## 

### Box\<T\>

`Box<T>`  C++  `unique_ptr` 8B  T`Box`  drop  `T::drop` `__rust_dealloc`

****

- **RAIIResource Acquisition Is Initialization**C++  Rust ——
- **Heap Allocator** glibc  `malloc`/`free`  Rust  `__rust_alloc`/`__rust_dealloc`
- **Reference Counting**
- **Atomic Operation** CPU 
- **Copy-on-WriteCOW**

```text
Box<T> :

 (8 ):                :
          
 ptr (8 bytes)   → T           
           (size of T)       
                             

Box::new(42) :
  1.  __rust_alloc(size_of::<i32>(), align_of::<i32>())
     →  4 4 
  2.  42 
  3. 

Box::drop :
  1.  <i32 as Drop>::drop(ptr)  // T 
  2.  __rust_dealloc(ptr, size_of::<i32>(), align_of::<i32>())
     →  4 

:
  ; Box::new(42)
  mov     edi, 4             ; size = 4
  mov     esi, 4             ; align = 4
  call    __rust_alloc       ; 
  mov     dword [rax], 42   ; 
  mov     [rbp-8], rax      ; 

  ; Box::drop
  mov     rdi, [rbp-8]       ; 
  call    <i32 as Drop>::drop ;  ( i32 )
  mov     rdi, [rbp-8]       ; 
  mov     esi, 4             ; size
  mov     edx, 4             ; align
  call    __rust_dealloc     ; 
```

### Rc\<T\>

`Rc<T>` `Cell<usize>`  {T, strong_count, weak_count}`clone` `drop`  T`Rc` `!Send + !Sync`

```text
Rc<T> :

:                        :
         
 ptr (8 bytes)   → RcBox {              
            strong: usize (8B) 
                               weak:   usize (8B) 
                               data:   T          
                             }                    
                            

:

Rc::clone(&rc):
  ;  ( Rc  !Send + !Sync)
  mov     rax, [rc_ptr]           ; rax = &RcBox
  mov     rcx, [rax]              ; rcx = strong_count
  add     rcx, 1                  ; strong_count += 1
  mov     [rax], rcx              ; 
  ;  LOCK 
  ;  Rc 

Rc::drop(&rc):
  mov     rax, [rc_ptr]
  mov     rcx, [rax]
  sub     rcx, 1                  ; strong_count -= 1
  mov     [rax], rcx
  cmp     rcx, 0
  jne     .skip_free              ;  0
  call    <T as Drop>::drop       ;  T
  call    __rust_dealloc          ;  RcBox
.skip_free:

Weak :
  weak_count 
   strong_count :
    -  T 
    -  RcBox ( weak )
   weak_count :
    -  RcBox
```

### Arc\<T\>

`Arc<T>` `AtomicUsize` `fetch_add` / `fetch_sub`  `LOCK` x86

```text
Arc<T> :

 Rc<T>  strong/weak  AtomicUsize:

:

 ArcBox {                 
   strong: AtomicUsize      ← 
   weak:   AtomicUsize      ← 
   data:   T              
 }                        


 (x86_64):

Arc::clone:
  lock inc qword [rax]     ; LOCK  + 
  ; :
  ; lock xadd [rax], rcx   ; 

Arc::drop:
  lock dec qword [rax]     ; LOCK  + 
  jnz .skip_free           ;  0
  ;  (acquire + release)

:
   (Rc): ~1-2 CPU 
   (Arc):  ~10-30 CPU  ()
  LOCK :    CPU 
  :     (acquire/release semantics)
```

### RefCell\<T\>

`RefCell<T>`  `Cell<isize>`  borrow count`borrow()` +1`borrow_mut()`  -1 `panic!` 

```text
RefCell<T> :

:

 ptr (8 bytes)   → :
         
                             RefCellBox {      
                               borrow: Cell    
                                 <isize>       
                               value: T        
                             }                 
                            

borrow :
  borrow :
    -1        →  (borrow_mut)
    0         → 
    1, 2, ... →  N 

borrow() :
  let r = cell.borrow();
  ; 1.  borrow 
  ; 2. : borrow != -1 ()
  ; 3. borrow += 1
  ; 4.  Ref<T> (RAII )
  ;  borrow == -1 → panic!("already borrowed")

borrow_mut() :
  let mut r = cell.borrow_mut();
  ; 1.  borrow 
  ; 2. : borrow == 0 ()
  ; 3. borrow = -1
  ; 4.  RefMut<T> (RAII )
  ;  borrow != 0 → panic!("already borrowed")

Ref<T>  RefMut<T>  drop:
   drop :
    Ref::drop → borrow -= 1 ()
    RefMut::drop → borrow = 0 ()

:
  :  +  +  ( 2-5 CPU )
  : 
```

### Cow\<T\>

`Cow<'a, T>` `&T`  `T` 

```text
Cow<'a, T> :

enum Cow<'a, T: ToOwned> {
    Borrowed(&'a T),    // 
    Owned(T::Owned),    // 
}

:
  Borrowed :
    
     : Borrowed (0)   
      (8B) →  
    

  Owned :
    
     : Owned (1)      
     : T::Owned         ← 
    

:
  let s: Cow<str> = Cow::Borrowed("hello");
  // 

  let mut s = s.into_owned();
  // 
  s.push_str(" world");

  :
    - : 
    - :  ()
```

### Pin\<T\>

`Pin<T>` 

```text
:

struct SelfRef {
    data: String,
    ptr: *const String,  //  data
}

impl SelfRef {
    fn new(s: String) -> Self {
        let mut sr = SelfRef { data: s, ptr: std::ptr::null() };
        sr.ptr = &sr.data as *const String;
        sr
    }
}

:
  let sr = SelfRef::new("hello".to_string());
  let sr2 = sr;  // ! sr2.data 
  // sr2.ptr  → !

Pin :
  let sr = Box::pin(SelfRef::new("hello".to_string()));
  // Pin<Box<SelfRef>>  SelfRef 
  // 

  // :
  // - std::mem::replace
  // - std::mem::swap
  // -  (destructuring)
  // -  Pin<&mut T>  &mut T ( Unpin)
```

### Send  Sync traits

```text
Send  Sync :

unsafe trait Send { }
//  T: Send T 

unsafe trait Sync { }
//  T: Sync &T 

:
   Send  Sync
  :
    - *const T, *mut T ()
    - Rc<T> ()
    - Cell<T>, RefCell<T> ()

:
  Box<T>:   Send ( T: Send)
            Sync ( T: Sync)
  Rc<T>:    !Send, !Sync
  Arc<T>:   Send ( T: Send + Sync)
            Sync ( T: Send + Sync)
  RefCell<T>: Send ( T: Send)
              !Sync ()
  Mutex<T>: Send + Sync ( T: Send)
  RwLock<T>: Send + Sync ( T: Send + Sync)

:
  // Rc<T> 
  use std::rc::Rc;
  let rc = Rc::new(42);
  std::thread::spawn(move || {
      println!("{}", rc);  //  : Rc<i32> 
  });

  // Arc<T> 
  use std::sync::Arc;
  let arc = Arc::new(42);
  let arc2 = Arc::clone(&arc);
  std::thread::spawn(move || {
      println!("{}", arc2);  //  OK: Arc<i32> 
  });
```

---

## 

```rust
// Box — 
let b = Box::new(42);
let raw = Box::into_raw(b); //  Box
unsafe { Box::from_raw(raw); } //  Box

// Rc — 
use std::rc::Rc;
let r = Rc::new(String::from("hi"));
let r2 = Rc::clone(&r); // +1
assert_eq!(Rc::strong_count(&r), 2);

// Arc — 
use std::sync::Arc;
let a = Arc::new(42);
let a2 = Arc::clone(&a);

// RefCell — 
use std::cell::RefCell;
let c = RefCell::new(42);
*c.borrow_mut() = 100; // 
let v = *c.borrow(); // 
// c.borrow_mut(); // panic! 

// Cow — 
use std::borrow::Cow;
let mut cow = Cow::Borrowed("hello");
cow.to_mut().push_str(" world"); // 
```

### 

| | Box\<T\> | Rc\<T\> | Arc\<T\> | RefCell\<T\> |
|--|--------|-------|--------|------------|
|  |  |  |  | + |
|  |  | () | () |  |
|  | `Send+Sync` | `!Send+!Sync` | `Send+Sync` | `Send+!Sync` |
|  |  | (RefCell) | (Mutex) | (runtime) |

### 

```rust
use std::cell::RefCell;
use std::rc::Rc;

// Rc<RefCell<T>>:  + 
let shared = Rc::new(RefCell::new(vec![1, 2, 3]));
let shared2 = Rc::clone(&shared);

shared.borrow_mut().push(4);
println!("{:?}", shared2.borrow()); // [1, 2, 3, 4]

use std::sync::{Arc, Mutex};

// Arc<Mutex<T>>:  + 
let shared = Arc::new(Mutex::new(vec![1, 2, 3]));
let shared2 = Arc::clone(&shared);

std::thread::spawn(move || {
    shared2.lock().unwrap().push(4);
});
```

---

## 

###  drop 

```text
 drop :

:  drop

let a = Box::new(1);
let b = Rc::new(2);
let c = Arc::new(3);
// drop : c, b, a ()

:
let x = Rc::new(RefCell::new(Box::new(String::from("hello"))));
// drop :
// 1. Rc::drop → strong_count -= 1
// 2.  strong_count == 0:
//    a. RefCell::drop ()
//    b. Box::drop → 
//    c. String::drop → 
//    d.  RcBox
```

### 

```text
:

Box<T>:
  : 8  (x86_64)
  : align_of::<T>()

Rc<T> / Arc<T>:
  : 8 
  RcBox : max(align_of::<usize>(), align_of::<T>())
  //  usize 

RefCell<T>:
  : 8 
  RefCellBox : max(align_of::<isize>(), align_of::<T>())
  // borrow  isize 

 (Rc<i32>):
  :                    :
            
   ptr (8B) →  strong: 1 (8B)      
             weak: 0 (8B)        
                         data: 42 (4B)       
                         padding: 4B           ← 
                        
  : 24  (8 + 8 + 4 + 4)
```

### 

```text
 ():

             Box      Rc       Arc      RefCell

             ~100ns   ~100ns   ~100ns   ~10ns
             N/A      ~2ns     ~10ns    N/A
 ()    ~0ns     ~0ns     ~0ns     ~2ns
 ()      ~0ns     N/A      N/A      ~2ns
Drop             ~2ns     ~2ns     ~10ns    ~0ns

: 

:
  - Box:  ( malloc)
  - Rc: 
  - Arc: 
  - RefCell: 
```

---

## 

### 

:  — Box + RefCell

```rust
use std::cell::RefCell;
use std::rc::Rc;

struct TreeNode {
    val: i32,
    left: Option<Rc<RefCell<TreeNode>>>,
    right: Option<Rc<RefCell<TreeNode>>>,
}

// Rc<RefCell<TreeNode>> :
// 1.  (Rc)
// 2.  (RefCell)
// 3.  (!Send + !Sync)
```
