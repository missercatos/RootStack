# 

## 

Rust 

****`macro_rules!` +  token AST  token  token  pass

****Proc MacroRust  Rust  `#[derive]`  `#[attr]` `.so` TokenStream TokenStream

macro arm  token `$pat:pat` fragment specifier`$(...)*` token  `macro_rules!` 


- `#[derive(Trait)]` —  trait 
- `#[attr]` — 
- `fn-like` — 

 token  proc-macro crate

---

## macro_rules!

### Token Token Tree

```text
Rust  token :

Token :
  -  token (identifier, keyword, literal, punctuation)
  -  token :
    - ( ... ) — 
    - [ ... ] — 
    - { ... } — 

:
  vec![1, 2, 3]

  Token :
    [Ident(vec)] [!] [LParen] [  [IntLit(1)] [,] [IntLit(2)] [,] [IntLit(3)]  ] [RParen]

  vec  token :
    [IntLit(1)] [,] [IntLit(2)] [,] [IntLit(3)]
```

### Fragment Specifier

```text
$:

 token :


                                    

 expr       (1 + 2, "hello", x, if ... {})   
 ty         (i32, String, Vec<T>)              
 pat        (x, (a, b), Some(_), 1..=5)        
 stmt       (let x = 1;, x.push(1);)           
 item       (fn, struct, impl, mod, ...)          
 block      ({ ... })                             
 ident      ()            
 literal    (1, "hello", true)                
 tt         token  ()              
 lifetime   ('a, 'static)                   
 meta       (derive(Debug), allow(unused))  
 vis        (pub, pub(crate), ...)      
 path       (std::vec::Vec, crate::foo)         


:
  - expr  if  tt
  - tt  token 
  -  $crate  crate
```

### Repetition

```text
$(...)*  — 
$(...)+  — 
$(...)?  — 

:
  $( $x:expr ),*     — 
  $( $x:expr );+     — 
  $( $x:expr ),+ ,   — 

:
  $( $( $x:expr ),* ),*
  : 1, 2; 3, 4, 5; 6

:
  $( v.push($x); )*  —  $x 
  $crate::$func($($arg),*)  — 
```

### 

```rust
// 
macro_rules! count {
    () => (0usize);
    ($head:tt $($tail:tt)*) => (1usize + count!($($tail)*));
}

assert_eq!(count!(), 0);
assert_eq!(count!(a), 1);
assert_eq!(count!(a b c), 3);
```

### Hygiene

```text
Rust :

:
  - 
  -  hygienic expansion "context"

  macro_rules! make_func {
      ($name:ident) => {
          fn $name() {
              let x = 1;  //  x  macro 
              println!("{}", x);
          }
      }
  }

  let x = 42;
  make_func!(my_func);
  my_func();  //  1 42
  //  x  x 

:
  -  $crate 
  - 
  - 
```

### macro_rules! 

```text
macro_rules! :

 (.rs)
    
    
Lexer → Token 
    
    
macro_rules!  ←  pass
                        ()
    
AST 
    
    
... ()

: macro_rules!  AST 
      
      : vec![...]  { let mut v = Vec::new(); ... }
```

---

## Proc Macro

### 

```text
:

1. Derive  — #[derive(MyTrait)]
   - / trait 
   - : /
   - : impl MyTrait for ... { ... }

2.  — #[my_attribute]
   - 
   - 
   - :  + 
   - : 

3.  — my_macro!(...)
   -  Rust 
   - : TokenStream
   - : TokenStream
```

### 

```text
:

1.  proc-macro crate
   -  (.so / .dylib / .dll)
   - : fn(TokenStream) -> TokenStream

2.  crate
   -  proc-macro 
   -  #[derive(X)] :
     a.  X  proc-macro 
     b.  TokenStream 
     c.  TokenStream
     d. 

3. :
   - /
   - 
   - 
   -  syn::Error
```

### quote!  parse

```rust
// quote!  —  Rust 
//  #variable 

use quote::quote;
use syn;

let name = syn::Ident::new("MyStruct", proc_macro2::Span::call_site());
let fields = vec![
    syn::Ident::new("field1", proc_macro2::Span::call_site()),
    syn::Ident::new("field2", proc_macro2::Span::call_site()),
];

let expanded = quote! {
    impl MyTrait for #name {
        fn method(&self) -> i32 {
            #(#fields)*  // 
            0
        }
    }
};
// expanded  TokenStream
```

```rust
// syn —  TokenStream  Rust 

use syn::{parse_macro_input, DeriveInput};

#[proc_macro_derive(MyTrait)]
pub fn my_trait_derive(input: proc_macro::TokenStream) -> proc_macro::TokenStream {
    // 
    let input = parse_macro_input!(input as DeriveInput);

    // 
    let name = &input.ident;

    // 
    let fields = match &input.data {
        syn::Data::Struct(data) => &data.fields,
        _ => panic!("MyTrait can only be derived for structs"),
    };

    // 
    let expanded = quote! {
        impl MyTrait for #name {
            fn method(&self) -> i32 {
                0
            }
        }
    };

    expanded.into()
}
```

### 

```rust
use syn::{parse_macro_input, DeriveInput};

#[proc_macro_derive(MyTrait)]
pub fn my_trait_derive(input: proc_macro::TokenStream) -> proc_macro::TokenStream {
    let input = parse_macro_input!(input as DeriveInput);

    // 
    match &input.data {
        syn::Data::Struct(_) => {},
        _ => {
            // 
            return syn::Error::new(
                input.ident.span(),
                "MyTrait can only be derived for structs"
            ).to_compile_error().into();
        }
    };

    // ... 
}
```

---

## cfg(test) 

### 

```rust
// cfg 
#[cfg(target_os = "linux")]
fn linux_only() {}

#[cfg(target_os = "windows")]
fn windows_only() {}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}

// cfg! 
if cfg!(target_os = "linux") {
    println!("Running on Linux");
}

//  cfg 
// RUSTFLAGS='--cfg my_feature' cargo build
#[cfg(my_feature)]
fn feature_flagged() {}
```

### cfg 

```rust
//  cfg 
#[cfg(all(target_os = "linux", target_arch = "x86_64"))]
fn linux_x86_64_only() {}

#[cfg(any(target_os = "linux", target_os = "macos"))]
fn unix_only() {}

#[cfg(not(target_os = "windows"))]
fn not_windows() {}

#[cfg(all(feature = "serde", not(feature = "no-serde")))]
fn with_serde() {}
```

---

##  C 

### C 

```c
// C  — 
#define MAX(a, b) ((a) > (b) ? (a) : (b))

//  1: 
int x = 5, y = 3;
MAX(x++, y);  //  ((x++) > (y) ? (x++) : (y))
               // x 

//  2: 
MAX(1, 2.5);  // 

//  3: 
#define SQUARE(x) x * x
SQUARE(1 + 2);  //  1 + 2 * 1 + 2 = 5 ( 9)
```

### Rust 

```rust
// Rust  — token 
macro_rules! max {
    ($a:expr, $b:expr) => {
        if $a > $b { $a } else { $b }
    };
}

//  1: $a:expr 
max!(x + 1, y);  // 

//  2: 
macro_rules! vec_of {
    ($($x:expr),* $(,)?) => {
        {
            let mut v = Vec::new();
            $( v.push($x); )*
            v
        }
    };
}

//  3: 
macro_rules! nested {
    ($head:expr, $($tail:expr),*) => {
        //  $head $tail
    };
    ($single:expr) => {
        // 
    };
}
```

### 

```text

                   C              Rust         

                       Token      
                               
                                           
                                  
                    cargo expand 
                                           
                                        
                                           
                                  
                                           

```

---

## 

### Internal Rules

```rust
// 
macro_rules! my_vec {
    //  — 
    ($($x:expr),* $(,)?) => {
        my_vec!(@internal $($x),*)
    };

    //  — 
    (@internal) => {
        Vec::new()
    };
    (@internal $head:expr $(, $tail:expr)*) => {
        {
            let mut v = Vec::new();
            v.push($head);
            $( v.push($tail); )*
            v
        }
    };
}
```

### TT Muncher 

```rust
// TT Muncher:  token 
macro_rules! print_list {
    // 
    () => {};

    // 
    ($head:tt $($tail:tt)*) => {
        println!("{}", $head);
        print_list!($($tail)*);
    };
}

print_list!(1 2 3 4);
// : 1 2 3 4 ()
```

### Push-down Accumulation 

```rust
// Push-down Accumulation: 
macro_rules! accumulate {
    // 
    ($($visited:tt)*) => {};

    // 
    ($head:tt $($tail:tt)*) => {
        accumulate!(@ accum ($head) $($tail)*);
    };

    // 
    (@ accum ($($accum:tt)*) $head:tt $($tail:tt)*) => {
        accumulate!(@ accum ($($accum)* $head) $($tail)*);
    };

    // 
    (@ accum ($($result:tt)*)) => {
        println!("Result: {:?}", stringify!($($result)*));
    };
}
```

---

## macro_export

```rust
// macro_export  crate 
#[macro_export]
macro_rules! public_macro {
    () => {};
}

//  $crate  crate
#[macro_export]
macro_rules! my_vec {
    ($($x:expr),* $(,)?) => {
        {
            let mut v = $crate::Vec::new();
            $( v.push($x); )*
            v
        }
    };
}

// macro_export :
// -  crate 
// -  use 
// - $crate  crate
// - 
```

---

## 

### 

```rust
macro_rules! vec2 {
 ($($x:expr),* $(,)?) => {
 {
 let mut v = Vec::new();
 $( v.push($x); )*
 v
 }
 };
}

let v = vec2![1, 2, 3]; //  push 
```

```rust
//  — 
macro_rules! add_ops {
 ($t:ty) => {
 impl std::ops::Add for $t { ... }
 };
}

add_ops!(i32);
add_ops!(f64);
```

### 

```rust
// derive 
#[proc_macro_derive(MyDebug)]
pub fn my_debug(input: TokenStream) -> TokenStream {
 let ast = syn::parse(input).unwrap();
 let gen = quote! { impl std::fmt::Debug for ... { ... } };
 gen.into()
}
```

### fragment specifier 

| specifier |  |
|-----------|------|
| `expr` |  |
| `ident` |  |
| `ty` |  |
| `pat` |  |
| `stmt` |  |
| `tt` | token  |
| `literal` |  |

---

## 
