# 

## 

### 

Rust ****Linear Logic Jean-Yves Girard  1987  affine ""Rust "" Copy movedrop

****

- **Linear Type******C++  `std::unique_ptr` 
- **Affine Type******Rust 
- **Relevant Type******
- **Linear Logic**

```text
:

 (Linear):     1    !x =     x.x = 
 (Affine):    1    !x = OK      x.x = 
 (Relevant):  1    !x =     x.x = OK
 (Unrestricted):   !x = OK      x.x = OK

Rust :
1.  ( drop)
2.  ( double-free  use-after-free)
3. Copy ""
```

### 

Rust 

```text
Move :

  Γ ⊢ e₁ : T      Γ, x:T ⊢ e₂ : U
    (let-move)
       Γ ⊢ let x = e₁; e₂ : U

  :  e₁  T ( x:T)  e₂  U
        let x = e₁; e₂  U
       : e₁  x

Copy :

  Γ ⊢ e₁ : T  (T: Copy)      Γ, x:T ⊢ e₂ : U
    (let-copy)
       Γ ⊢ let x = e₁; e₂ : U

  :  T  Copy let  e₁
       e₁ 

Drop :

  Γ ⊢ e : T      Γ ⊢ drop(e) : ()
    (drop)
       Γ ⊢ { ... ; drop(e) } : U
```

### Move 

Move  `memcpy` 24B  ptr+len+cap"" MIRMid-level Intermediate Representation move ""

```text
String  Move  ():

Before move:
  Stack:                Heap:
     
   s1:                  "hello"           
     ptr → h  e  l  l  o     
     len = 5          
     cap = 5       
  

After `let s2 = s1;` (memcpy 24 ):
  Stack:                Heap:
     
   s1: ()          "hello"           
     ptr     → h  e  l  l  o     
     len = ?          
     cap = ?               ↑ 
           ( Rc/Arc )
   s2:                     
     ptr 
     len = 5       
     cap = 5       
  

:  24  (ptr+len+cap)
 s1:  MIR  s1  "moved"
```

### MIR Drop Elaboration

 drop 

```text
MIR Drop Elaboration :

 MIR ():
  bb0: {
      _1 = String::new();          //  String
      _2 = _1;                     // move  _2
      _3 = _2;                     // move  _3
  }                                // _3  drop

Drop Elaboration :
  bb0: {
      _1 = String::new();
      _2 = move _1;               // _1  moved
      _3 = move _2;               // _2  moved
      drop(_3) = [return: bb1, unwind: bb2]; //  drop
  }

:
  -  drop  =  (LCD)
  - drop  = 
  -  () drop

:
  call    String::drop    ;  T::drop
  mov     rdi, [rbp-24]   ; 
  mov     rsi, [rbp-16]   ; 
  call    __rust_dealloc  ; 
```

### 

|  |  |  |
|------|------|----------|
|  |  |  |
|  (move) |  |  |
|  (drop) |  |  |
|  (borrow)  | / |  |

###  GC 

|  |  |  |  |  |
|------|----------|-----------|-----------|-------------|
| C |  `free()` |  |  |  |
| C++ | RAII / `unique_ptr` |  |  |  |
| Java/Go | GC trace |  |  |  () |
| Rust |  drop  |  |  |  (Rc ) |

###  ML/OCaml 

ML/OCaml GC ML  Rust 

```text
ML/OCaml vs Rust :

ML/OCaml:
  - 
  - : ref  ()
  - :  GC (mark-and-sweep)
  - 

Rust:
  - 
  - : &mut T ()
  - :  +  ()
  - 

ML  "linear type" :
  - Mezzo (2013): 
  - Alms (2014): ML 
  -  Rust 
```

---

## 

### 

```rust
let s1 = String::from("hello");
let s2 = s1; // move: s1 
// println!("{}", s1); // 

let x = 5;
let y = x; // Copy: x 
```

### Drop 

```rust
{
 let s = String::from("hi"); // s 
 println!("{}", s); // s 
} // s.drop() 

fn consume(s: String) { } //  drop
```

### 

$$
\text{} \xrightarrow{\text{move}} \text{} \xrightarrow{\text{scope ends}} \text{drop}
$$

### NLL

```rust
let mut s = String::from("hi");
let r1 = &s;
println!("{}", r1); // r1 
let r2 = &mut s; // OK: r1 ""NLL
```

### 

```rust
// 

//  1: 
fn process(data: String) {
    println!("{}", data);
} // data  drop

let s = String::from("hello");
process(s);      // s  data
// println!("{}", s); // : s 

//  2: 
fn create() -> String {
    let s = String::from("new");
    s // 
}

let s = create(); // s 

//  3: 
enum Shape {
    Circle(f64),
    Rect(f64, f64),
    Text(String), // String  Shape 
}

let shape = Shape::Text(String::from("hello"));
// shape  String 
//  shape drop  String  drop

//  4: 
struct Person {
    name: String,
    age: u32,
}

let p = Person {
    name: String::from("Alice"),
    age: 30,
};

let name = p.name; // move: p.name 
// println!("{}", p.name); // : p.name 
//  p.age  (Copy )
println!("{}", p.age); // OK: age  u32 (Copy)
```

### Copy 

```rust
// Copy  move
//  Copy  Clone
// Copy  derive ""

#[derive(Copy, Clone)]
struct Point {
    x: i32,
    y: i32,
}

let p1 = Point { x: 1, y: 2 };
let p2 = p1; // Copy: p1  p2 
println!("{:?} {:?}", p1, p2); // OK

//  Copy:
// -  (String, Vec<T>, Box<T>)
// -  Copy 
// -  Drop  (Copy  Drop )

// Copy :
// mov eax, [rbp - 4]   ;  p1.x
// mov [rbp - 8], eax   ;  p2.x ()
// 
```

### 

```rust
// 
enum Option<T> {
    Some(T),
    None,
}

let opt: Option<String> = Some(String::from("hello"));

match opt {
    Some(s) => {
        // s  String 
        println!("{}", s);
    } // s  drop
    None => {}
}

// opt  ( move  match )

//  as_ref()
let opt: Option<String> = Some(String::from("hello"));
if let Some(s) = opt.as_ref() {
    println!("{}", s); // s  &String
}
println!("{:?}", opt); // OK: opt 
```

---

## 

### 

```text
Rust :

1. HIR (High-level IR)
   →  (HIR borrowck)
       → 

2. MIR (Mid-level IR)
   → NLL borrowck (Non-Lexical Lifetimes)
       → 
       → Drop elaboration ( drop )
       → 

3. LLVM IR
   → noalias  ()
       → LLVM  pass 

:
  :
    &x       → borrow(x, region)     // 
    &mut x   → borrow_mut(x, region) // 
    x        → consume(x)            // /

  :
    - : 
    - : 
    - : 
```

### 

```text
Rust :

1.  use-after-free:
   
   

2.  double-free:
    drop 
    drop

3. :
   &mut T 
   

4. :
   
   

5. :
    drop (Rc )
   
```

---

## 

### 

:  — 

```rust
fn multiply(a: Vec<u32>, b: Vec<u32>) -> Vec<u32> {
 // a  b  Vec
 let mut result = vec![0; a.len() + b.len()];
 result
}
// a  b  drop (Vec  drop )
//  result 
```
