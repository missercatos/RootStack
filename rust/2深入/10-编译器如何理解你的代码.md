# 

## 

Rust 

```
 (.rs)
 → Lexer/Parse → AST (abstract syntax tree)
 → HIR (high-level IR):  IR
 → MIR (mid-level IR): borrow checkdrop elaboration
 → LLVM IR: SSA 
 → 
```

**HIR** AST →  Rust  trait 

**MIR**  HIR  borrow checkerNLLnon-lexical lifetimesdrop MIR  Rust 

**LLVM IR** MIR → LLVM IRLLVM  passnoalias SIMD `-C opt-level=3` 

rust-analyzer  libsyntax2  IDE 

Cargo MIR Clean build vs incremental 10%~50% 

---

## HIR → MIR → LLVM IR 

### 

```text
 → Token  → AST

: fn main() { let x = 1 + 2; }

Token :
  [Fn] [Ident("main")] [LParen] [RParen] [LBrace]
  [Let] [Ident("x")] [Eq] [IntLit(1)] [Plus] [IntLit(2)] [Semi]
  [RBrace]

AST ():
  Item(Fn) {
    name: "main",
    body: Block {
      stmts: [
        Let(Pat::Ident("x"), Expr::BinOp(Plus, Lit(1), Lit(2)))
      ]
    }
  }
```

###  HIR 

```text
AST →  → HIR

:
  vec![1, 2, 3] → { let mut v = Vec::new(); v.push(1); v.push(2); v.push(3); v }

HIR  AST :
  - HIR 
  - HIR 
  - HIR  desugaring
    - for loop → loop + Iterator::next
    - if let → match
    - async fn → fn  impl Future
    - ? → match + return
```

### MIR 

```text
HIR → MIR

MIR CFG

fn main() -> i32 {
    let x: i32;
    let _temp: i32;

    bb0: {
        _temp = Add(1, 2);   //  0
        x = _temp;
        return;
    }
}

MIR :
  - 
  - basic block
  - terminator
  - 
  - 
```

### LLVM IR 

```text
MIR → LLVM IR

LLVM IR ():
  define i32 @main() {
  entry:
    %x = add i32 1, 2
    ret i32 %x
  }

LLVM IR :
  - SSA (Static Single Assignment) 
  - 
  - 
  -  pass
```

### 

```text
LLVM IR →  →  →  → 

x86_64  ():
  main:
      mov     eax, 3      ; 1 + 2 = 3
      ret

 x 
```

---

## MIR 

### MIR 

```text
MIR :

fn example(x: i32) -> i32 {
    // 
    let mut _0: i32;          // 
    let _1: i32;              //  x
    let _2: i32;              // 

    // 
    bb0: {
        _2 = Add(_1, 1);      // 
        _0 = _2;              // 
        return;               // 
    }
}

:
  - bb 0  + 1 
  - : Goto, SwitchInt, Return, UnwindResume, etc.
  - SSA-like
```

### Borrow Check  MIR 

```text
 borrow check:

HIR
  
  
MIR 
  
  
MIR 
  
  

    Borrow Checker (NLL)   ← 
    -          
    -          
    -          

  
  
Drop Elaboration
  
  
MIR 
  
  
LLVM IR 
```

** borrow check  MIR  LLVM IR **

```text
:
  1. MIR  Rust 
  2. LLVM IR 
  3. MIR 
  4. NLL (Non-Lexical Lifetimes)  MIR  CFG 

 — NLL :

fn example() {
    let mut x = 5;
    let y = &x;        // y 
    println!("{}", y);
    // y 
    x += 1;            // y 
}
```

### Drop Elaboration

```text
MIR  drop :

:
  let x = String::from("hello");
  let y = x;
  // x  panic  drop x

MIR ():
  bb0: {
      x = String::new("hello");
      y = x;                    // 
      goto -> bb1;
  }
  bb1: {
      use(y);
      return;
  }
  // panic :
  bb2: {                         // cleanup block
      drop(x);                   //  bb0-bb1  panicdrop x
      resume;
  }

Drop Elaboration :
  -  drop
  -  drop 
  -  panic  cleanup
```

---

## LLVM  Pass 

###  pass

```text
LLVM  ():

 → [] → [] → [] → [] → [] → 

:

                                                

 -mem2reg              SSA     
 -simplifycfg                      
 -instcombine          a + 0 → a               
 -gvn                              
 -loop-unroll                                      
 -loop-vectorize       SIMD                   
 -slp-vectorize        SLP                           
 -inline                                           
 -dce                                            
 -dead-store-elim                                
 -licm                                       

```

### noalias 

```rust
fn add(x: &mut i32, y: &mut i32) {
    *x += 1;
    *y += 1;
    *x += 1;
}

// LLVM IR ():
//   %x = load i32, ptr %x_ptr
//   %x1 = add i32 %x, 1
//   store i32 %x1, ptr %x_ptr
//   %y = load i32, ptr %y_ptr
//   %y1 = add i32 %y, 1
//   store i32 %y1, ptr %y_ptr
//   %x2 = load i32, ptr %x_ptr
//   %x3 = add i32 %x2, 1
//   store i32 %x3, ptr %x_ptr

// LLVM  noaliasx  y :
//   %x = load i32, ptr %x_ptr
//   %y = load i32, ptr %y_ptr
//   %x_new = add i32 %x, 2    ←  x+=1 
//   %y_new = add i32 %y, 1
//   store i32 %x_new, ptr %x_ptr
//   store i32 %y_new, ptr %y_ptr
```

### 

```rust
const fn fibonacci(n: u32) -> u32 {
    match n {
        0 => 0,
        1 => 1,
        _ => fibonacci(n - 1) + fibonacci(n - 2),
    }
}

let x = fibonacci(10);
// : x = 55
// : mov eax, 55
```

---

## 

### MIR dump

```bash
#  MIR 
rustc --emit mir main.rs

#  cargo
cargo rustc -- --emit=mir

#  MIR
# :
# #![feature(rustc_attrs)]
# #[rustc_dump_mir_graph]
```

### LLVM IR 

```bash
#  LLVM IR
rustc --emit llvm-ir main.rs

#  cargo
cargo rustc -- --emit=llvm-ir

#  LLVM IR
rustc -C opt-level=2 --emit llvm-ir main.rs
```

### 

```bash
# 
rustc --emit asm main.rs

# 
rustc -C opt-level=3 --emit asm main.rs

#  cargo
cargo rustc --release -- --emit=asm
```

### cargo expand

```bash
#  cargo-expand
cargo install cargo-expand

# 
cargo expand

# 
cargo expand my_module
```

---

## 

### 

```text
:
  1.  (cargo build -p <dep>)
     - 
     - 

  2. 
     - 
     - 

  3.  trait 
     - trait 
     -  trait bound 

  4. Borrow Check
     - NLL 

  5. LLVM
     - 
     - 

  6. 
     - debug 
     - release + LTO 
```

### 

```text
1.  cargo check  cargo build
   -  borrow check 
   - 
   -  3-5 

2. 
   -  trait object (Box<dyn Trait>) 
   - 

3.  sccache
   - 
   - 

4.  mold Linux
   -  lld  3-5 
   - cargo install mold
   - RUSTFLAGS="-C link-arg=-fuse-ld=mold" cargo build

5.  ThinLTO
   -  fat LTO 
   - -C lto=thin

6.  crate
   -  crate 
   -  crate
   - 
```

### 

```bash
# 
cargo build --timings

# 
cargo build -Z time-passes

# 
cargo build -Z time-passes 2>&1 | grep codegen
```

---

## ThinLTO vs Full LTO

### Full LTO

```text
Full LTO:
  1.  crate  LLVM IR 
  2. 
  3. 
  4. 

   .rlib →  LLVM  →  → 
```

### ThinLTO

```text
ThinLTO:
  1.  crate  LLVM IR
  2. summary crate 
  3. 
  4.  LTO
  5.  Full LTO

  .rlib →  LLVM  +  →  → 
```

###  LTO

```text
 LTO :

  - ThinLTO: 
  - Full LTO: 

  :
    - :  LTO (-C lto=off)
    - CI:  ThinLTO (-C lto=thin)
    - :  Full LTO (-C lto=fat)
```

---

## 

###  MIR

```bash
rustc --emit mir main.rs
```

###  LLVM IR

```bash
rustc --emit llvm-ir main.rs
```

### 

```bash
rustc --emit asm -C opt-level=3 main.rs
```

### 

|  |  |
|------|------|
| `-C opt-level=0..3/s/z` |  |
| `-C debuginfo=0..2` |  |
| `-C lto=fat/thin` |  |
| `-C panic=abort/unwind` | panic  |
| `-C target-cpu=native` |  CPU  |
| `-Z time-passes` |  |

### lint 

```rust
#[deny(warnings)]
#[allow(unused_variables)]
#[warn(missing_docs)]
```

---

## 
